# K-0011: issue-audit 계획 감사의 회귀 방지 예외가 spec 본문 표기만 인정해 plan 전용 항목·템플릿 고정 블록을 예외로 읽지 못한다

- **유형**: known issue
- **등재일**: 2026-09-25
- **출처**: 이슈 #104 계획 감사 발견 F-4 (1차 F-1 잔여)
- **위험도**: 낮음(LOW)
- **수용 사유**: 예외 표기 위치를 plan 전용 항목과 템플릿 고정 블록까지 넓히려면 issue-audit `--plan` 관점 2 문구와 issue-plan 템플릿 Task N 고정 블록을 함께 고쳐야 해 #104(모델 기록 형식) 범위를 넘는다. #104에서는 회귀 방지 항목 4건에 표시를 두고 R1-5·Task N 항목은 의존 관계 문장으로 남겼으며, 감사인이 그 문장을 읽어 낮음으로 판정해 감사 자체는 성립했다.
- **재검토 조건**: issue-audit `--plan` 관점 2의 예외 문구 변경 시, K-0009 헬퍼 결정화 착수 시, 또는 다른 이슈의 계획 감사에서 템플릿 고정 블록·의존 관계 표기 항목이 가짜 `[D]` 후보로 다시 올라올 때
- **상태**: 수용

## 내용

issue-audit `--plan` 관점 2는 구현 전에도 통과하는 `[D]` 명령을 가짜 `[D]` 후보로 올리며, 예외는 spec 항목 본문의 "회귀 방지 항목" 표시만 인정한다. spec DoD에 없는 plan 전용 항목과 plan 템플릿 Task N 고정 블록의 헤더 집합·수행 모델 검사는 issue-work 접기 기준대로 의존 관계 문장으로 적혀 있어도 표기 위치가 규칙과 맞지 않아 후보로 올라온다. 계획 감사마다 같은 항목이 낮음 발견으로 반복될 수 있다.

## 재검토 이력

없음

<details>
<summary>근거·배경 펼치기</summary>

- 위험도 산출: 영향 축 "기술 품질 의견"(표기 위치 문제라 검증 보장에 영향이 없고, 감사인이 의존 관계 문장을 읽어 판정한다) × 발생확률 축 "통상 사용"(규칙 공백이라 모든 이슈의 계획 감사에서 반복된다) → 낮음(LOW).
- 검토했으나 채택하지 않은 대안 1: #104 spec에 Task N 두 항목의 예외 표기를 두는 것. 두 항목은 plan 템플릿 고정 블록과 같은 문장이라 이슈 단위로 고치면 템플릿 SSoT에서 이탈한다.
- 검토했으나 채택하지 않은 대안 2: plan 전용 항목(Task 3 집계 헬퍼 검사)을 spec R4로 복제하는 것. 표시는 항목이 있는 plan 본문에 이미 있고, 복제하면 Task 3 완료 기준과 중복된다.
- 관련: K-0009. 헬퍼 결정화 시 기계가 읽는 예외 표기가 필요하며, 그때 표기 위치 규칙도 함께 정해진다. 이슈 #104 계획 감사 1차 F-1이 2차 F-4로 잔여.

</details>
