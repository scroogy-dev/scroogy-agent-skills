# K-0005: install-skills 설치 결과 템플릿이 배포 제외 패턴 리터럴을 복제한다

- **유형**: 기술부채
- **등재일**: 2026-08-08
- **출처**: 이슈 #84 / audit 발견 F-4
- **위험도**: 낮음(LOW)
- **수용 사유**: 현재 템플릿의 리터럴은 5단계 패턴과 일치해 실해가 없고, 불일치는 5단계 제외 패턴이 변경될 때만 발현한다. 5단계에는 이미 "단일 출처" 명시가 있어 패턴 변경 작업자가 참조처를 함께 훑을 단서가 존재한다.
- **재검토 조건**: `install-skills/SKILL.md` 설치 절차 5단계의 배포 제외 패턴 값(`tests/`, `*.test.*`)을 추가·변경·삭제할 때, 또는 설치 결과 보고의 제외 패턴 표기가 실제 배포와 다른 사례가 관측될 때 (패턴을 설명하는 서술문의 문체·구두점 정비는 계기로 보지 않는다. 이슈 #87 재검토에서 좁혔다)
- **상태**: 수용

## 내용

`install-skills/templates/install-result-template.md`의 `적용 옵션 내역` 중 `배포 제외 패턴` 행이 `tests/`, `*.test.*` 리터럴을 직접 보유한다. 제외 패턴의 단일 출처는 `install-skills/SKILL.md` 설치 절차 5단계인데 템플릿이 같은 값을 복제해, 5단계 패턴 변경 시 템플릿 갱신을 빠뜨리면 실제 배포와 사용자 보고가 달라진다.

## 재검토 이력

| 일자 | 계기 | 판단 | 결과 |
|------|------|------|------|
| 2026-08-11 | 이슈 #87 audit(OpenAI, GPT-5.6 Sol) 발견 F-4 재제기 | 반려 (조건 미충족) | 조건은 5단계 배포 제외 패턴 변경이다. 이번 변경은 패턴을 설명하는 인접 서술문의 문체 정비이고 `--exclude` 패턴 값은 그대로다. 템플릿 리터럴과 5단계 패턴이 여전히 일치해 실해가 없다. 조건 문구를 패턴 값의 추가·변경·삭제로 명시했다 |

<details>
<summary>근거·배경 펼치기</summary>

- **발생확률**: 특수 조건·엣지. 현재 값은 일치하며, 불일치는 5단계 패턴을 추가·변경하고 템플릿 갱신을 빠뜨린 경우에만 발생한다.
- **영향**: 기능 저하. 배포 자체는 5단계가 결정하므로 설치 결과물은 정상이고, 사용자 보고의 패턴 표기만 실제와 어긋난다.
- **채택하지 않은 대안**: 템플릿의 해당 행을 "5단계에서 실제 적용한 `--exclude` 패턴 그대로" 자리표시자로 교체하는 안. 한 줄 보정으로 해소 가능하나, 낮음(LOW) 기본 처리(원장 이관)에 따라 사용자가 이관을 선택했다.
- 이슈 #84 3차 audit(OpenAI, GPT-5.6 Sol)이 낮음(LOW) 등급으로 제기했고 audit 자신도 템플릿에서 리터럴 제거를 권고했다. `--response` 검토에서 등급을 유지하고 이관했다.

</details>
