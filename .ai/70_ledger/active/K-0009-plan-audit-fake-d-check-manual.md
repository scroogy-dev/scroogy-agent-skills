# K-0009: issue-audit 계획 감사의 가짜 `[D]` 사전 판별이 결정적 헬퍼로 분리되지 않았다

- **유형**: 기술부채
- **등재일**: 2026-09-09
- **출처**: 이슈 #100 (이슈 본문 검토 필요 2, Task 0에서 사용자 승인)
- **위험도**: 낮음(LOW)
- **수용 사유**: 헬퍼가 "구현 전에도 통과해야 하는 회귀 방지 항목"을 예외로 가르려면 spec·plan 템플릿에 기계가 읽는 예외 표기가 필요하다. 그 표기 도입은 템플릿 계약 변경이라 첫 도입 범위를 넘고, 계획 감사 자체가 이번에 처음 생겨 실사용 사례가 없다. 첫 도입은 감사인 수동 절차로 두고 판별이 갈리는 사례를 먼저 모은다.
- **재검토 조건**: 계획 감사 2회 이상에서 가짜 `[D]` 판별 결과가 감사 모델 간에 갈린 사례 관측 시, 또는 spec·plan 템플릿에 회귀 방지 예외 표기가 도입될 때
- **상태**: 수용

## 내용

issue-audit `--plan` 2단계 관점 2 "가짜 `[D]` 사전 판별"은 spec DoD·plan 완료 기준의 `[D]` 검증 명령을 구현 전 트리에서 실행해 통과하는 명령을 후보로 올리는 절차다. 회귀 방지 항목 예외는 spec 본문의 "회귀 방지 항목" 문구를 감사인이 읽어 판단하며, 명령 추출·실행·예외 판정을 하는 헬퍼가 없다. 감사 모델에 따라 예외 인정과 후보 선정이 갈릴 수 있고, 판별 결과를 `[D]`로 재검증할 수 없다.

## 재검토 이력

없음

<details>
<summary>근거·배경 펼치기</summary>

- 위험도 산출: 영향 축 "기능 저하"(판별이 갈려도 감사 자체는 성립하고 `--response` 승인 게이트가 남는다) × 발생확률 축 "특수 조건·엣지"(회귀 방지 항목이 있는 spec에서 감사 모델이 예외 문구를 다르게 읽을 때) → 낮음(LOW).
- 검토했으나 채택하지 않은 대안: `check-plan.sh`에 `--fake-d` 모드를 두고 접기 안 bash 블록을 추출·실행하는 방식. issue-work 러너가 템플릿 게이트 명령을 추출하는 방식과 같아 구현은 가능하나, 회귀 방지 예외를 기계가 읽으려면 spec 항목에 고정 표기(예: `[D][회귀]`)가 필요해 템플릿 계약 변경을 동반한다.
- 관련 논의: 이슈 #100 본문 검토 필요 2, spec `전제 (Assumptions)`의 "검토 필요 2(가짜 `[D]` 결정화)".

</details>
