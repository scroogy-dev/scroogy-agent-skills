# Issue #21 실행요약 issue-work --clear 시점·순서 모호함 개선

> 스펙: [issue-0021-spec.md](./issue-0021-spec.md) | 계획: [issue-0021-plan.md](./issue-0021-plan.md)

## 다음 작업

> ✅ 모든 작업이 완료되었습니다.

---

## Task별 수행 결과

### Task 1: SKILL.md --clear 시점·순서 규칙 명문화

- **결과**: 완료
- **수행 내용 요약**:
  - `--clear` 용도 아래에 **시점** 항목 추가 — "구현·리뷰가 끝나 PR 머지를 올리기 직전에 실행", 머지 후 실행 시 archive 이관이 `main` 직접 푸시가 되는 문제까지 명시
  - 4단계(archive 이관)에 "PR 머지 전, 작업 브랜치에서 수행해 같은 PR에 포함" 규칙 추가
  - 3단계(이슈 댓글 질의)에 "git 변경이 아니므로 머지 전·후 어느 시점이든 가능"을 명시해 archive 이관과 타이밍 분리
- **특이 사항**: 기존 1~5단계 구성은 변경 없이 유지(규칙 문장만 추가). spec의 라인 참조(101/107/111행)가 현재 SKILL.md와 일치함을 확인

---

### Task 2: 이슈 완료 시 절차에 순서 규칙 반영

- **결과**: 완료
- **수행 내용 요약**:
  - `## 이슈 완료 시` 절차 3단계(archive 이관)에 "PR 머지 전, 작업 브랜치에서 수행해 같은 PR에 포함" 문장 추가
  - `--clear` 4단계와 **동일한 문장**을 사용해 두 절차의 archive 이관 타이밍 표현을 일치시킴
- **특이 사항**: `--clear` 4단계는 `## 이슈 완료 시`의 1·2·4단계를 포함한다는 기존 관계(SKILL.md 말미 주석)와 모순 없음

---

### Task 3: issue-workflow-template.md 동기화

- **결과**: 완료
- **수행 내용 요약**:
  - `templates/issue-workflow-template.md`의 `## 이슈 완료 시` 3단계에 SKILL.md와 동일한 "PR 머지 전, 작업 브랜치에서 수행해 같은 PR에 포함" 문장 추가
- **특이 사항**: 이 repo의 실제 가이드 인스턴스 `.ai/90_issues/active/issue-workflow.md`도 변경 전 템플릿 복사본이라 동일 동기화 대상 — spec 범위(`templates/`)에는 없으므로 Task 4에서 점검·제안

---

### Task 4: 일관성 검증

- **결과**: 완료
- **수행 내용 요약**:
  - SKILL.md·템플릿 두 파일의 archive 이관 문구를 대조 — "PR 머지 전, 작업 브랜치에서 수행해 같은 PR에 포함…" 문장이 SKILL.md 2곳(72·115행)·템플릿 1곳(38행)에 한 글자도 다르지 않게 일치함을 확인
  - DoD 5항목 전부 충족 확인 (시점 정의 / archive 머지 전 규칙 / 댓글 타이밍 분리 / 템플릿 동기화 / 표현 일치)
  - 검증 중 발견한 이 repo의 `active/issue-workflow.md` 구버전을 사용자 승인 후 동일 규칙으로 동기화
- **특이 사항**: `active/issue-workflow.md` 동기화는 spec 범위(`templates/`) 밖이었으나, 이 repo가 스킬을 self-dogfooding하므로 다음 이슈에서 올바른 절차를 따르도록 사용자 확인 후 함께 반영

---

### Task 5: 새 이슈 시작 시 issue-workflow.md 동기화 동작 명문화 (스코프 확장)

- **결과**: 완료
- **수행 내용 요약**:
  - SKILL.md "새 이슈 시작 시" 2단계를 "없으면 생성, 있으면 내용 불일치 시 갱신(덮어쓰기), 동일하면 유지"로 확장 — 템플릿(SSoT) 개선이 기존 active 인스턴스에 자동 전파됨
  - `--workflow-only` 용도·동작을 "내용 비교 없이 무조건 덮어쓰는 강제 복구"로 차별화하고, 일반 흐름(조건부 갱신)과 명시적으로 대조
  - spec 제목·목표·범위·DoD 확장 반영 (포함 항목 +1, 비포함에 '타 repo 일괄 갱신 제외' 추가, DoD +2 → 총 7항목)
- **특이 사항**: 사용자 제보 — `--workflow-only`는 과거 archive 이관 오동작으로 가이드가 유실됐던 사고의 복구 장치. 이 의도를 용도 문구(유실·손상 복구)에 반영. Task 4에서 손으로 했던 동기화가 이제 일반 흐름으로 자동화됨
