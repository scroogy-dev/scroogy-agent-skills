# Issue #74 감사 리포트 git-pr 긴 산출물 중복 생성 제거

> 감사 일시: 2026-08-07  
> 감사 모델: OpenAI, GPT-5  
> 감사 회차: 1차 (이전 리포트: 없음)  
> 감사 대상 브랜치: `issue-0074` (`origin/main...HEAD`)  
> 스펙 출처: [./issue-0074-spec.md](./issue-0074-spec.md) (작성 시점 경로는 `.ai/90_issues/active/issue-0074/issue-0074-spec.md`, --clear로 이관)

---

## 1단계: 적합성 검증 (Compliance Check)

### 요구사항 대조

| # | 요구사항 | 판정 | 근거 |
|---|----------|------|------|
| 1 | `git-pr` 3단계에서 본문 전문 대신 파일 경로와 요약 제시 | 충족(PASS) | `git-pr/SKILL.md:84-90`에 본문 전문 출력 금지, 본문 경로, 이슈·리스크·결정사항 요약, 경로 제시의 승인 효력을 명시했다. |
| 2 | PR 제목·본문의 저장 위치와 파일명 규약 명시 | 충족(PASS) | `git-pr/SKILL.md:73-82`에 `.ai/99_workspace/`, `pr-<이슈번호>-title.md`, `pr-<이슈번호>-body.md`, 번호·통합 배포 규칙을 명시했다. |
| 3 | head SHA 대조 통과 후 삭제 질의와 `issue-work --clear` 회수 경로 명시 | 충족(PASS) | `git-pr/SKILL.md:147-149`에 대조·보고 후 삭제 질의, 실패·불일치 시 보존, 다음 `--clear` 회수를 순서대로 규정했다. |
| 4 | `issue-work --clear` 댓글 초안에 파일 1회 생성·경로+요약 규칙 적용 | 충족(PASS) | `issue-work/SKILL.md:178-211`에 댓글 파일 생성, 전문 출력 금지, 승인 후 `--body-file` 등록, 본문 승격 목록 제시, 5단계 정리를 명시했다. |

### 완료의 정의(DoD) 대조

| # | DoD 항목 | 판정 | 근거 |
|---|----------|------|------|
| 1 | `git-pr` 저장 위치와 파일명 규약 2종 | 충족(PASS) | 스펙의 고정 문구 검증 명령 결과 위반 0건. |
| 2 | `git-pr` 전문 출력 금지·삭제 질의·`--clear` 회수 문구 | 충족(PASS) | 스펙의 고정 문구 검증 명령 결과 위반 0건. |
| 3 | `issue-work --clear` 댓글 파일 규약·전문 출력 금지 | 충족(PASS) | 스펙의 고정 문구 검증 명령 결과 위반 0건. |
| 4 | 파일 1회 생성 → 경로+요약 → 승인 게이트 유지, 기존 규정과 무모순 | 부분 충족(PARTIAL) | 파일 기반 제시와 승인 질의는 유지했으나, 승인한 파일 내용의 동일성을 제출 직전에 검증하지 않는다(F-1). `git-pr` 2단계의 재출력 금지와 3단계의 제목 전문 출력도 문면상 충돌한다(F-2). |

### 범위 검증

- **스펙 비포함(Out) 침범 여부**: 없음. `git-review`, 짧은 산출물, 설치된 스킬 사본은 변경하지 않았다.
- **스펙에 없는 추가 구현 여부**: `git-pr`의 포크·승인 거절·생성 수단 부재·MCP host 제한 종료 문구 4곳도 파일 경로 안내로 바뀌었다. 스펙 전제에서 명시 승인한 정합 보정이므로 범위 위반으로 보지 않는다.

### 도메인/계약 정합성

- `.ai/30_contract/`, `.ai/40_domain/`, `.ai/50_adr/`, `.ai/60_codebase/`에 이번 변경과 직접 관련된 문서·색인이 없다.
- `.ai/10_rules/writing-principles.md`의 접기 금지 항목은 `git-pr` 요약과 `issue-work` 본문 승격 목록에 보존됐다.
- `.ai/70_ledger/`에 이번 발견과 같은 수용 항목은 없다.

---

## 2단계: 비판적 검증 (Critical Review)

### 발견 사항

| # | 위험도 | 영향 | 발생확률 | 분류 | 계보 | 설명 | 관련 파일 |
|---|--------|------|----------|------|------|------|-----------|
| F-1 | 중간(MEDIUM) | 스펙·기능 달성 차단 | 특수 조건·엣지 | 암묵적 가정 | 신규 | 승인 후 외부 등록 전 파일이 바뀌지 않는다고 가정해, 승인하지 않은 본문이 PR·이슈 댓글로 공개될 수 있다. | `git-pr/SKILL.md`, `issue-work/SKILL.md` |
| F-2 | 중간(MEDIUM) | 기능 저하 | 통상 사용 | 부작용 | 신규 | `git-pr`는 후속 단계의 텍스트 재출력을 금지하면서 바로 다음 단계에서 제목 전문 출력을 요구해 실행 규칙이 충돌한다. | `git-pr/SKILL.md` |
| F-3 | 낮음(LOW) | 기능 저하 | 특수 조건·엣지 | 엣지케이스 | 신규 | 이전 이슈의 잔존 PR 파일을 다음 이슈의 `--clear`에서 보존하면 현재 이슈 archive로 잘못 이관될 수 있다. | `git-pr/SKILL.md`, `issue-work/SKILL.md` |

### 기등재 참조 항목

해당 없음.

### 이전 발견 추적

해당 없음.

### 상세 분석

#### F-1: 승인 시점의 파일 내용이 제출 시점까지 고정되지 않는다

- **위험도**: 중간(MEDIUM)
- **영향 축**: 스펙·기능 달성 차단
- **발생확률 축**: 특수 조건·엣지
- **분류**: 암묵적 가정
- **계보**: 신규
- **권장 조치**:
  - PR 제목·본문 또는 이슈 댓글을 제시할 때 파일별 내용 해시를 승인값과 함께 고정한다.
  - `gh`·MCP 외부 쓰기 직전에 해시를 재검증하고, 달라졌으면 제출을 중단한 뒤 경로·요약을 다시 제시해 재승인받는다.
  - MCP 폴백도 재검증한 동일 스냅샷의 제목·본문만 전달하도록 규정한다.
- **완료 기준**:
  - 승인 뒤 파일을 수정한 반례에서 PR 생성·이슈 댓글 등록이 중단된다.
  - 수정된 파일은 새 요약 제시와 명시적 재승인 없이는 외부에 등록되지 않는다.
  - 변경이 없는 정상 경로에서는 기존 `--body-file` 흐름이 유지된다.

<details>
<summary>근거·상세 설명 펼치기</summary>

- **설명**: `git-pr/SKILL.md:86-90`은 제출되는 파일 자체를 승인 대상으로 삼지만, `git-pr/SKILL.md:123-128`은 제출 시점의 현재 파일을 다시 읽는다. 저장소·커밋 SHA는 `git-pr/SKILL.md:91-105`에서 불변값으로 보관·재검증하지만 제목·본문에는 동등한 검증이 없다. `issue-work/SKILL.md:178-185`의 댓글 파일도 같은 구조다.
- **영향**: 승인과 외부 공개 사이에 사용자·다른 에이전트·도구가 파일을 수정하면 승인 화면의 경로·요약과 실제 PR·댓글 내용이 달라진다. 승인 게이트가 존재해도 승인한 콘텐츠를 보장하지 못한다.

</details>

#### F-2: 제목 재출력 규칙이 같은 절 안에서 충돌한다

- **위험도**: 중간(MEDIUM)
- **영향 축**: 기능 저하
- **발생확률 축**: 통상 사용
- **분류**: 부작용
- **계보**: 신규
- **권장 조치**:
  - 2단계의 포괄 문구를 “본문 전문은 이후 대화에 다시 출력하지 않는다”로 좁히고, 짧은 제목은 3단계에서 전문 제시하는 예외임을 같은 위치에 명시한다.
  - `--draft`를 포함한 최종 제시 문구가 제목 전문 + 본문 경로·요약이라는 하나의 규칙만 가리키게 정리한다.
- **완료 기준**:
  - `git-pr` 2·3단계를 함께 읽었을 때 제목 전문 출력 여부가 하나로만 해석된다.
  - 본문 전문 출력 금지와 제목 전문 제시가 동시에 명시되며 서로를 부정하지 않는다.

<details>
<summary>근거·상세 설명 펼치기</summary>

- **설명**: `git-pr/SKILL.md:73`은 제목·본문을 묶어 “이후 단계는 대화에 텍스트를 다시 쓰지 않는다”고 규정한다. 반면 `git-pr/SKILL.md:87`과 plan Task 1은 제목 전문을 그대로 제시하라고 요구한다.
- **영향**: 일반 규칙을 우선하면 승인 단계에서 제목 전문이 빠지고, 구체 규칙을 우선하면 앞선 재출력 금지를 위반한다. 현재 의도는 추론할 수 있지만 DoD가 요구한 문면상 무모순 강제에는 미달한다.

</details>

#### F-3: 다음 이슈의 `--clear`가 이전 이슈 PR 파일의 보존 목적지를 잘못 잡을 수 있다

- **위험도**: 낮음(LOW)
- **영향 축**: 기능 저하
- **발생확률 축**: 특수 조건·엣지
- **분류**: 엣지케이스
- **계보**: 신규
- **권장 조치**:
  - 5단계에서 `pr-<이슈번호>-*`와 `issue-<번호>-comment.md`의 번호를 정규화해 일치하는 archive 이슈 디렉터리를 보존 목적지로 제안한다.
  - 일치하는 archive가 없거나 통합 PR이라 목적지가 모호하면 삭제·현재 이슈 이관을 자동 제안하지 말고 목적지를 질의한다.
- **완료 기준**:
  - Issue #74를 먼저 `--clear`한 뒤 남은 `pr-74-body.md`를 Issue #75의 `--clear`에서 보존해도 `archive/issue-0075/`로 이동하지 않는다.
  - 번호 매핑이 모호한 파일은 사용자 확인 전 이동·삭제되지 않는다.

<details>
<summary>근거·상세 설명 펼치기</summary>

- **설명**: `git-pr/SKILL.md:149`은 선행 `--clear` 뒤 생긴 보존 파일도 다음 `--clear`가 회수한다고 규정한다. 그러나 `issue-work/SKILL.md:215-217`은 보존 파일을 정리 중인 현재 `archive/issue-<번호>/`로 옮기도록 제안하며, 파일명에 든 원래 이슈 번호를 대조하지 않는다.
- **영향**: PR 파일은 삭제되지 않더라도 다른 이슈의 archive에 섞여 출처와 이후 경로 참조가 어긋날 수 있다.

</details>

---

## 종합 의견

- **판정**: 부분 충족(PARTIAL). 요구한 파일명·제시·정리 문구와 기존 회귀 테스트는 충족하지만, 의미 게이트는 F-1·F-2 때문에 닫히지 않았다.
- **검증 결과**: 스펙 `[D]` 명령 위반 0건, plan-summary 게이트 `0 / 0`, `bash issue-work/tests/run-tests.sh` 28/28 통과, `git diff --check origin/main...HEAD` 통과.
- **우선순위**: 승인 콘텐츠 동일성을 보장하는 F-1을 먼저 보정하고 재감사한다. F-2는 같은 보정 회차에서 문구를 단일화하는 것이 적절하다. F-3은 기본 처리 기준상 원장 이관 대상이다.
