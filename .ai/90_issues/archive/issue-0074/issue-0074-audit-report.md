# Issue #74 감사 리포트 git-pr 긴 산출물 중복 생성 제거

> 감사 일시: 2026-08-07  
> 감사 모델: OpenAI, GPT-5  
> 감사 회차: 2차 (이전 리포트: [./issue-0074-audit-report-1.md](./issue-0074-audit-report-1.md))  
> 감사 대상 브랜치: `issue-0074` (`origin/main...HEAD`, `406dc23..0559bea`; 구현 관련 미커밋 변경 없음)  
> 스펙 출처: [./issue-0074-spec.md](./issue-0074-spec.md) (작성 시점 경로는 `.ai/90_issues/active/issue-0074/issue-0074-spec.md`, --clear로 이관)

---

## 종합 의견

- **판정**: 충족(PASS). 요구사항 4건과 DoD 4건을 모두 충족하며, 2단계 신규·재제기 발견은 없다.
- **이전 발견**: F-2는 문면 충돌이 제거되어 닫혔다. F-1·F-3은 보정되지 않았으나 각각 원장 `K-0003`·`K-0004`로 수용됐고 재검토 조건이 충족되지 않아 현재 위험도·카테고리 집계에서 제외한다.
- **검증 결과**: 스펙 `[D]` 검증 위반 0건, plan-summary 게이트 `0 / 0`, `issue-work` 테스트 28/28, 셸 구문 및 `git diff --check origin/main...HEAD` 통과.
- **교차모델 조건**: 일반 Task 수행 모델은 Anthropic이고 감사 모델은 OpenAI이므로 벤더 분리 조건을 충족한다. summary의 audit 모델·Task N 결과 반영은 사용자가 수행할 후속 기록이다.

## 요약

- 1단계 적합성: 충족(PASS) 8건 / 미충족(FAIL) 0건 / 부분 충족(PARTIAL) 0건 / 판정 불가(N/A) 0건
- 2단계 위험도: 높음(HIGH) 0건 / 중간(MEDIUM) 0건 / 낮음(LOW) 0건 / 정보(INFO) 0건
- 기등재 참조: 2건 (원장 기등재와 일치 — 위험도·카테고리 집계에 포함하지 않음)
- 이전 발견: 닫힘 1건 / 잔여 2건 (잔여: F-1, F-3 — 각각 `K-0003`, `K-0004`로 수용)

| 카테고리 | 건수 |
|----------|------|
| 기능 | 0 |
| 아키텍처 | 0 |
| 버그 | 0 |
| 보안 | 0 |
| 코드품질 | 0 |
| 성능 | 0 |
| 테스트 | 0 |

---

## 1단계: 적합성 검증 (Compliance Check)

### 요구사항 대조

| # | 요구사항 | 판정 | 근거 |
|---|----------|------|------|
| 1 | `git-pr` 3단계에서 본문 전문 대신 파일 경로와 요약 제시 | 충족(PASS) | `git-pr/SKILL.md:84-90`이 제목 전문, 본문 파일 경로, 이슈·리스크·결정사항 요약과 파일 경로 제시의 승인 효력을 함께 규정한다. |
| 2 | PR 제목·본문의 저장 위치와 파일명 규약 명시 | 충족(PASS) | `git-pr/SKILL.md:73-82`가 `.ai/99_workspace/`, 제목·본문 파일명, 번호 표기, 통합 배포, 디렉토리 부재·수정 요청 경로를 명시한다. |
| 3 | head SHA 대조 통과 후 삭제 질의와 `issue-work --clear` 회수 경로 명시 | 충족(PASS) | `git-pr/SKILL.md:139-149`가 head SHA 대조·보고 뒤 삭제 질의, 실패·불일치 시 보존, 후속 `--clear` 회수를 순서대로 강제한다. |
| 4 | `issue-work --clear` 댓글 초안에 파일 1회 생성·경로+요약 규칙 적용 | 충족(PASS) | `issue-work/SKILL.md:178-211`이 파일 1회 생성, 경로·요약·승격 목록 제시, 전문 출력 금지, 승인 후 `--body-file` 등록과 같은 실행의 5단계 회수를 규정한다. |

### 완료의 정의(DoD) 대조

| # | DoD 항목 | 판정 | 근거 |
|---|----------|------|------|
| 1 | `git-pr` 저장 위치와 파일명 규약 2종 | 충족(PASS) | 스펙의 고정 문구 검증 명령 결과 위반 0건. |
| 2 | `git-pr` 전문 출력 금지·삭제 질의·`--clear` 회수 문구 | 충족(PASS) | 스펙의 고정 문구 검증 명령 결과 위반 0건. 삭제 질의는 head SHA 대조·보고 뒤에만 실행된다. |
| 3 | `issue-work --clear` 댓글 파일 규약·전문 출력 금지 | 충족(PASS) | 스펙의 고정 문구 검증 명령 결과 위반 0건. |
| 4 | 파일 1회 생성 → 경로+요약 → 승인 게이트 유지, 기존 규정과 무모순 | 충족(PASS) | `git-pr/SKILL.md:73`은 본문 전문만 재출력 금지 대상으로 좁히고 제목 전문을 명시적 예외로 둔다. `:86-97`과 `issue-work/SKILL.md:178-211`은 파일 자체를 승인 대상으로 유지하면서 경로·요약 제시, 수정 시 같은 파일 갱신, 승인 후 해당 파일 제출을 일관되게 규정한다. |

### 범위 검증

- **스펙 비포함(Out) 침범 여부**: 없음. `git-review`, 짧은 산출물, 설치된 스킬 사본은 변경하지 않았다.
- **스펙에 없는 추가 구현 여부**: 포크·승인 거절·생성 수단 부재·MCP host 제한의 종료 문구 4곳은 스펙 전제에서 명시 승인한 정합 보정이다. 원장 2건과 summary 갱신은 1차 감사 `--response`의 기록 산출물이며 기능 범위를 확장하지 않는다.

### 도메인/계약 정합성

- `.ai/30_contract/index.md`, `.ai/40_domain/index.md`, `.ai/50_adr/index.md`에 이번 변경을 제약하는 관련 문서는 없다. ADR 0001은 결정적 헬퍼 테스트 배치 규칙이라 문서 전용 변경과 무관하다.
- `.ai/60_codebase/index.md`는 비어 있으며 실제 `origin/main...HEAD`의 `git-pr/SKILL.md`·`issue-work/SKILL.md`를 직접 확인했다. 색인 갱신이 필요한 불일치는 없다.
- `.ai/10_rules/writing-principles.md`의 접기 금지 대상은 `git-pr` 요약과 `issue-work` 본문 승격 목록에 유지된다.

---

## 2단계: 비판적 검증 (Critical Review)

### 발견 사항

신규·재제기 발견 없음.

### 상세 분석

해당 없음.

### 기등재 참조 항목

<details>
<summary>기등재 참조 표 펼치기</summary>

| 원장 항목 | 상태 | 대조한 발견 내용 | 재검토 조건 충족 | 처리 |
|-----------|------|------------------|------------------|------|
| K-0003 | 수용 | 1차 F-1 — 승인 뒤 외부 등록 전 제목·본문·댓글 파일의 내용 동일성을 재검증하지 않는다. | 미충족 — 등재 뒤 보정은 `git-pr` 2단계의 제목 예외 문구만 수정했고 3·4단계 승인 게이트는 재수정하지 않았다. 승인 뒤 파일 변조로 미승인 내용이 게시된 사례도 관측되지 않았다. | 기등재 K-0003 참조만. 신규 `F-` 번호와 위험도·카테고리 집계에서 제외한다. |
| K-0004 | 수용 | 1차 F-3 — 다음 이슈의 `--clear`가 이전 이슈 PR 파일을 현재 이슈 archive의 보존 대상으로 제안할 수 있다. | 미충족 — 등재 뒤 `issue-work --clear` 5단계 보존 목적지 규정은 수정되지 않았고 실제 오이관 사례도 관측되지 않았다. | 기등재 K-0004 참조만. 신규 `F-` 번호와 위험도·카테고리 집계에서 제외한다. |

</details>

### 이전 발견 추적

<details>
<summary>이전 발견 추적 표 펼치기</summary>

| 이전 발견 | 완료 기준 항목 | 판정 (닫힘/잔여) | 근거 |
|-----------|----------------|------------------|------|
| F-1 | 승인 뒤 파일 수정 반례에서 PR 생성·이슈 댓글 등록 중단 | 잔여 | 파일 내용 해시·스냅샷 재검증은 추가되지 않았다. 원장 `K-0003` 수용 상태이며 재검토 조건은 미충족이다. |
| F-1 | 수정된 파일을 새 요약 제시와 재승인 없이 외부 등록하지 않음 | 잔여 | 외부 개입으로 파일이 바뀐 경우를 탐지하는 규정이 없다. 원장 `K-0003`으로 추적한다. |
| F-1 | 변경 없는 정상 경로에서 기존 `--body-file` 흐름 유지 | 닫힘 | `git-pr/SKILL.md:121-128`과 `issue-work/SKILL.md:185`가 파일 기반 정상 제출 흐름을 유지한다. |
| F-2 | 2·3단계에서 제목 전문 출력 여부가 하나로만 해석됨 | 닫힘 | `git-pr/SKILL.md:73`이 본문 전문만 금지하고 제목은 3단계 예외라고 명시하며, `:86-88`이 같은 규칙을 구체화한다. |
| F-2 | 본문 전문 출력 금지와 제목 전문 제시가 서로를 부정하지 않음 | 닫힘 | 두 규칙의 대상이 본문과 제목으로 분리되어 문면 충돌이 제거됐다. |
| F-3 | Issue #74 PR 파일이 Issue #75 archive로 제안·이동되지 않음 | 잔여 | `issue-work/SKILL.md:215-217`은 여전히 현재 정리 이슈의 archive만 보존 목적지로 제안한다. 원장 `K-0004`로 추적한다. |
| F-3 | 번호 매핑이 모호한 파일은 목적지 확인 전 이동·삭제되지 않음 | 잔여 | 5단계의 일반 사용자 확인은 유지되지만 파일명 번호 정규화·불일치 전용 질의는 없다. 원장 `K-0004` 수용 범위다. |

</details>

<details>
<summary>검증 증거 펼치기</summary>

- 스펙 `[D]` 고정 문구 검증 3개 묶음: 위반 출력 0건.
- plan-summary Task 헤더 집합: 차이 0건.
- 일반 Task 결과 확정 게이트: `0`.
- 완료·부분 완료 Task 수행 모델 게이트: `0`.
- `bash -n issue-work/tests/run-tests.sh`: 통과.
- `bash issue-work/tests/run-tests.sh`: 28 통과, 0 실패.
- `git diff --check origin/main...HEAD`: 통과.
- 구현 변경 파일: `git-pr/SKILL.md`, `issue-work/SKILL.md`; 감사 대응 기록: 이슈 문서와 원장 `K-0003`·`K-0004`.

</details>
