# Issue #74 실행요약 git-pr 긴 산출물 중복 생성 제거 — 본문은 99_workspace 파일로, 대화에는 요약만

> 스펙: [issue-0074-spec.md](./issue-0074-spec.md) | 계획: [issue-0074-plan.md](./issue-0074-plan.md)

## 다음 작업

> ✅ 모든 작업이 완료되었습니다.

## 모델 기록

<!--
이슈 단위 요약(대표값)이다. 세부 SSoT는 아래 `Task별 수행 결과`의 `수행 모델`이며,
한 구분에 여러 모델이 관여했으면 대표 모델을 적고 세부는 Task별 기록을 본다.

형식: "벤더, 모델명". 설계·구현·audit 어느 칸이든 어느 벤더·모델이든 가능하다
(예: Anthropic, Claude Opus 4.8 (claude-opus-4-8) / OpenAI, GPT-5.x / Google, Gemini 3.x).
설계 모델과 구현 모델은 다를 수 있어 칸을 나눈다 — 같으면 같은 값을 적는다.
"audit 모델 ≠ 구현 모델" 조건을 나중에 명령으로 확인 가능하도록 기록한다. 모델 전환은 사람이 수행하므로 기계로 강제하기 어렵다.
구현에 여러 벤더가 관여했으면 audit 모델의 벤더는 Task별 `수행 모델`의 모든 벤더와도 달라야 한다
— 대표 행 비교만으로는 audit 벤더가 구현 일부에 참여한 경우를 놓친다.
-->

| 구분 | 모델 |
|------|------|
| 설계 모델 | Anthropic, Claude Fable 5 (claude-fable-5) |
| 구현 모델 | Anthropic, Claude Opus 5 (claude-opus-5) |
| audit 모델 | OpenAI, GPT-5 |

---

## Task별 수행 결과

<!--
Task 0 및 일반 실행 Task(Task N 제외) 블록의 지표 4종은 사후 집계용이라 표기를 고정한다
(값이 없어도 필드는 지우지 않는다. Task N 예외의 근거는 아래 Task N 블록 주석).
- 수행 모델: "벤더, 모델명" — `모델 기록` 표와 같은 형식. 한 Task를 모델 전환·재시도로 둘 이상이 수행했으면
  관여한 모델을 전부 ` / `로 나열한다 — 하나만 남기면 누락된 벤더가 audit을 맡아도 Task N의 강화 조건을 통과한다.
  `-`는 미착수·스킵 Task 전용이며, `결과`가 완료·부분 완료면 실제 값을 필수로 기록한다
  (`-`로 남기면 Task N의 교차모델 강화 조건이 공집합이 되어 우회가 된다)
- audit 발견: 이 Task와 관련된 audit 발견사항 건수 — 없으면 `0건` (`-` 금지)
- 보정 반영: `--response` 항목별 승인을 통과해 실제로 보정한 건수 — 없으면 `0건` (`-` 금지)
- 재시도: 같은 Task에서 재수정·재시도한 횟수 — 없으면 `0회` (`-` 금지)

한 발견이 여러 Task 산출물에 걸쳐도 가장 직접 수정된 주 Task 하나에만 귀속한다 —
`audit 발견`·`보정 반영` 모두 그 주 Task에서 1회만 세고 관련된 다른 Task에는 세지 않는다.
Task 축에서도 이슈 단위 합계가 중복으로 부풀지 않게 하기 위함이다(아래 회차 축 중복 제외와 같은 취지).

수치 3종은 회차별 값이 아니라 이슈 전체 누적이다 — 여러 차수 감사가 같은 발견을 재확인해도
1건으로 세고(중복 제외), 새 회차 값으로 덮어쓰지 않는다. 보정률 사후 집계의 분모·분자가 회차 반복으로 부풀지 않게 하기 위함이다.

수치 3종에 `-`를 쓰지 않는 이유: 아래 스니펫이 `[0-9]+`만 추출하므로 `-`인 Task는
합계에서 조용히 빠지고, 전부 `-`면 합계 자체가 빈 문자열이 된다. `-` 허용은 비수치 필드(`수행 모델`)에만 둔다.

수치 3종의 기본값은 주석이 아니라 리터럴 `0건`/`0회`로 둔다: 주석 placeholder는 렌더링 값이
비어 있는데도 집계기가 주석 안 숫자를 실제 값으로 오인하고, `미기록` 같은 형식 위반도 걸러지지 않는다.

보정률은 별도 필드로 두지 않고 `보정 반영 / audit 발견`으로 사후 집계한다.
아래 스니펫이 동작하도록 `- **<필드>**: <숫자><단위>` 표기를 지킨다 (줄 시작, 굵게, 콜론 뒤 숫자).

  # 이슈 하나의 보정률
  found=$(grep -E '^- \*\*audit 발견\*\*:' issue-0074-summary.md | grep -oE '[0-9]+' | paste -sd+ - | bc)
  fixed=$(grep -E '^- \*\*보정 반영\*\*:' issue-0074-summary.md | grep -oE '[0-9]+' | paste -sd+ - | bc)
  echo "보정률: ${fixed}/${found}"

  # 모델별 집계는 `수행 모델` 행과 함께 뽑는다
  grep -E '^- \*\*(수행 모델|audit 발견|보정 반영|재시도)\*\*:' issue-0074-summary.md
-->

### Task 0 (고정): 구현 시작 게이트 — 전제·모호점 확인

<!--
plan 템플릿의 첫 고정 Task에 대응하는 블록이다. 삭제하지 말 것.
질의로 해소한 전제는 spec `## 전제 (Assumptions)`에 남기고, 여기에는 수행 사실만 적는다.
나열한 항목이 없었으면 `수행 내용 요약`에 `전제 누락 없음` 한 줄을 기록한다.
-->

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: 잔여 모호점 2건을 사용자 질의로 해소해 spec 전제에 반영 — (1) `.ai/99_workspace/` 부재 시 디렉토리 생성 후 그대로 사용(대화 출력 폴백 없음, `git-review` 선례 정합), (2) 3단계 밖 "제목·본문을 남긴 채 종료" 문구 4곳도 파일 경로 안내로 함께 갱신. 질의 없이 해소한 항목 1건(파일명 이슈번호 표기: PR은 `pr-74-`, 댓글은 `issue-0074-`)도 근거와 함께 전제에 기록.
- **특이 사항**: 설계 단계(2026-08-07)에서 이슈 #74의 미결 논점 2건(정리 타이밍 A+B 병행, `--clear` 댓글 동일 규칙 적용)을 사용자 질의로 선해소하고 spec 전제에 기록함 — 구현 착수 시 잔여 모호점만 점검하면 된다. Task 0에서 확정한 문구 4곳 갱신은 spec 범위(3단계)를 소폭 넘는 확장이며, Task 1·2의 [D] anchor 검증에는 영향이 없다.

---

### Task 1: git-pr 파일 1회 생성·경로+요약 제시 전환

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 2건
- **보정 반영**: 1건
- **재시도**: 0회
- **수행 내용 요약**: `git-pr/SKILL.md` 2단계에 저장 경로 표(제목·본문)와 파일명 규약을 추가 — 이슈번호는 앞자리 0 없이, 통합 배포는 첫 이슈 번호, `.ai/99_workspace/` 부재 시 생성, 수정 요청 시 같은 파일 갱신. 3단계 제시를 "제목 전문 + 본문 파일 경로 + 요약(이슈 목록·리스크·결정사항)"으로 대체하고 전문 출력 금지 문구와 "파일 경로 제시도 제시에 해당" 문구를 명시. 4단계 `gh pr create` 예시의 `'<제목 파일>'` placeholder를 실제 경로로 치환.
- **특이 사항**: Task 0에서 확정한 대로 3단계 밖 종료 문구 4곳(1단계 포크 미지원, 3단계 승인 거절, 4단계 생성 수단 부재·MCP host 제한)을 "파일 경로 안내 후 종료"로 함께 갱신 — spec 범위를 소폭 넘는 확장이라 audit 시 검토 대상.
  `--response` 검토(2026-08-07): audit 발견 2건 — F-2(제목 재출력 규칙이 2단계 포괄 문구와 문면상 충돌)는 **반영**해 2단계 문구 범위를 "본문 전문"으로 좁히고 한 줄 제목의 예외를 같은 위치에 명시. F-1(승인한 제목·본문 파일의 내용 동일성이 제출 직전에 미검증)은 위험도를 중간(MEDIUM) → 낮음(LOW)으로 재산출해 원장 `K-0003`으로 **이관** — 변경 전에도 제출값이 파일이었고(회귀 아님) 승인·제출 매체는 오히려 일치하게 됐으며, 내용 동일성 검증은 spec 전제("게이트 절차 불변")를 넘는 추가 보장이다.

---

### Task 2: git-pr 임시 파일 정리 타이밍(A+B) 반영

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 1건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: `git-pr/SKILL.md` 4단계 말미(head SHA 대조·PR 보고 이후)에 삭제 질의 절차를 추가 — 거절 시 보존, 대조 전에는 질의하지 않고 보존만(재시도에 승인 텍스트 필요), 보존분은 `issue-work --clear` 5단계가 회수하며 `--clear` 선행 실행 회차도 다음 `--clear`에서 정리됨을 명시.
- **특이 사항**: 삭제 질의를 4단계 마지막 항목으로 배치해 "대조 통과 이후" 순서를 절차 순서 자체로 강제. plan [QD] 기준(생성 실패·불일치 시 사본이 먼저 사라지는 경로 없음)에 대응하는 예외 문구를 별도 하위 항목으로 분리했다.
  `--response` 검토(2026-08-07): audit 발견 1건 — F-3(다음 이슈 `--clear` 5단계가 보존 파일의 목적지를 정할 때 파일명의 이슈 번호를 대조하지 않음)은 낮음(LOW) 기본 처리대로 원장 `K-0004`로 **이관**. 5단계가 목적지를 제안하고 사용자 확인을 거치며, 실현 조건이 3중이라 자동 오이관이 아니다.

---

### Task 3: issue-work --clear 이슈 댓글 동일 규칙 적용

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: `issue-work/SKILL.md` `--clear` 3단계를 갱신 — 초안을 `.ai/99_workspace/issue-<번호>-comment.md`에 1회만 쓰고, 대화에는 경로·요약·승격 목록만 제시하며 전문 출력 금지. 승인 시 `gh issue comment --body-file`로 그대로 등록하고, 파일은 같은 실행의 5단계가 회수. 파일명은 디렉토리명 표기(패딩 유지), `gh` 인자는 앞자리 0 제거로 두 표기가 다름을 예시와 함께 명시.
- **특이 사항**: 기존 본문 승격 규칙(결정사항·미해결 리스크·다음 액션)이 파일 안에서만 지켜지면 대화로 승인하는 사용자에겐 접힌 것과 같으므로, **승격 목록을 대화 제시분에도 포함**하도록 규정을 덧붙였다. 거절·gh 부재 시 초안 파일이 남아 5단계 정리 대상이 됨도 명시. `issue-work/tests/run-tests.sh` 28건 통과(회귀 없음).

---

### Task N (고정): 교차모델 issue-audit 검증 — 사용자 수동 수행

<!--
plan 템플릿의 마지막 고정 Task에 대응하는 블록이다. 삭제하지 말 것.
사용자가 직접 수행하며, 구현 AI는 이 블록을 대신 채우지 않는다.
지표 4종을 두지 않는다 — 수행 모델은 위 `모델 기록` 표의 `audit 모델` 행이 SSoT이고,
audit 발견·보정 반영은 이 Task가 만들어낸 값이라 각 대상 Task 블록에 집계된다.
-->

- **결과**: 완료
- **수행 내용 요약**: 사용자가 OpenAI GPT-5로 2회 수행 — 1차([./issue-0074-audit-report-1.md](./issue-0074-audit-report-1.md)) 부분 충족(PARTIAL), 발견 3건(F-1·F-2 중간(MEDIUM), F-3 낮음(LOW)). `--response` 검토로 F-2만 반영하고 F-1·F-3은 원장 `K-0003`·`K-0004`로 이관. 2차([./issue-0074-audit-report.md](./issue-0074-audit-report.md)) 충족(PASS) — 요구사항 4건·DoD 4건 전부 충족, 신규·재제기 발견 0건, F-2 닫힘 확인.
- **특이 사항**: 구현은 Anthropic, audit은 OpenAI로 벤더 분리 조건 충족. 기등재 `K-0003`·`K-0004`는 재검토 조건 미충족이라 2차에서 참조만 하고 위험도·카테고리 집계에서 제외 — 두 항목은 원장 `active/`에 수용 상태로 남는다.
