# Issue #76 실행요약 issue-audit 리포트 역피라미드 구조 재배치

> 스펙: [issue-0076-spec.md](./issue-0076-spec.md) | 계획: [issue-0076-plan.md](./issue-0076-plan.md)

## 다음 작업

> ✅ 모든 작업이 완료되었습니다.

## 모델 기록

<!--
이슈 단위 요약(대표값)이다. 세부 SSoT는 아래 `Task별 수행 결과`의 `수행 모델`이며,
한 구분에 여러 모델이 관여했으면 대표 모델을 적고 세부는 Task별 기록을 본다.

형식: "벤더, 모델명". 설계·구현·audit 어느 칸이든 어느 벤더·모델이든 가능하다
(예: Anthropic, Claude Opus 4.8 (claude-opus-4-8) / OpenAI, GPT-5.x / Google, Gemini 3.x).
설계 모델과 구현 모델은 다를 수 있어 칸을 나눈다 — 같으면 같은 값을 적는다.
"audit 모델 ≠ 구현 모델" 조건을 나중에 명령으로 확인 가능하도록 기록한다. 모델 전환은 사람이 수행하므로 기계로 강제하기 어렵다.
구현에 여러 벤더가 관여했으면 audit 모델의 벤더는 Task별 `수행 모델`의 모든 벤더와도 달라야 한다
— 대표 행 비교만으로는 audit 벤더가 구현 일부에 참여한 경우를 놓친다.
-->

| 구분 | 모델 |
|------|------|
| 설계 모델 | Anthropic, Claude Fable 5 (claude-fable-5) |
| 구현 모델 | Anthropic, Claude Fable 5 (claude-fable-5) |
| audit 모델 | OpenAI, GPT-5 |

---

## Task별 수행 결과

<!--
Task 0 및 일반 실행 Task(Task N 제외) 블록의 지표 4종은 사후 집계용이라 표기를 고정한다
(값이 없어도 필드는 지우지 않는다. Task N 예외의 근거는 아래 Task N 블록 주석).
- 수행 모델: "벤더, 모델명" — `모델 기록` 표와 같은 형식. 한 Task를 모델 전환·재시도로 둘 이상이 수행했으면
  관여한 모델을 전부 ` / `로 나열한다 — 하나만 남기면 누락된 벤더가 audit을 맡아도 Task N의 강화 조건을 통과한다.
  `-`는 미착수·스킵 Task 전용이며, `결과`가 완료·부분 완료면 실제 값을 필수로 기록한다
  (`-`로 남기면 Task N의 교차모델 강화 조건이 공집합이 되어 우회가 된다)
- audit 발견: 이 Task와 관련된 audit 발견사항 건수 — 없으면 `0건` (`-` 금지)
- 보정 반영: `--response` 항목별 승인을 통과해 실제로 보정한 건수 — 없으면 `0건` (`-` 금지)
- 재시도: 같은 Task에서 재수정·재시도한 횟수 — 없으면 `0회` (`-` 금지)

한 발견이 여러 Task 산출물에 걸쳐도 가장 직접 수정된 주 Task 하나에만 귀속한다 —
`audit 발견`·`보정 반영` 모두 그 주 Task에서 1회만 세고 관련된 다른 Task에는 세지 않는다.
Task 축에서도 이슈 단위 합계가 중복으로 부풀지 않게 하기 위함이다(아래 회차 축 중복 제외와 같은 취지).

수치 3종은 회차별 값이 아니라 이슈 전체 누적이다 — 여러 차수 감사가 같은 발견을 재확인해도
1건으로 세고(중복 제외), 새 회차 값으로 덮어쓰지 않는다. 보정률 사후 집계의 분모·분자가 회차 반복으로 부풀지 않게 하기 위함이다.

수치 3종에 `-`를 쓰지 않는 이유: 아래 스니펫이 `[0-9]+`만 추출하므로 `-`인 Task는
합계에서 조용히 빠지고, 전부 `-`면 합계 자체가 빈 문자열이 된다. `-` 허용은 비수치 필드(`수행 모델`)에만 둔다.

수치 3종의 기본값은 주석이 아니라 리터럴 `0건`/`0회`로 둔다: 주석 placeholder는 렌더링 값이
비어 있는데도 집계기가 주석 안 숫자를 실제 값으로 오인하고, `미기록` 같은 형식 위반도 걸러지지 않는다.

보정률은 별도 필드로 두지 않고 `보정 반영 / audit 발견`으로 사후 집계한다.
아래 스니펫이 동작하도록 `- **<필드>**: <숫자><단위>` 표기를 지킨다 (줄 시작, 굵게, 콜론 뒤 숫자).

  # 이슈 하나의 보정률
  found=$(grep -E '^- \*\*audit 발견\*\*:' issue-0076-summary.md | grep -oE '[0-9]+' | paste -sd+ - | bc)
  fixed=$(grep -E '^- \*\*보정 반영\*\*:' issue-0076-summary.md | grep -oE '[0-9]+' | paste -sd+ - | bc)
  echo "보정률: ${fixed}/${found}"

  # 모델별 집계는 `수행 모델` 행과 함께 뽑는다
  grep -E '^- \*\*(수행 모델|audit 발견|보정 반영|재시도)\*\*:' issue-0076-summary.md
-->

### Task 0 (고정): 구현 시작 게이트 — 전제·모호점 확인

<!--
plan 템플릿의 첫 고정 Task에 대응하는 블록이다. 삭제하지 말 것.
질의로 해소한 전제는 spec `## 전제 (Assumptions)`에 남기고, 여기에는 수행 사실만 적는다.
나열한 항목이 없었으면 `수행 내용 요약`에 `전제 누락 없음` 한 줄을 기록한다.
-->

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Fable 5 (claude-fable-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: 전제 누락 없음 — 대상 파일(issue-audit/SKILL.md·리포트 템플릿)과 카테고리 표 원본(git-review/SKILL.md)을 대조한 결과, 구현에 필요한 결정이 spec 전제 6건과 plan 작업 내용에 모두 문서화되어 있음을 확인.
- **특이 사항**: diff 검증 패턴에 걸리는 행이 git-review/SKILL.md에 정확히 헤더+7행뿐임을 사전 확인 — 원본 쪽 오탐 없음.

---

### Task 1: 카테고리 축 도입 — SKILL.md 정의·리포트 표 반영

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Fable 5 (claude-fable-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: issue-audit/SKILL.md 2단계 "검증 관점" 아래에 `#### 카테고리` 소절 신설 — git-review 원본 표(헤더+7행)를 문구까지 그대로 복사하고 두 축 역할 구분(카테고리=집계 축, 관점=탐색 축)과 동기화 주석 기재. 리포트 템플릿 발견 사항 표 헤더의 `분류` 열을 `카테고리` 열로 교체, 표 아래 주석을 카테고리 7종 안내로 변경, 상세 블록 `분류` 필드를 `카테고리`·`관점` 2필드로 교체.
- **특이 사항**: 완료 기준 `[D]` 2건 검증 통과 (출력 0건). 동기화 주석은 위험도 매트릭스 선례 문구("스킬 단독 실행을 위해 복사해 둠 — 한쪽 변경 시 양쪽 동기화")를 미러링.

---

### Task 2: 리포트 템플릿 역피라미드 재배치 — 요약 신설·표 접기

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Fable 5 (claude-fable-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: `## 종합 의견`을 머리말 바로 아래 최상단으로 이동하고 그 아래 `## 요약` 신설 — 1단계 판정 4종·2단계 위험도 4등급·카테고리 집계 표(7행, `상태` 열 없음)·기등재 참조 건수·이전 발견 닫힘/잔여(잔여 번호 자리 포함) 배치. `### 기등재 참조 항목`·`### 이전 발견 추적`을 `### 상세 분석` 뒤로 이동하고 각 표를 `<details>` 접기로 감싸며 "건수·잔여 번호는 요약에 노출(접기 금지 정합)" 안내 주석 기재.
- **특이 사항**: 완료 기준 `[D]` 3건 검증 통과 (출력 0건). 카테고리 집계 표의 건수 예시 표기는 git-review 결과 템플릿(#75) 관례를 따름.

---

### Task 3: SKILL.md 정합 갱신 — 출력 요약 형식·구조 참조

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Fable 5 (claude-fable-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: 출력 요약 형식 2단계 블록에 카테고리별 건수 줄(7종 전부 나열) 추가, 기등재 참조 줄의 집계 제외 문구를 "위험도·카테고리 집계"로 확장. 3단계 결과 기록의 "'감사 모델' 줄(#26)" 줄 번호 앵커를 "리포트 머리말의 '감사 모델' 줄"로 교체. 본문의 템플릿 구조 서술 전수 확인 — `### 기등재 참조 항목` 섹션명·발견 사항 표의 집계 대상 역할이 새 구조에서도 유지되어 추가 정합 수정 불요.
- **특이 사항**: 완료 기준 `[D]` 2건 통과에 더해 spec `완료의 정의` `[D]` 6건 전체 재실행 통과 (출력 0건). `[QD]` 1건(본문·템플릿 모순 없음)은 Task N 교차모델 audit 채점 대상.

---

### Task N (고정): 교차모델 issue-audit 검증 — 사용자 수동 수행

<!--
plan 템플릿의 마지막 고정 Task에 대응하는 블록이다. 삭제하지 말 것.
사용자가 직접 수행하며, 구현 AI는 이 블록을 대신 채우지 않는다.
지표 4종을 두지 않는다 — 수행 모델은 위 `모델 기록` 표의 `audit 모델` 행이 SSoT이고,
audit 발견·보정 반영은 이 Task가 만들어낸 값이라 각 대상 Task 블록에 집계된다.
-->

- **결과**: 완료
- **수행 내용 요약**: 사용자가 OpenAI, GPT-5로 1차 감사 수행 — 리포트는 `./issue-0076-audit-report.md` (작성 시점 경로는 `.ai/99_workspace/issue-0076-audit-report.md`, --clear로 이관). 요구사항 6건·DoD 7건 전부 충족(PASS), 범위 침범 없음, 2단계 발견 0건. 발견 0건이라 `--response` 보정 대상 없음.
- **특이 사항**: 리포트 검증 근거에 spec `[D]` 6건 재실행 통과, 회귀 테스트 통과(install-skills 20/20, issue-work 28/28), 원장 K-0002 재검토 조건 미충족(위험도 축·매트릭스 본문 무변경) 확인이 기재됨.
