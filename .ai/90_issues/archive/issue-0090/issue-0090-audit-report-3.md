# Issue #90 감사 리포트 3차 재감사

> 감사 일시: 2026-08-19  
> 감사 모델: OpenAI, GPT-5.6  
> 감사 회차: 3차 (이전 리포트: `issue-0090-audit-report-2.md`)  
> 감사 대상 브랜치: issue-0090 (미커밋 보정 포함)  
> 스펙 출처: `./issue-0090-spec.md` (작성 시점 경로는 `.ai/90_issues/active/issue-0090/issue-0090-spec.md`, --clear로 이관)

---

## 종합 의견

부분 충족이다. 2차의 F-5는 뒤 확장 열을 허용하는 정책을 SSoT와 회귀 테스트에 명시해 닫혔다. 다만 같은 발견을 두 번 센 summary 지표와, 필수 경로가 확장 열에만 있어도 표준 행으로 오인하는 거짓 통과를 새로 확인했다. 명시된 DoD 명령, 9개 러너의 317개 단언, 8개 스킬의 임시 설치 검증은 모두 통과했다.

## 요약

- 1단계 적합성: 충족(PASS) 14건 / 미충족(FAIL) 0건 / 부분 충족(PARTIAL) 1건 / 판정 불가(N/A) 0건
- 2단계 위험도: 높음(HIGH) 0건 / 중간(MEDIUM) 2건 / 낮음(LOW) 0건 / 정보(INFO) 0건
- 기등재 참조: 6건 (원장 기등재와 일치, 위험도·카테고리 집계에 포함하지 않음)
- 이전 발견: 닫힘 1건 / 잔여 0건 (F-5 닫힘)

| 카테고리 | 건수 |
|----------|------|
| 기능 | 0 |
| 아키텍처 | 0 |
| 버그 | 중간(MEDIUM) 2 |
| 보안 | 0 |
| 코드품질 | 0 |
| 성능 | 0 |
| 테스트 | 0 |

---

## 1단계: 적합성 검증 (Compliance Check)

### 요구사항 대조

| # | 요구사항 | 판정 | 근거 |
|---|----------|------|------|
| 1 | `architecture.md`에 디자인 원칙과 고정 소절 5종 신설 | 충족(PASS) | 원칙 근거 4종, 위치별 역할, 체크리스트, 예외, 기존 체계 관계가 있다. 고정 앵커 검증도 통과했다. |
| 2 | 임계 수치 사용 여부와 근거 기록 | 충족(PASS) | 임계 수치를 두지 않는 결정과 형식 충족을 막기 위한 근거가 `### 판단 체크리스트`에 있다. |
| 3 | `.ai/AI-CONTEXT.md`에 디자인 원칙 포인터 추가 | 충족(PASS) | `### 결정화 판단`이 `10_rules/architecture.md`를 가리킨다. |
| 4 | 15개 스킬의 판정·처리·근거를 타당하게 전수 조사 | 충족(PASS) | 15개 스킬이 세 판정과 허용 처리 어휘로 기록됐다. code-map은 실제 `check` 절차에 맞게 교정되고 K-0006으로 이관됐다. |
| 5 | 결정화 대상 구현과 나머지 항목 종결 | 부분 충족(PARTIAL) | 8개 스킬의 헬퍼·테스트와 code-map 이관은 완료됐다. 다만 `check-context.sh`가 필수 경로의 열 위치를 확인하지 않아 거짓 통과한다(F-8). |
| 6 | 비포함 범위 준수와 실익이 낮은 항목 종결 | 충족(PASS) | ADR 0001 본문과 검증 레벨 체계를 바꾸지 않았다. 조사 결과용 상주 문서도 만들지 않았다. |

### 완료의 정의(DoD) 대조

| # | DoD 항목 | 판정 | 근거 |
|---|----------|------|------|
| 1 | 디자인 원칙 섹션과 소절 5종 존재 | 충족(PASS) | 스펙의 고정 앵커 검증 명령이 무출력으로 통과했다. |
| 2 | 임계값 결정과 근거 한 줄 | 충족(PASS) | 수치 임계값을 두지 않는 결정과 근거가 실질적으로 남아 있다. |
| 3 | ADR 0001과 검증 레벨 3종 관계 표기 | 충족(PASS) | `ADR 0001`·`[D]`·`[QD]`·`[ND]` 앵커와 관계 설명이 있다. |
| 4 | AI-CONTEXT 스킬 작성 규칙 포인터 | 충족(PASS) | 스펙의 포인터 검증 명령이 무출력으로 통과했다. |
| 5 | 조사 결과 15행·판정·근거 | 충족(PASS) | 형식 게이트가 15행을 확인했다. 판정 근거도 실제 스킬 절차와 대조했다. |
| 6 | `결정화 대상` 처리 어휘 확정 | 충족(PASS) | 8건은 `이번 이슈 반영`, code-map은 `원장 K-0006`으로 기록됐다. |
| 7 | 모든 `*/tests/run-tests.sh` 통과 | 충족(PASS) | 9개 러너의 317개 단언이 통과했다. F-8 반례는 현재 테스트 집합에 없다. |
| 8 | 신규 헬퍼·테스트의 ADR 0001 배치와 배포 제외 | 충족(PASS) | 헬퍼는 각 `scripts/`, 테스트는 각 `tests/`에 있다. 8개 스킬을 임시 설치한 결과 `verify-install.sh`가 PASS했고 11개 헬퍼는 실행 가능하며 테스트는 제외됐다. |
| 9 | 비대상 행 처리 어휘 확정 | 충족(PASS) | 허용 어휘 검증 명령이 무출력으로 통과했다. |

### 범위 검증

- **스펙 비포함(Out) 침범 여부**: 없음. ADR 0001 본문, 검증 레벨 체계, 결정화 불가 스킬의 리팩터링을 건드리지 않았다.
- **스펙에 없는 추가 구현 여부**: 없음. F-5 대응의 정책 명시는 기존 사용자 작성분 보존 원칙과 실제 허용 동작을 일치시킨 감사 보정이다.

### 도메인/계약 정합성

- 계약·도메인 관련 문서: 관련 항목 없음.
- ADR 0001: 테스트 동일 위치, 경량 러너, 설치 시 개발 전용 경로 제외 규칙과 맞는다.
- 코드베이스 색인: 등록된 항목이 없어 실제 소스와 테스트를 직접 확인했다. 색인 갱신 필요 의견은 없다.

---

## 2단계: 비판적 검증 (Critical Review)

### 발견 사항

| # | 위험도 | 영향 | 발생확률 | 카테고리 | 계보 | 설명 | 관련 파일 |
|---|--------|------|----------|----------|------|------|-----------|
| F-7 | 중간(MEDIUM) | 기능 저하 | 통상 사용 | 버그 | 신규 | 2차의 F-5 잔여를 새 발견처럼 다시 세어 summary 누적 지표가 7/7로 부풀었다. | `./issue-0090-summary.md:48`, `:116` |
| F-8 | 중간(MEDIUM) | 스펙·기능 달성 차단 | 특수 조건·엣지 | 버그 | 신규 | 필수 경로가 `파일` 열에 없고 사용자 확장 열에만 있어도 표준 행이 존재한다고 판정한다. | `ai-workspace/scripts/check-context.sh:133`, `ai-workspace/SKILL.md:250` |

### 상세 분석

#### F-7: 재감사 잔여 발견을 중복 집계함

- **위험도**: 중간(MEDIUM)
- **영향 축**: 기능 저하
- **발생확률 축**: 통상 사용
- **카테고리**: 버그
- **관점**: 부작용, 암묵적 가정
- **계보**: 신규
- **권장 조치**:
  - 같은 `F-n`은 감사 회차와 보정 횟수에 관계없이 `audit 발견`·`보정 반영`에서 한 번만 센다.
  - 이번 3차 발견을 반영할 때 F-1~F-8의 고유 번호와 Task별 귀속을 기준으로 지표를 다시 계산한다.
- **완료 기준**:
  - F-5는 1차 발견과 2차 잔여 확인을 합쳐 한 건으로 집계한다.
  - 이 리포트의 F-7·F-8을 기록한 뒤 Task 2는 F-1 한 건, Task 3은 F-2~F-8 일곱 건으로 기록한다.
  - `보정 반영`은 항목별 승인 뒤 실제로 보정한 고유 발견 번호만 센다.
  - `summarize-metrics.sh` 출력과 보존된 리포트의 고유 발견 번호 수가 일치한다.

<details>
<summary>근거·상세 설명 펼치기</summary>

- summary는 같은 발견을 여러 차수에서 재확인해도 한 건으로 세라고 명시한다.
- 보존된 1·2차 리포트의 고유 번호는 F-1~F-6 여섯 건이다. 2차 발견 표의 F-5는 1차 F-5의 잔여다.
- 현재 summary는 Task 2를 1건, Task 3을 6건으로 적어 총 7건을 낸다. `summarize-metrics.sh`도 형식만 검사하므로 잘못된 값 그대로 `7/7`을 출력한다.
- 2차 보정 전의 Task 3 값 5건은 F-2~F-6과 일치했다.

</details>

#### F-8: 필수 경로의 열 위치를 확인하지 않음

- **위험도**: 중간(MEDIUM)
- **영향 축**: 스펙·기능 달성 차단
- **발생확률 축**: 특수 조건·엣지
- **카테고리**: 버그
- **관점**: 엣지케이스, 누락된 검증
- **계보**: 신규
- **권장 조치**:
  - 표준 2행은 행 전체 부분 문자열이 아니라 첫 번째 `파일` 셀을 정규화해 정확히 대조한다.
  - 뒤 확장 열을 허용하는 정상·반례를 함께 회귀 테스트로 고정한다.
- **완료 기준**:
  - `context-loading.md`와 `writing-principles.md` 경로는 첫 번째 `파일` 셀에 있을 때만 해당 표준 행으로 인정한다.
  - 앞 3열이 정상인 4열 표에서 필수 경로를 확장 열로 옮긴 두 반례는 종료 코드 1과 해당 행 누락 사유를 낸다.
  - 정상 3열 표와 필수 경로가 `파일` 셀에 있는 4열 확장 표는 종료 코드 0을 유지한다.
  - 모든 `*/tests/run-tests.sh`가 통과한다.

<details>
<summary>근거·상세 설명 펼치기</summary>

- SSoT는 두 표준 행의 `파일` 셀 값을 각각 정한다. 사용자 확장 열은 뒤에만 허용한다.
- 구현은 `index($0, needle)`로 행 전체를 검색한다. 경로가 어느 셀에 있는지는 보지 않는다.
- `파일` 셀을 `.ai/10_rules/custom.md`로 바꾸고 확장 `비고` 셀에 `.ai/10_rules/context-loading.md`를 둔 4열 입력을 실행했다. 결과는 무출력, 종료 코드 0이었다.
- F-5는 헤더 이름·순서와 확장 열 허용 계약의 문제였다. 이 발견은 표준 행의 셀 위치를 확인하지 않는 별도 원인이다.

</details>

### 기등재 참조 항목

<details>
<summary>기등재 참조 표 펼치기</summary>

| 원장 항목 | 상태 | 대조한 발견 내용 | 재검토 조건 충족 | 처리 |
|-----------|------|------------------|------------------|------|
| K-0001 | 수용 | ai-workspace update-3 루트 상주 파일 회귀 fixture 부재 | 미충족 | 기등재 K-0001 참조. 이번 변경은 update-4 검사다. |
| K-0002 | 수용 | issue-audit 축 경계 우선 규칙 부재 | 미충족 | 기등재 K-0002 참조. 축 값·정의·매트릭스는 바뀌지 않았다. |
| K-0003 | 수용 | 승인한 제목·본문 파일의 제출 직전 내용 동일성 미검증 | 미충족 | 기등재 K-0003 참조. 이번 보정은 ai-workspace 검사 계약이다. |
| K-0004 | 수용 | `--clear` 보존 목적지의 이슈 번호 미대조 | 미충족 | 기등재 K-0004 참조. 5단계 보존 목적지 규정은 바뀌지 않았다. |
| K-0005 | 수용 | 설치 결과 템플릿의 제외 패턴 리터럴 복제 | 미충족 | 기등재 K-0005 참조. 제외 패턴 값은 바뀌지 않았다. |
| K-0006 | 수용 | code-map `check` 정합성 점검의 결정적 헬퍼 부재 | 미충족 | 기등재 K-0006 참조. 검사 표가 바뀌지 않았고 `.ai/60_codebase/` 색인도 없다. |

</details>

### 이전 발견 추적

<details>
<summary>이전 발견 추적 표 펼치기</summary>

| 이전 발견 | 완료 기준 항목 | 판정 (닫힘/잔여) | 근거 |
|-----------|----------------|------------------|------|
| F-5 | 프로젝트 규칙 표의 3열 계약 확정 | 닫힘 | 정확히 3열만 허용하는 대신 앞 3열의 이름·순서를 고정하고 뒤 확장 열을 허용한다고 SSoT에 명시했다. 사용자 작성분 보존 원칙과 맞는 대체 조치다. |
| F-5 | 2열 구버전과 4열 이상 입력의 처리 구분 | 닫힘 | 2열은 기존처럼 열 확장 대상으로 실패하고, 4열 이상은 명시된 정책에 따라 통과한다. 두 경로의 기대 동작이 계약과 테스트에 고정됐다. |
| F-5 | dev/doc 템플릿·현재 안내도·4열 회귀 검증 | 닫힘 | 세 표준 입력과 4열 확장 입력이 통과한다. 이름이 다르거나 순서가 바뀐 3열 표는 실패한다. |
| F-5 | 모든 테스트 러너 통과 | 닫힘 | 9개 러너의 317개 단언이 모두 통과했다. |

</details>
