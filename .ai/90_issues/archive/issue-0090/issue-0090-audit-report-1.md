# Issue #90 감사 리포트 전체 스킬 결정화 여지 전수 조사 및 개선

> 감사 일시: 2026-08-13  
> 감사 모델: OpenAI, GPT-5  
> 감사 회차: 1차 (이전 리포트: 없음)  
> 감사 대상 브랜치: issue-0090  
> 스펙 출처: `./issue-0090-spec.md` (작성 시점 경로는 `.ai/90_issues/active/issue-0090/issue-0090-spec.md`, --clear로 이관)

---

## 종합 의견

부분 충족이다. 문서 앵커, 처리 어휘, 9개 테스트 러너, ADR 0001 배치와 실제 설치 제외는 모두 통과했다. 그러나 `code-map`의 명시적 `check` 절차를 누락해 전수 조사 판정이 틀렸고, 새 헬퍼 5곳이 잘못된 입력을 종료 코드 0으로 통과시킨다. F-1은 보정 후 재검증이 필요하다.

## 요약

- 1단계 적합성: 충족(PASS) 13건 / 미충족(FAIL) 0건 / 부분 충족(PARTIAL) 2건 / 판정 불가(N/A) 0건
- 2단계 위험도: 높음(HIGH) 1건 / 중간(MEDIUM) 5건 / 낮음(LOW) 0건 / 정보(INFO) 0건
- 기등재 참조: 5건 (원장 기등재와 일치, 위험도·카테고리 집계에 포함하지 않음)
- 이전 발견: 해당 없음

| 카테고리 | 건수 |
|----------|------|
| 기능 | 높음(HIGH) 1 |
| 아키텍처 | 0 |
| 버그 | 중간(MEDIUM) 4 |
| 보안 | 중간(MEDIUM) 1 |
| 코드품질 | 0 |
| 성능 | 0 |
| 테스트 | 0 |

---

## 1단계: 적합성 검증 (Compliance Check)

### 요구사항 대조

| # | 요구사항 | 판정 | 근거 |
|---|----------|------|------|
| 1 | `architecture.md`에 디자인 원칙과 고정 소절 5종 신설 | 충족(PASS) | 원칙 근거, 위치별 역할, 체크리스트, 예외, 기존 체계 관계가 모두 있다. |
| 2 | 임계 수치 사용 여부와 근거 기록 | 충족(PASS) | 수치 임계값을 두지 않는 결정과 형식 충족을 막기 위한 근거가 있다. |
| 3 | `.ai/AI-CONTEXT.md`에 디자인 원칙 포인터 추가 | 충족(PASS) | `### 결정화 판단`에서 `architecture.md`를 가리킨다. |
| 4 | 15개 스킬의 판정·처리·근거를 타당하게 전수 조사 | 부분 충족(PARTIAL) | 15행 형식은 갖췄으나 `code-map` 판정 근거가 실제 local/global `check` 절차와 모순된다(F-1). |
| 5 | 결정화 대상 구현과 나머지 항목 종결 | 부분 충족(PARTIAL) | 8개 스킬에 헬퍼를 연결했으나 F-1 대상이 빠졌고 새 헬퍼 5곳에 거짓 통과 반례가 있다(F-2~F-6). |
| 6 | 비포함 범위 준수와 실익이 낮은 항목 종결 | 충족(PASS) | ADR 0001 본문·검증 레벨 체계는 바꾸지 않았고 조사 상주 문서를 만들지 않았다. 잔여 행도 허용 어휘로 종결했다. |

### 완료의 정의(DoD) 대조

| # | DoD 항목 | 판정 | 근거 |
|---|----------|------|------|
| 1 | 디자인 원칙 섹션과 소절 5종 존재 | 충족(PASS) | 명시된 앵커 검증 명령 통과. |
| 2 | 임계값 결정과 근거 한 줄 | 충족(PASS) | `### 판단 체크리스트`에 결정과 근거가 실질적으로 남아 있다. |
| 3 | ADR 0001과 검증 레벨 3종 관계 표기 | 충족(PASS) | 앵커 명령 통과. 도입 판단·배치·검증 레벨의 적용 지점도 구분했다. |
| 4 | AI-CONTEXT 스킬 작성 규칙 포인터 | 충족(PASS) | 명시된 검증 명령 통과. |
| 5 | 조사 결과 15행·판정·근거 | 충족(PASS) | 형식 게이트 통과, 실제 스킬 디렉토리도 15개다. 의미 타당성 문제는 요구사항 4와 F-1에 기록했다. |
| 6 | `결정화 대상` 처리 어휘 확정 | 충족(PASS) | 허용 어휘 검증 명령 통과. |
| 7 | 모든 `*/tests/run-tests.sh` 통과 | 충족(PASS) | 9개 러너, 총 303개 단언이 모두 통과했다. F-2~F-6 반례는 기존 테스트에 없다. |
| 8 | 신규 헬퍼·테스트 ADR 0001 배치와 배포 제외 | 충족(PASS) | 헬퍼는 각 `scripts/`, 테스트는 각 `tests/`에 있다. 8개 스킬을 임시 경로에 실제 설치한 뒤 `verify-install.sh`가 PASS했고 `tests/`·`*.test.*` 잔존은 0건이었다. |
| 9 | 비대상 행 처리 어휘 확정 | 충족(PASS) | 허용 어휘 검증 명령 통과. |

### 범위 검증

- **스펙 비포함(Out) 침범 여부**: 없음. ADR 0001 본문, 검증 레벨 체계, 결정화 불가 스킬의 리팩터링을 건드리지 않았다.
- **스펙에 없는 추가 구현 여부**: 없음. `.ai/AI-CONTEXT.md` 트리와 `install-skills` 설명 갱신은 신규 헬퍼 배치·실행에 필요한 동기화다.

### 도메인/계약/ADR 정합성

- 계약·도메인 관련 문서: 해당 없음.
- ADR 0001: 테스트 동일 위치, 경량 러너, 설치 시 개발 전용 경로 제외 규칙과 정합하다.
- 코드베이스 색인: 등록된 항목이 없어 실제 소스코드를 직접 확인했다. 색인 갱신 필요 의견은 없다.

---

## 2단계: 비판적 검증 (Critical Review)

### 발견 사항

| # | 위험도 | 영향 | 발생확률 | 카테고리 | 계보 | 설명 | 관련 파일 |
|---|--------|------|----------|----------|------|------|-----------|
| F-1 | 높음(HIGH) | 스펙·기능 달성 차단 | 통상 사용 | 기능 | 신규 | `code-map`의 명시적 `check` 호출 지점을 놓쳐 결정화 대상을 누락했다. | `./issue-0090-spec.md:164`, `code-map/references/local.md:152`, `code-map/references/global.md:167` |
| F-2 | 중간(MEDIUM) | 스펙·기능 달성 차단 | 특수 조건·엣지 | 버그 | 신규 | `check-clear.sh --completion`이 완료 체크박스가 없는 Task를 완료로 통과시킨다. | `issue-work/scripts/check-clear.sh:53` |
| F-3 | 중간(MEDIUM) | 스펙·기능 달성 차단 | 특수 조건·엣지 | 버그 | 신규 | `summarize-metrics.sh`가 Task별 필드 실재·유일성을 확인하지 않아 누락된 Task를 포함한 잘못된 합계를 통과시킨다. | `issue-work/scripts/summarize-metrics.sh:56` |
| F-4 | 중간(MEDIUM) | 스펙·기능 달성 차단 | 특수 조건·엣지 | 보안 | 신규 | `validate-title.sh`가 여러 줄 입력의 첫 줄만 검증해 실제 제출 문자열의 나머지를 검증하지 않는다. | `git-pr/scripts/validate-title.sh:59`, `git-pr/SKILL.md:134` |
| F-5 | 중간(MEDIUM) | 스펙·기능 달성 차단 | 특수 조건·엣지 | 버그 | 신규 | `check-context.sh`가 프로젝트 규칙 표의 열 이름을 보지 않고 열 개수만 검사한다. | `ai-workspace/scripts/check-context.sh:88`, `ai-workspace/SKILL.md:249` |
| F-6 | 중간(MEDIUM) | 스펙·기능 달성 차단 | 특수 조건·엣지 | 버그 | 신규 | `check-lobby.sh`가 `### 진입 절차`의 부모 섹션을 확인하지 않아 잘못된 위치의 4단계를 통과시킨다. | `ai-workspace-directory/scripts/check-lobby.sh:179`, `ai-workspace-directory/references/standard-structure.md:79` |

### 상세 분석

#### F-1: code-map의 결정화 호출 지점 누락

- **위험도**: 높음(HIGH)
- **영향 축**: 스펙·기능 달성 차단
- **발생확률 축**: 통상 사용
- **카테고리**: 기능
- **관점**: 암묵적 가정, 누락된 검증
- **계보**: 신규
- **권장 조치**: `code-map`의 local/global 참조 절차까지 포함해 판단 체크리스트를 다시 적용하고 처리 방향을 재확정한다.
- **완료 기준**:
  - `code-map` 조사 행의 “검증 단계가 없다”는 근거를 실제 `check` 절차와 정합하게 고친다.
  - local/global의 반복 가능한 검사 가운데 결정화 범위를 정하고, 조사 행에 허용된 처리 값을 기록한다.
  - 이번 이슈 반영을 선택하면 결정적 검사·테스트와 호출 지점을 추가하고 전체 러너를 통과시킨다.

<details>
<summary>근거·상세 설명 펼치기</summary>

- spec은 `code-map`에 산출물 검증 단계가 없다고 적었다.
- `references/local.md`는 `check` 모드 전용 정합성 점검을 명시한다. 교차 참조 파일 존재, 태그 목록, 엔트리포인트 존재 검사가 반복된다.
- `references/global.md`에도 리포 접근성, 목록 일치, 교차 참조 유효성 검사가 있다.
- 생성 본체의 코드 해석과 별개로 파일 존재·링크·태그 검사는 같은 입력에 같은 결과를 내며, 여러 문서에 반복되고 기계 판정과 명확한 오류가 가능하다.

</details>

#### F-2: 완료 체크박스가 없는 Task의 거짓 통과

- **위험도**: 중간(MEDIUM)
- **영향 축**: 스펙·기능 달성 차단
- **발생확률 축**: 특수 조건·엣지
- **카테고리**: 버그
- **관점**: 엣지케이스, 누락된 검증
- **계보**: 신규
- **권장 조치**: 각 `### Task ` 블록에서 완료 체크박스의 실재·유일성·상태를 블록 단위로 검증한다.
- **완료 기준**:
  - Task마다 `- [ ] 완료` 또는 `- [x] 완료`가 정확히 1개인지 검사한다.
  - 누락·중복·미체크 Task는 이름과 함께 종료 코드 1로 보고한다.
  - 세 반례와 정상 plan을 `issue-work/tests/run-tests.sh`에 추가한다.

<details>
<summary>근거·상세 설명 펼치기</summary>

- 현재 awk는 체크박스를 찾았을 때만 미체크 여부를 출력한다. Task 블록은 있으나 체크박스가 없으면 아무것도 출력하지 않는다.
- 설계 종료 게이트와 Task N만 체크되고 Task 0의 완료 체크박스가 없는 입력을 실행한 결과는 무출력, 종료 코드 0이었다.

</details>

#### F-3: Task별 지표 누락·중복의 거짓 통과

- **위험도**: 중간(MEDIUM)
- **영향 축**: 스펙·기능 달성 차단
- **발생확률 축**: 특수 조건·엣지
- **카테고리**: 버그
- **관점**: 엣지케이스, 누락된 검증
- **계보**: 신규
- **권장 조치**: 전역 필드 개수 대신 Task 블록별 계약을 검증한 뒤 합산한다.
- **완료 기준**:
  - Task 0과 일반 Task마다 지표 3종이 정확히 1개인지 검사한다.
  - 한 Task의 누락을 다른 Task의 중복 필드로 상쇄할 수 없게 한다.
  - Task N의 지표는 계속 거부하고, Task별 누락·중복 반례 테스트를 추가한다.

<details>
<summary>근거·상세 설명 펼치기</summary>

- `sum_field`는 파일 전체에서 필드가 1개 이상인지만 확인한다.
- Task 0에 지표 3종을 두고 Task 1에는 아무 지표도 두지 않은 입력이 종료 코드 0과 `보정률: 0/1`을 냈다.
- 이 결과는 SKILL.md의 “Task 0 및 일반 실행 Task는 값이 없어도 필드를 남긴다”는 계약과 다르다.

</details>

#### F-4: 여러 줄 PR 제목의 미검증 내용 통과

- **위험도**: 중간(MEDIUM)
- **영향 축**: 스펙·기능 달성 차단
- **발생확률 축**: 특수 조건·엣지
- **카테고리**: 보안
- **관점**: 엣지케이스, 부작용
- **계보**: 신규
- **권장 조치**: 인자 모드와 파일 모드 모두 PR 제목이 정확히 한 줄인지 먼저 검증한다.
- **완료 기준**:
  - 끝의 개행 1개는 허용하되 제목 안의 추가 LF·CR은 종료 코드 1로 거부한다.
  - 파일에서 검증한 값과 `cat`으로 제출되는 값이 같은 한 줄임을 보장한다.
  - 여러 줄 `--title` 인자와 여러 줄 제목 파일 반례를 테스트에 추가한다.

<details>
<summary>근거·상세 설명 펼치기</summary>

- 파일 모드는 `sed -n '1p'`만 읽지만 제출은 `--title "$(cat ...title.md)"`로 파일 전체를 넘긴다.
- `--title $'feat: 정상 제목 (#90)\n승인되지 않은 둘째 줄'`과 같은 내용의 제목 파일이 모두 종료 코드 0으로 통과했다.
- K-0003은 승인 뒤 파일 변조를 다룬다. 이 발견은 변조가 없어도 검증 대상과 제출 대상의 범위가 처음부터 다른 별도 원인이다.

</details>

#### F-5: 프로젝트 규칙 표 헤더 이름 미검증

- **위험도**: 중간(MEDIUM)
- **영향 축**: 스펙·기능 달성 차단
- **발생확률 축**: 특수 조건·엣지
- **카테고리**: 버그
- **관점**: 엣지케이스, 누락된 검증
- **계보**: 신규
- **권장 조치**: `## 프로젝트 규칙`의 첫 표 헤더를 열 수와 이름으로 함께 검증한다.
- **완료 기준**:
  - 헤더가 `파일`·`설명`·`사용 시점` 3열인지 순서까지 검사한다.
  - 3열이지만 이름이 다른 표를 종료 코드 1로 거부하고 회귀 테스트로 고정한다.
  - dev/doc 템플릿과 현재 repo 안내도는 계속 통과한다.

<details>
<summary>근거·상세 설명 펼치기</summary>

- SKILL.md는 3열을 `파일`·`설명`·`사용 시점`으로 고정한다.
- 헬퍼는 `NF - 2`가 3 이상인지만 보며 헤더 셀 이름을 읽지 않는다.
- 현재 안내도의 헤더를 `foo`·`bar`·`baz`로 바꾼 입력도 종료 코드 0으로 통과했다.

</details>

#### F-6: 진입 절차의 부모 섹션 미검증

- **위험도**: 중간(MEDIUM)
- **영향 축**: 스펙·기능 달성 차단
- **발생확률 축**: 특수 조건·엣지
- **카테고리**: 버그
- **관점**: 엣지케이스, 누락된 검증
- **계보**: 신규
- **권장 조치**: 진입 절차 탐색 범위를 `## 에이전트 운영 지침` 섹션 안으로 제한한다.
- **완료 기준**:
  - 해당 H2 안의 `### 진입 절차`만 인정하고 번호 4단계를 센다.
  - 다른 H2에 같은 서브헤딩과 4단계가 있어도 종료 코드 1로 거부한다.
  - 정상 fixture와 잘못된 부모 섹션 반례를 테스트에 추가한다.

<details>
<summary>근거·상세 설명 펼치기</summary>

- 표준 구조는 `### 진입 절차`가 `## 에이전트 운영 지침`의 하위임을 강제한다.
- 헬퍼는 파일 전체에서 첫 `### 진입 절차`를 찾는다.
- 실제 운영 지침의 소절을 이름 변경하고 `## 정체성` 아래에 가짜 4단계를 넣은 입력이 종료 코드 0으로 통과했다.

</details>

### 기등재 참조 항목

<details>
<summary>기등재 참조 표 펼치기</summary>

| 원장 항목 | 상태 | 대조한 발견 내용 | 재검토 조건 충족 | 처리 |
|-----------|------|------------------|------------------|------|
| K-0001 | 수용 | ai-workspace update-3 루트 상주 파일 회귀 fixture 부재 | 미충족 | 기등재 K-0001 참조. 이번 변경은 update-4 검사다. |
| K-0002 | 수용 | issue-audit 축 경계 우선 규칙 부재 | 미충족 | 기등재 K-0002 참조. 매트릭스 헬퍼는 축 정의를 바꾸지 않았다. |
| K-0003 | 수용 | 승인한 제목·본문 파일의 제출 직전 내용 동일성 미검증 | 미충족 | 기등재 K-0003 참조. F-4는 승인 후 변조가 없어도 생기는 별도 원인이다. |
| K-0004 | 수용 | `--clear` 보존 목적지의 이슈 번호 미대조 | 미충족 | 기등재 K-0004 참조. 이번 변경은 1·6단계 검사다. |
| K-0005 | 수용 | 설치 결과 템플릿의 제외 패턴 리터럴 복제 | 미충족 | 기등재 K-0005 참조. 제외 패턴 값은 바뀌지 않았다. |

</details>

### 이전 발견 추적

해당 없음.
