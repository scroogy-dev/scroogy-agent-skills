# Issue #90 감사 리포트 2차 재감사

> 감사 일시: 2026-08-18  
> 감사 모델: OpenAI, GPT-5  
> 감사 회차: 2차 (이전 리포트: `issue-0090-audit-report-1.md`)  
> 감사 대상 브랜치: issue-0090  
> 스펙 출처: `./issue-0090-spec.md` (작성 시점 경로는 `.ai/90_issues/active/issue-0090/issue-0090-spec.md`, --clear로 이관)

---

## 종합 의견

부분 충족이다. 1차 발견 F-1·F-2·F-3·F-4·F-6은 완료 기준을 충족해 닫혔다. F-5는 잘못된 3열 헤더를 거부하도록 보정됐지만 네 번째 열이 붙은 표를 종료 코드 0으로 통과시켜 잔여로 판정한다. 명시된 DoD 명령, 9개 러너의 316개 단언, 8개 스킬의 임시 설치 검증은 모두 통과했다.

## 요약

- 1단계 적합성: 충족(PASS) 14건 / 미충족(FAIL) 0건 / 부분 충족(PARTIAL) 1건 / 판정 불가(N/A) 0건
- 2단계 위험도: 높음(HIGH) 0건 / 중간(MEDIUM) 1건 / 낮음(LOW) 0건 / 정보(INFO) 0건
- 기등재 참조: 6건 (원장 기등재와 일치, 위험도·카테고리 집계에 포함하지 않음)
- 이전 발견: 닫힘 5건 / 잔여 1건 (잔여: F-5)

| 카테고리 | 건수 |
|----------|------|
| 기능 | 0 |
| 아키텍처 | 0 |
| 버그 | 중간(MEDIUM) 1 |
| 보안 | 0 |
| 코드품질 | 0 |
| 성능 | 0 |
| 테스트 | 0 |

---

## 1단계: 적합성 검증 (Compliance Check)

### 요구사항 대조

| # | 요구사항 | 판정 | 근거 |
|---|----------|------|------|
| 1 | `architecture.md`에 디자인 원칙과 고정 소절 5종 신설 | 충족(PASS) | 원칙 근거 4종, 위치별 역할, 체크리스트, 예외, 기존 체계 관계가 있다. 고정 앵커 검증도 통과했다. |
| 2 | 임계 수치 사용 여부와 근거 기록 | 충족(PASS) | 임계 수치를 두지 않는 결정과 형식 충족을 막기 위한 근거가 `### 판단 체크리스트`에 있다. |
| 3 | `.ai/AI-CONTEXT.md`에 디자인 원칙 포인터 추가 | 충족(PASS) | `### 결정화 판단`이 `10_rules/architecture.md`를 가리킨다. |
| 4 | 15개 스킬의 판정·처리·근거를 타당하게 전수 조사 | 충족(PASS) | 1차 F-1의 `code-map` 판정이 `결정화 대상`으로 교정됐다. 구현 잔여는 원장 K-0006으로 이관했다. |
| 5 | 결정화 대상 구현과 나머지 항목 종결 | 부분 충족(PARTIAL) | 8개 스킬의 헬퍼·테스트와 code-map 원장 이관은 완료됐다. 다만 `check-context.sh`가 정확한 3열 계약을 아직 강제하지 못한다(F-5 잔여). |
| 6 | 비포함 범위 준수와 실익이 낮은 항목 종결 | 충족(PASS) | ADR 0001 본문과 검증 레벨 체계를 바꾸지 않았다. 조사 결과용 상주 문서도 만들지 않았다. |

### 완료의 정의(DoD) 대조

| # | DoD 항목 | 판정 | 근거 |
|---|----------|------|------|
| 1 | 디자인 원칙 섹션과 소절 5종 존재 | 충족(PASS) | 스펙의 앵커 검증 명령이 통과했다. |
| 2 | 임계값 결정과 근거 한 줄 | 충족(PASS) | 결정과 근거가 실질적으로 남아 있다. |
| 3 | ADR 0001과 검증 레벨 3종 관계 표기 | 충족(PASS) | `ADR 0001`·`[D]`·`[QD]`·`[ND]` 앵커와 관계 설명이 있다. |
| 4 | AI-CONTEXT 스킬 작성 규칙 포인터 | 충족(PASS) | 스펙의 포인터 검증 명령이 통과했다. |
| 5 | 조사 결과 15행·판정·근거 | 충족(PASS) | 형식 게이트가 15행을 확인했다. 1차 지적을 반영한 code-map 근거도 실제 참조 절차와 맞는다. |
| 6 | `결정화 대상` 처리 어휘 확정 | 충족(PASS) | 8건은 `이번 이슈 반영`, code-map은 `원장 K-0006`으로 기록됐다. |
| 7 | 모든 `*/tests/run-tests.sh` 통과 | 충족(PASS) | 9개 러너의 316개 단언이 모두 통과했다. F-5의 4열 반례는 현재 테스트 집합에 없다. |
| 8 | 신규 헬퍼·테스트의 ADR 0001 배치와 배포 제외 | 충족(PASS) | 헬퍼는 각 `scripts/`, 테스트는 각 `tests/`에 있다. 8개 스킬을 임시 경로에 설치한 결과 `verify-install.sh`가 PASS했고 스크립트는 포함됐으며 테스트는 제외됐다. |
| 9 | 비대상 행 처리 어휘 확정 | 충족(PASS) | 허용 어휘 검증 명령이 통과했다. |

### 범위 검증

- **스펙 비포함(Out) 침범 여부**: 없음. ADR 0001 본문, 검증 레벨 체계, 결정화 불가 스킬의 리팩터링을 건드리지 않았다.
- **스펙에 없는 추가 구현 여부**: 없음. K-0006 등재와 이슈 문서 갱신은 1차 감사 대응과 잔여 항목 종결에 필요하다.

### 도메인/계약 정합성

- 계약·도메인 관련 문서: 관련 항목 없음.
- ADR 0001: 테스트 동일 위치, 경량 러너, 설치 시 개발 전용 경로 제외 규칙과 맞는다.
- 코드베이스 색인: 등록된 항목이 없어 실제 소스와 테스트를 직접 확인했다. 색인 갱신 필요 의견은 없다.

---

## 2단계: 비판적 검증 (Critical Review)

### 발견 사항

| # | 위험도 | 영향 | 발생확률 | 카테고리 | 계보 | 설명 | 관련 파일 |
|---|--------|------|----------|----------|------|------|-----------|
| F-5 | 중간(MEDIUM) | 스펙·기능 달성 차단 | 특수 조건·엣지 | 버그 | F-5 잔여 | `check-context.sh`가 앞의 3개 헤더 이름만 맞으면 네 번째 이후 열을 허용한다. | `ai-workspace/scripts/check-context.sh:102`, `ai-workspace/SKILL.md:249` |

### 상세 분석

#### F-5: 프로젝트 규칙 표의 추가 열이 통과함

- **위험도**: 중간(MEDIUM)
- **영향 축**: 스펙·기능 달성 차단
- **발생확률 축**: 특수 조건·엣지
- **카테고리**: 버그
- **관점**: 엣지케이스, 누락된 검증
- **계보**: F-5 잔여
- **권장 조치**:
  - 열 수가 정확히 3개인지 검사한다. 2열은 기존 구버전 확장 대상으로, 4열 이상은 표준 3열 위반으로 구분해 보고한다.
  - `파일`·`설명`·`사용 시점`·`비고` 헤더를 가진 4열 반례를 회귀 테스트에 추가한다.
- **완료 기준**:
  - `## 프로젝트 규칙` 표는 `파일`·`설명`·`사용 시점` 3열일 때만 해당 검사를 통과한다.
  - 2열 구버전 표는 기존의 열 확장 사유를 유지하고, 4열 이상 표는 종료 코드 1과 별도 위반 사유를 낸다.
  - dev/doc 템플릿과 현재 `.ai/AI-CONTEXT.md`가 통과하며 4열 반례는 실패한다.
  - 모든 `*/tests/run-tests.sh`가 통과한다.

<details>
<summary>근거·상세 설명 펼치기</summary>

- SSoT는 `## 프로젝트 규칙`에 `파일`·`설명`·`사용 시점`의 3열 표가 존재해야 한다고 정한다.
- 현재 구현은 열 수가 3개 미만일 때만 실패한다. 3개 이상이면 첫 3개 헤더 이름만 조합해 비교한다.
- 현재 안내도의 헤더에 `비고` 열을 추가한 변형 파일을 실행한 결과는 무출력, 종료 코드 0이었다.
- 1차 F-5의 첫 완료 기준은 헤더가 세 이름의 3열인지 순서까지 검사하는 것이므로, 이름이 다른 3열 반례만 추가한 현재 보정으로는 닫히지 않는다.

</details>

### 기등재 참조 항목

<details>
<summary>기등재 참조 표 펼치기</summary>

| 원장 항목 | 상태 | 대조한 발견 내용 | 재검토 조건 충족 | 처리 |
|-----------|------|------------------|------------------|------|
| K-0001 | 수용 | ai-workspace update-3 루트 상주 파일 회귀 fixture 부재 | 미충족 | 기등재 K-0001 참조. 이번 변경은 update-4 검사다. |
| K-0002 | 수용 | issue-audit 축 경계 우선 규칙 부재 | 미충족 | 기등재 K-0002 참조. 헬퍼는 축 값·정의·매트릭스를 바꾸지 않았다. |
| K-0003 | 수용 | 승인한 제목·본문 파일의 제출 직전 내용 동일성 미검증 | 미충족 | 기등재 K-0003 참조. 이번 보정은 검증 대상 범위를 파일 전체로 맞춘 것이다. |
| K-0004 | 수용 | `--clear` 보존 목적지의 이슈 번호 미대조 | 미충족 | 기등재 K-0004 참조. 이번 변경은 1·6단계 검사다. |
| K-0005 | 수용 | 설치 결과 템플릿의 제외 패턴 리터럴 복제 | 미충족 | 기등재 K-0005 참조. 제외 패턴 값은 바뀌지 않았다. |
| K-0006 | 수용 | code-map `check` 정합성 점검의 결정적 헬퍼 부재 | 미충족 | 기등재 K-0006 참조. 참조 문서의 검사 표는 바뀌지 않았고 `.ai/60_codebase/` 색인도 아직 없다. |

</details>

### 이전 발견 추적

<details>
<summary>이전 발견 추적 표 펼치기</summary>

| 이전 발견 | 완료 기준 항목 | 판정 (닫힘/잔여) | 근거 |
|-----------|----------------|------------------|------|
| F-1 | code-map 조사 행의 잘못된 근거 교정 | 닫힘 | 실제 local/global `check` 절차에 맞는 근거로 바뀌었다. |
| F-1 | 결정화 범위와 허용된 처리 값 확정 | 닫힘 | 판정을 `결정화 대상`으로 바꾸고 `원장 K-0006`으로 종결했다. |
| F-1 | 이번 이슈 반영 시 헬퍼·테스트·호출 지점 추가 | 닫힘 | 이번 이슈 반영을 선택하지 않아 조건이 발동하지 않는다. |
| F-2 | Task마다 완료 체크박스가 정확히 1개인지 검사 | 닫힘 | `check-clear.sh`가 Task 블록별 실재·유일성·체크 상태를 검사한다. |
| F-2 | 누락·중복·미체크 Task를 이름과 종료 코드 1로 보고 | 닫힘 | 세 상태를 구분해 보고하고 테스트가 이를 검증한다. |
| F-2 | 반례와 정상 plan 회귀 테스트 | 닫힘 | 체크박스 소실·중복·미체크와 정상 fixture가 러너에 있다. |
| F-3 | 일반 Task마다 지표 3종이 정확히 1개인지 검사 | 닫힘 | `summarize-metrics.sh`가 Task 블록별 필드 수를 검사한다. |
| F-3 | Task 사이 누락·중복 상쇄 차단 | 닫힘 | 한 Task의 누락과 다른 Task의 중복을 결합한 반례가 종료 코드 1을 낸다. |
| F-3 | Task N 지표 거부와 누락·중복 회귀 테스트 | 닫힘 | Task N 지표, 필드 누락, 블록 간 상쇄 반례가 러너에 있다. |
| F-4 | 말미 개행 1개만 허용하고 내부 LF·CR 거부 | 닫힘 | 원문 끝을 보존해 읽은 뒤 LF·CR을 검사한다. |
| F-4 | 파일 검증 범위와 제출 문자열 범위 일치 | 닫힘 | 파일 전체를 읽어 검사하므로 `cat`으로 제출하는 범위와 같다. |
| F-4 | 여러 줄 인자·파일 반례 테스트 | 닫힘 | 두 모드의 여러 줄 입력과 CR 반례가 러너에 있다. |
| F-5 | 헤더가 세 이름의 정확한 3열인지 순서까지 검사 | 잔여 | 첫 3개 이름과 순서는 검사하지만 4열 이상을 허용한다. |
| F-5 | 이름이 다른 3열 표 거부 테스트 | 닫힘 | `foo`·`bar`·`baz` 헤더 반례가 종료 코드 1을 낸다. |
| F-5 | dev/doc 템플릿과 현재 안내도 통과 | 닫힘 | 세 정상 입력이 모두 통과한다. |
| F-6 | `## 에이전트 운영 지침` 안의 진입 절차만 인정 | 닫힘 | 탐색 범위를 해당 H2 안으로 제한했다. |
| F-6 | 다른 H2의 가짜 진입 절차 거부 | 닫힘 | 부모 섹션 변형 반례가 종료 코드 1을 낸다. |
| F-6 | 정상·잘못된 부모 섹션 회귀 테스트 | 닫힘 | 두 경로가 러너에 고정됐다. |

</details>
