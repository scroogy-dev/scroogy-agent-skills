# Issue #92 감사 리포트 git-review-quiz 보정본 재감사

> 감사 일시: 2026-08-30  
> 감사 모델: OpenAI, GPT-5  
> 감사 회차: 2차 (이전 리포트: `issue-0092-audit-report-1.md`)  
> 감사 대상 브랜치: issue-0092 (미커밋 Task N 보정 포함)  
> 스펙 출처: `./issue-0092-spec.md` (작성 시점 경로는 `.ai/90_issues/active/issue-0092/issue-0092-spec.md`, --clear로 이관)

---

## 종합 의견

부분 충족이다. 공식 DoD와 46개 회귀 테스트는 통과하지만, Task 2의 결정적 완료 게이트가 댓글 전용 fixture 4개 때문에 실패한다. 형식 검사 헬퍼도 R3·R5와 permalink 계약의 일부 위반을 정상으로 판정한다. F-5는 높음(HIGH)이므로 보정 뒤 재검증해야 한다.

## 요약

- 1단계 적합성: 충족(PASS) 13건 / 미충족(FAIL) 0건 / 부분 충족(PARTIAL) 1건 / 판정 불가(N/A) 1건
- 2단계 위험도: 높음(HIGH) 1건 / 중간(MEDIUM) 2건 / 낮음(LOW) 2건 / 정보(INFO) 0건
- 기등재 참조: 1건 (원장 기등재와 일치, 위험도·카테고리 집계에 포함하지 않음)
- 이전 발견: 닫힘 1건 / 잔여 3건 (잔여: F-1, F-3, F-4)

| 카테고리 | 건수 |
|----------|------|
| 기능 | 낮음(LOW) 1 |
| 아키텍처 | 0 |
| 버그 | 중간(MEDIUM) 2 |
| 보안 | 0 |
| 코드품질 | 0 |
| 성능 | 0 |
| 테스트 | 높음(HIGH) 1 · 낮음(LOW) 1 |

---

## 1단계: 적합성 검증 (Compliance Check)

### 요구사항 대조

| # | 요구사항 | 판정 | 근거 |
|---|----------|------|------|
| 1 | 대상 식별·근거 확보·문항 생성·대화형 진행·댓글 게시·옵션 3종을 담은 `SKILL.md` 신설 | 충족(PASS) | PR·브랜치 모드, 근거 문서 0건 처리, 7단계 실행, 대화형 처리, 승인 게이트가 본문에 있다. |
| 2 | `templates/quiz-template.md`를 산출 형식의 SSoT로 신설 | 충족(PASS) | 대상·문항·응답 기록 골격과 객관식·주관식 예시가 있고 일반 모드 검사를 통과한다. |
| 3 | R1~R7 검사 헬퍼와 회귀 테스트 신설 | 부분 충족(PARTIAL) | 공식 러너 46건은 통과한다. 그러나 R3·R5 위반과 잘못된 permalink가 종료 코드 0으로 통과하고, Task 2의 결정적 fixture 전수 게이트도 4건을 실패한다(F-1, F-4~F-6). |
| 4 | `.ai/AI-CONTEXT.md`와 `README.md`에 스킬 등록 | 충족(PASS) | 디렉토리 트리·스킬 표·Git 정책·관계도 5개 위치와 트리 하위 행 검사가 통과했다. |
| 5 | 실제 PR #91로 댓글·대화형 시험 실행 | 충족(PASS) | 두 산출물은 현재 헬퍼를 통과한다. 대화형은 5문항·응답 5행이고 댓글 본문의 5개 permalink는 시험 head SHA와 일치한다. |

### 완료의 정의(DoD) 대조

| # | DoD 항목 | 판정 | 근거 |
|---|----------|------|------|
| 1 | 프론트매터 `name` 일치·`description` 20자 이상 | 충족(PASS) | 스펙의 결정적 명령이 무출력으로 통과했다. |
| 2 | 표준 섹션 5개·옵션 소절 집합 일치 | 충족(PASS) | 헤더·소절 집합 검사가 무출력으로 통과했다. |
| 3 | 러너 0실패·R1~R7 반례 행·템플릿 통과 | 충족(PASS) | `tests/run-tests.sh`는 46건 통과·0실패다. 반례 완전성과 별도 plan 게이트 문제는 요구사항 3과 2단계 발견에 기록했다. |
| 4 | ADR 0001 테스트 배치·러너 위치 | 충족(PASS) | 테스트는 `tests/`에만 있고 실행 권한이 있다. 임시 설치본의 `verify-install.sh`도 PASS다. |
| 5 | 시험 산출물 형식·문항 수·응답 기록·permalink | 충족(PASS) | 스펙의 갱신된 명령이 무출력으로 통과했다. 댓글 본문은 `--comment --head e1543ed…` 검사도 통과했다. |
| 6 | 안내도·README 5개 위치 등록 | 충족(PASS) | 스펙의 결정적 명령이 무출력으로 통과했다. |
| 7 | `git-review-quiz/` em dash 문자 0건 | 충족(PASS) | 금지 문자 검색 결과가 0건이다. |
| 8 | 시험 문항이 변경 자체를 묻고 힌트가 정답을 직접 노출하지 않으며 근거 위치가 diff와 일치 | 충족(PASS) | 두 산출물의 10문항을 PR #91 head `e1543ed…`의 실제 파일과 대조했다. 변경 지점에 결속되고 힌트는 판단 위치만 가리킨다. |
| 9 | 다른 스킬 파일 없이 `SKILL.md` 절차 완결 | 충족(PASS) | 저장소 확정, PR 조회, diff, 문서 0건 처리, 파일 작성, 검사, 승인·게시 명령이 본문에 있다. |
| 10 | 대화형 진행의 사용자 체감이 자연스러움 | 판정 불가(N/A) | 산출 기록으로 힌트·오답·결과 요약 수행은 확인했다. 독립 재감사에서는 당시 사용자의 체감 판정을 직접 관찰할 수 없다. |

### 범위 검증

- **스펙 비포함(Out) 침범 여부**: 없음. 온보딩 스킬, 머지 차단, 라인 댓글, 추정 문서, `--count`, 세션 간 통계를 추가하지 않았다.
- **스펙에 없는 추가 구현 여부**: 없음. 보정 변경은 초회 감사 F-1~F-4와 직접 결속된다.

### 도메인/계약 정합성

- 계약 인덱스는 자리표시자뿐이고 도메인에는 실질 정책·명세가 없다. 시험 실행의 "근거 문서 없음, 테크 문항만" 판정과 일치한다.
- ADR 0001의 `scripts/`·`tests/` 배치와 배포 제외 규칙을 지킨다. 임시 설치 검증도 통과했다.
- 코드베이스 색인에는 등록 항목이 없어 실제 소스와 현재 diff를 직접 확인했다.

---

## 2단계: 비판적 검증 (Critical Review)

### 발견 사항

| # | 위험도 | 영향 | 발생확률 | 카테고리 | 계보 | 설명 | 관련 파일 |
|---|--------|------|----------|----------|------|------|-----------|
| F-1 | 중간(MEDIUM) | 스펙·기능 달성 차단 | 특수 조건·엣지 | 버그 | F-1 잔여 | 같은 행에서 닫기 태그가 열기 태그보다 먼저 와도 개수 차가 음수가 아니면 짝 없는 닫기를 놓친다. | `git-review-quiz/scripts/check-quiz.sh` |
| F-3 | 낮음(LOW) | 기술 품질 의견 | 통상 사용 | 테스트 | F-3 잔여 | 지원하지 않는 형식 어휘는 현재 거부하지만 이를 고정한 전용 회귀 fixture가 없다. | `git-review-quiz/tests/fixtures/`, `git-review-quiz/tests/run-tests.sh` |
| F-4 | 낮음(LOW) | 기능 저하 | 특수 조건·엣지 | 기능 | F-4 잔여 | 댓글 permalink가 확정 저장소·위치와 다른 URL이거나 `http`여도 SHA 형식만 맞으면 통과한다. | `git-review-quiz/scripts/check-quiz.sh`, `git-review-quiz/SKILL.md` |
| F-5 | 높음(HIGH) | 스펙·기능 달성 차단 | 통상 사용 | 테스트 | 신규 | Task 2의 결정적 fixture 전수 게이트가 댓글 전용 반례 4개를 일반 모드로 실행해 실패한다. | `./issue-0092-plan.md`, `git-review-quiz/tests/run-tests.sh` |
| F-6 | 중간(MEDIUM) | 스펙·기능 달성 차단 | 특수 조건·엣지 | 버그 | 신규 | 주관식 선택지를 첫 접기 뒤에 두면 R5의 선택지 0개 규칙을 우회한다. | `git-review-quiz/scripts/check-quiz.sh` |

### 상세 분석

#### F-1: 같은 행의 태그 순서를 검사하지 않는다

- **위험도**: 중간(MEDIUM)
- **영향 축**: 스펙·기능 달성 차단
- **발생확률 축**: 특수 조건·엣지
- **카테고리**: 버그
- **관점**: 엣지케이스, 누락된 검증
- **계보**: F-1 잔여
- **권장 조치**: 태그를 등장 순서대로 처리하거나 `<details>`·`</details>`를 독립 행으로 제한해 행 안의 선행 닫기를 거부한다.
- **완료 기준**:
  - 깊이 0에서 `</details><details><details>`가 나온 입력은 R3 위반·종료 코드 1을 낸다.
  - 같은 행에 여러 태그가 있어도 각 닫기 시점의 깊이가 음수가 되는지 판정한다.
  - 전용 fixture를 러너에 추가하고 기존 46개 케이스와 템플릿은 계속 통과한다.

<details>
<summary>근거·상세 설명 펼치기</summary>

- `check-quiz.sh:241-249`는 한 행의 열기·닫기 개수를 각각 센 뒤 `depth += o - c`로 합산한다. 행 안의 태그 순서는 보지 않는다.
- 정상 fixture의 첫 힌트 여는 행을 `</details><details><details>`로 바꾼 입력은 짝 없는 닫기로 시작하지만 무출력·종료 코드 0이었다.
- 독립 행의 짝 없는 닫기와 종료 시 양수가 남는 깊이는 새 fixture가 잡으므로 초회 발견의 일부는 닫혔다.

</details>

#### F-3: 지원하지 않는 형식 어휘의 전용 회귀 fixture가 없다

- **위험도**: 낮음(LOW)
- **영향 축**: 기술 품질 의견
- **발생확률 축**: 통상 사용
- **카테고리**: 테스트
- **관점**: 누락된 검증
- **계보**: F-3 잔여
- **권장 조치**: `### Q1 [테크 · OX]`처럼 지원하지 않는 형식 어휘를 담은 R1 fixture를 추가한다.
- **완료 기준**:
  - 형식 어휘가 `객관식`·`주관식` 밖인 fixture가 R1 위반·종료 코드 1을 낸다.
  - 러너가 해당 사유를 직접 대조하고 fixture 전수 커버리지에 포함한다.
  - 기존 잘못된 관점·대괄호 누락·번호 누락 fixture는 계속 통과한다.

<details>
<summary>근거·상세 설명 펼치기</summary>

- `OX`로 바꾼 수동 입력은 현재 `위반 R1 Q1`·종료 코드 1을 내므로 런타임 결함은 닫혔다.
- `tests/fixtures/invalid-r1-*.md`에는 번호 건너뜀, 관점 오기, 인식 범위 밖, 대괄호 누락, 번호 누락만 있다. 초회 F-3 완료 기준에 적힌 "지원하지 않는 형식 어휘 반례"는 고정되지 않았다.

</details>

#### F-4: permalink가 확정 저장소·변경 위치를 가리키는지 확인하지 않는다

- **위험도**: 낮음(LOW)
- **영향 축**: 기능 저하
- **발생확률 축**: 특수 조건·엣지
- **카테고리**: 기능
- **관점**: 암묵적 가정, 누락된 검증
- **계보**: F-4 잔여
- **권장 조치**: 댓글 검사에 확정 호스트·소유자·저장소를 넘기고 위치 경로·줄 범위를 URL과 대조한다.
- **완료 기준**:
  - 확정 저장소와 다른 호스트·소유자·저장소의 URL은 R2 위반을 낸다.
  - `http` URL과 위치 행의 경로·줄 범위가 permalink와 다른 입력은 R2 위반을 낸다.
  - head/base SHA 대조, 누락, 브랜치명, 줄 앵커 반례는 계속 통과한다.
  - 잘못된 저장소·경로·줄 범위·scheme fixture를 러너에 추가한다.

<details>
<summary>근거·상세 설명 펼치기</summary>

- `SKILL.md:41-45`는 대상 저장소를 불변값으로 보관하지만, `check-quiz.sh` 인터페이스는 `--head`·`--base`만 받아 저장소를 대조할 수 없다.
- `check-quiz.sh:79-91`은 URL 모양과 SHA·앵커 존재만 보고, `:205`의 정규식은 `http`도 허용한다.
- 정상 댓글 fixture의 저장소를 `other-owner/other-repo`로 바꾸거나 Q1 앵커를 `#L1`로 바꾸거나 scheme을 `http`로 바꾼 세 입력이 모두 `--comment --head ... --base ...`에서 무출력·종료 코드 0이었다.

</details>

#### F-5: Task 2의 결정적 완료 게이트가 현재 fixture 체계와 모순된다

- **위험도**: 높음(HIGH)
- **영향 축**: 스펙·기능 달성 차단
- **발생확률 축**: 통상 사용
- **카테고리**: 테스트
- **관점**: 부작용, 스펙 모호성, 누락된 검증
- **계보**: 신규
- **권장 조치**: plan의 전수 게이트를 일반 반례와 댓글 전용 반례로 나누고 각 모드의 기대 종료 코드를 명시한다.
- **완료 기준**:
  - `invalid-comment-*`는 일반 모드 무출력·종료 0, 댓글 모드 R2/R7 위반·종료 1로 판정한다.
  - 나머지 `invalid-*`는 일반 모드에서 위반·종료 1로 판정한다.
  - plan Task 2의 결정적 명령이 출력 0건으로 끝난다.
  - 러너의 fixture 전수 커버리지와 46개 이상 케이스가 계속 통과한다.

<details>
<summary>근거·상세 설명 펼치기</summary>

- `issue-0092-plan.md:143-162`는 모든 `invalid-*.md`를 일반 모드로 실행해 종료 코드 1을 요구한다.
- `run-tests.sh:107-130`은 `invalid-comment-*`가 댓글 모드에서만 실패하고 일반 모드에서는 통과해야 한다고 반대로 고정한다.
- plan 명령을 그대로 재실행하면 `invalid-comment-permalink-anchor.md`, `-branch.md`, `-missing.md`, `invalid-comment-record.md` 네 파일이 `rc=0`이라 위반 4행을 출력한다.

</details>

#### F-6: 주관식의 접기 뒤 선택지를 세지 않는다

- **위험도**: 중간(MEDIUM)
- **영향 축**: 스펙·기능 달성 차단
- **발생확률 축**: 특수 조건·엣지
- **카테고리**: 버그
- **관점**: 엣지케이스, 암묵적 가정
- **계보**: 신규
- **권장 조치**: 주관식은 문항 전체의 접기 밖 선택지 행을 세고 위치와 무관하게 1개라도 있으면 거부한다.
- **완료 기준**:
  - 힌트 접기 뒤와 정답 접기 뒤에 선택지를 둔 주관식 입력이 각각 R5 위반·종료 코드 1을 낸다.
  - 객관식은 첫 접기 앞의 `(a)`부터 연속인 선택지만 유효하다는 기존 판정을 유지한다.
  - 두 반례 fixture를 러너에 추가하고 기존 정상 fixture는 계속 통과한다.

<details>
<summary>근거·상세 설명 펼치기</summary>

- `check-quiz.sh:233-239`는 `!seen_details`일 때만 선택지 수를 올린다. `flush_q():133-134`의 주관식 0개 판정도 이 값에 의존한다.
- 정상 fixture의 Q2 힌트 접기 뒤에 `- (a)`, `- (b)`를 넣은 입력이 무출력·종료 코드 0이었다.
- 스펙 R5는 주관식 선택지를 위치와 무관하게 0개로 정한다.

</details>

### 기등재 참조 항목

<details>
<summary>기등재 참조 표 펼치기</summary>

| 원장 항목 | 상태 | 대조한 발견 내용 | 재검토 조건 충족 | 처리 |
|-----------|------|------------------|------------------|------|
| K-0003 | 수용 | 승인 뒤 게시 직전까지 본문 파일의 내용 동일성을 해시로 재검증하지 않는 구조가 댓글 흐름에도 있다. | 미충족 | 기등재 K-0003 참조. `git-pr` 승인 게이트의 승인 대상·검증 값·절차 순서를 바꾸지 않았고 실제 미승인 게시 사례도 없다. |

</details>

### 이전 발견 추적

<details>
<summary>이전 발견 추적 표 펼치기</summary>

| 이전 발견 | 완료 기준 항목 | 판정 (닫힘/잔여) | 근거 |
|-----------|----------------|------------------|------|
| F-1 | 닫히지 않은 `<details>`가 R3/R4 위반·종료 1 | 닫힘 | `invalid-r3-details-unclosed.md`와 러너 사유 대조가 통과한다. |
| F-1 | 힌트와 정답 summary를 한 `<details>`에 둔 입력 거부 | 닫힘 | `invalid-r4-hint-answer-same-block.md`가 R4 위반을 낸다. |
| F-1 | 문항 종료 깊이 0, 불필요한 닫기 태그 거부 | 잔여 | 독립 행의 닫기는 잡지만 같은 행에서 닫기가 먼저 나오는 순서를 놓친다. |
| F-1 | 반례 fixture 추가, 기존 정상 fixture·템플릿 통과 | 잔여 | 기본 반례는 추가됐지만 위 순서 반례가 없고 실제로 통과한다. |
| F-2 | `(a), (a)` 중복 라벨 거부 | 닫힘 | `invalid-r5-label-duplicate.md`가 R5 위반을 낸다. |
| F-2 | `(a), (c)` 건너뜀·`(b)` 시작 거부 | 닫힘 | skip·not-start fixture가 각각 R5 위반을 낸다. |
| F-2 | 기존 연속 라벨 정상 입력 통과 | 닫힘 | 두 정상 fixture와 템플릿이 통과한다. |
| F-2 | 세 반례 fixture 추가 | 닫힘 | duplicate·skip·not-start 세 fixture가 러너와 전수 커버리지에 포함된다. |
| F-3 | 마지막 `Q3`를 `QX`로 바꾼 입력 거부 | 닫힘 | `invalid-r1-header-unrecognized.md`가 R1 위반을 낸다. |
| F-3 | 대괄호·번호 누락, 지원하지 않는 형식 어휘 거부 | 닫힘 | 두 누락 fixture와 수동 `OX` probe가 모두 R1 위반을 낸다. |
| F-3 | 문항 후보 본문을 R2~R6 검사에서 제외하지 않음 | 닫힘 | `## 문항` 아래 모든 `### `를 후보로 열어 후속 규칙도 판정한다. |
| F-3 | 반례 fixture 추가, 기존 정상 입력 통과 | 잔여 | 형식 어휘 `OX` 동작은 맞지만 이를 고정한 전용 fixture가 없다. |
| F-4 | 댓글 모드의 모든 위치 행에 permalink 정확히 1개 | 닫힘 | 누락 fixture와 실제 댓글 본문의 위치 행 전수 대조가 통과한다. |
| F-4 | URL이 확정 저장소·SHA·경로·줄 앵커 계약을 지킴 | 잔여 | SHA·형식만 보고 확정 저장소와 위치 경로·줄 범위를 대조하지 않으며 `http`도 허용한다. |
| F-4 | 추가·수정은 head, 삭제는 base SHA와 일치 | 닫힘 | `--head`·`--base` 대조와 정상 base fixture가 통과한다. |
| F-4 | 누락·브랜치명·잘못된 SHA·줄 앵커 반례 추가 | 닫힘 | 세 fixture와 잘못된 `--head` 인자 케이스가 러너에 있다. |

</details>
