# Issue #92 실행요약 git-review-quiz: PR 변경 이해도를 문항으로 점검하는 스터디 모드 스킬 신설

> 스펙: [issue-0092-spec.md](./issue-0092-spec.md) | 계획: [issue-0092-plan.md](./issue-0092-plan.md)

## 다음 작업

> ✅ 모든 작업이 완료되었습니다.

## 모델 기록

<!--
이슈 단위 요약(대표값)이다. 세부 SSoT는 아래 `Task별 수행 결과`의 `수행 모델`이며,
한 구분에 여러 모델이 관여했으면 대표 모델을 적고 세부는 Task별 기록을 본다.

형식: "벤더, 모델명". 설계·구현·audit 어느 칸이든 어느 벤더·모델이든 가능하다
(예: Anthropic, Claude Opus 4.8 (claude-opus-4-8) / OpenAI, GPT-5.x / Google, Gemini 3.x).
설계 모델과 구현 모델은 다를 수 있어 칸을 나눈다 — 같으면 같은 값을 적는다.
"audit 모델 ≠ 구현 모델" 조건을 나중에 명령으로 확인 가능하도록 기록한다. 모델 전환은 사람이 수행하므로 기계로 강제하기 어렵다.
구현에 여러 벤더가 관여했으면 audit 모델의 벤더는 Task별 `수행 모델`의 모든 벤더와도 달라야 한다
— 대표 행 비교만으로는 audit 벤더가 구현 일부에 참여한 경우를 놓친다.
-->

| 구분 | 모델 |
|------|------|
| 설계 모델 | Anthropic, Claude Fable 5 (claude-fable-5) |
| 구현 모델 | Anthropic, Claude Opus 5 (claude-opus-5[1m]) |
| audit 모델 | OpenAI, GPT-5 |

---

## Task별 수행 결과

<!--
Task 0 및 일반 실행 Task(Task N 제외) 블록의 지표 4종은 사후 집계용이라 표기를 고정한다
(값이 없어도 필드는 지우지 않는다. Task N 예외의 근거는 아래 Task N 블록 주석).
- 수행 모델: "벤더, 모델명" — `모델 기록` 표와 같은 형식. 한 Task를 모델 전환·재시도로 둘 이상이 수행했으면
  관여한 모델을 전부 ` / `로 나열한다 — 하나만 남기면 누락된 벤더가 audit을 맡아도 Task N의 강화 조건을 통과한다.
  `-`는 미착수·스킵 Task 전용이며, `결과`가 완료·부분 완료면 실제 값을 필수로 기록한다
  (`-`로 남기면 Task N의 교차모델 강화 조건이 공집합이 되어 우회가 된다)
- audit 발견: 이 Task와 관련된 audit 발견사항 건수 — 없으면 `0건` (`-` 금지)
- 보정 반영: `--response` 항목별 승인을 통과해 실제로 보정한 건수 — 없으면 `0건` (`-` 금지)
- 재시도: 같은 Task에서 재수정·재시도한 횟수 — 없으면 `0회` (`-` 금지)

한 발견이 여러 Task 산출물에 걸쳐도 가장 직접 수정된 주 Task 하나에만 귀속한다 —
`audit 발견`·`보정 반영` 모두 그 주 Task에서 1회만 세고 관련된 다른 Task에는 세지 않는다.
Task 축에서도 이슈 단위 합계가 중복으로 부풀지 않게 하기 위함이다(아래 회차 축 중복 제외와 같은 취지).

수치 3종은 회차별 값이 아니라 이슈 전체 누적이다 — 여러 차수 감사가 같은 발견을 재확인해도
1건으로 세고(중복 제외), 새 회차 값으로 덮어쓰지 않는다. 보정률 사후 집계의 분모·분자가 회차 반복으로 부풀지 않게 하기 위함이다.

수치 3종에 `-`를 쓰지 않는 이유: 아래 스니펫이 `[0-9]+`만 추출하므로 `-`인 Task는
합계에서 조용히 빠지고, 전부 `-`면 합계 자체가 빈 문자열이 된다. `-` 허용은 비수치 필드(`수행 모델`)에만 둔다.

수치 3종의 기본값은 주석이 아니라 리터럴 `0건`/`0회`로 둔다: 주석 placeholder는 렌더링 값이
비어 있는데도 집계기가 주석 안 숫자를 실제 값으로 오인하고, `미기록` 같은 형식 위반도 걸러지지 않는다.

보정률은 별도 필드로 두지 않고 `보정 반영 / audit 발견`으로 사후 집계한다.
아래 스니펫이 동작하도록 `- **<필드>**: <숫자><단위>` 표기를 지킨다 (줄 시작, 굵게, 콜론 뒤 숫자).

  # 이슈 하나의 보정률
  found=$(grep -E '^- \*\*audit 발견\*\*:' issue-0092-summary.md | grep -oE '[0-9]+' | paste -sd+ - | bc)
  fixed=$(grep -E '^- \*\*보정 반영\*\*:' issue-0092-summary.md | grep -oE '[0-9]+' | paste -sd+ - | bc)
  echo "보정률: ${fixed}/${found}"

  # 모델별 집계는 `수행 모델` 행과 함께 뽑는다
  grep -E '^- \*\*(수행 모델|audit 발견|보정 반영|재시도)\*\*:' issue-0092-summary.md
-->

### Task 0 (고정): 구현 시작 게이트 (전제·모호점 확인)

<!--
plan 템플릿의 첫 고정 Task에 대응하는 블록이다. 삭제하지 말 것.
질의로 해소한 전제는 spec `## 전제 (Assumptions)`에 남기고, 여기에는 수행 사실만 적는다.
나열한 항목이 없었으면 `수행 내용 요약`에 `전제 누락 없음` 한 줄을 기록한다.
-->

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5[1m])
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: 전제·모호점 5건을 나열해 전부 spec `## 전제 (Assumptions)`에 반영했다. 사용자 질의 2건(Task 5 대화형 시험의 응답 주체, 커밋 단위)은 답변을 받아 확정했고, 나머지 3건(근거 문서 "없음" 판정 기준, 시험 실행 시 스킬 호출 방법, R5 선택지 정규식 일반화)은 사전 확인으로 해소해 기록했다. 미해소 0건.
- **특이 사항**: 사전 확인 결과 spec 전제와 실제가 일치했다. `gh` 인증 정상, PR #91 조회 가능(머지 상태, head SHA `e1543ed…`, 41개 파일), `install-skills`는 `*/SKILL.md` 동적 스캔이라 새 스킬 등록 작업이 없다. `.ai/40_domain/glossary.md`는 파일이 존재하나 빈 표 템플릿이라 근거 문서 0건 판정이 유지된다.

---

### Task 1: 산출 파일 템플릿 quiz-template.md 작성

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5[1m])
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: `git-review-quiz/templates/quiz-template.md`를 만들었다. 첫머리 SSoT 동기화 주석, `# 리뷰 퀴즈` 제목, `## 대상` 표(대상·기준 커밋·형식·관점·근거 문서·문항 수 6행), `## 문항`(Q1 테크 객관식 선택지 3개, Q2 비즈니스 주관식 permalink 병기), `## 응답 기록` 순서다. 정답·해설 블록은 `(<문자>). <근거>` 또는 모범 답안 다음에 `근거:` 행, 비즈니스 문항은 `정책 근거:` 행을 두는 형식으로 고정했다. 완료 기준 2건 모두 통과했고 em dash 0건이다.
- **특이 사항**: 검증 명령이 이 세션 기본 셸(zsh)에서 `bad output format specification`으로 실패한다. zsh가 큰따옴표 안 `$h[[:space:]]`를 배열 첨자로 파싱하기 때문이며 명령 자체의 결함은 아니다. `bash`로 감싸 실행하면 통과한다. Task N 재검증 때도 bash로 실행해야 한다.

---

### Task 2: 형식 검사 헬퍼 check-quiz.sh와 회귀 테스트 작성

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5[1m]) / Anthropic, Claude Fable 5 (claude-fable-5)
- **audit 발견**: 8건
- **보정 반영**: 8건
- **재시도**: 5회
- **수행 내용 요약**: `git-review-quiz/scripts/check-quiz.sh`, `tests/run-tests.sh`, `tests/fixtures/` 18개(정상 2개, 반례 16개)를 만들었다. 헬퍼는 `^### Q[0-9]+ \[` 앵커로 문항 블록을 나눠 R1~R6을 블록 단위로, R7을 파일 전체로 판정하고 `<details>`/`</details>` 행으로 접기 깊이를 토글한다. 통과는 무출력·종료 0, 위반은 `위반 R<n> Q<m>: <사유>` 1행씩·종료 1, 사용오류는 종료 2다. 러너는 27 케이스(템플릿 SSoT 정합, 정상 fixture 전수, 반례 16종 사유 대조, 규칙·fixture 커버리지, 사용오류 5종)를 돌려 0 실패로 끝난다. 완료 기준 3건과 spec DoD 3·4·7 검증 명령이 모두 출력 0건이다.
- **특이 사항**: R2 병기를 `(base)`와 permalink 동시 표기까지 허용했다. spec R2 문구는 둘을 "또는"으로 열거하지만, 삭제 코드 문항을 `--comment`로 게시하면 base SHA permalink 행에 `(base)` 표기가 함께 있어야 Task 5 검증 명령의 SHA 대조 제외가 성립한다. 정상 fixture `valid-comment.md`의 Q2가 이 조합을 회귀로 고정한다. 또 헬퍼는 마크다운 코드 펜스를 상태로 다루지 않아 문항 본문 코드 블록 안의 `### `·`<details>` 행도 문서 구조로 센다. 이 전제를 헬퍼 첫머리 주석에 적었고, Task 3에서 SKILL.md `## 문항 생성 규칙`에 "문항 본문에 코드 블록을 두지 않는다"를 넣어야 정합이 유지된다.

  **Task N audit 보정 (2026-08-30, `--response` 승인 게이트 통과분 4건)**: 초판 헬퍼가 규칙을 옮기다 만 지점 네 곳을 채웠다.
  F-1은 접기 깊이와 블록 소유를 보지 않아 닫히지 않은 `<details>`와 힌트·정답 한 블록 병합이 통과했고, 깊이가 새면 이후 행이 전부 접기 안으로 판정되어 R6(정답 비노출)까지 무력화됐다 (재현으로 확인, audit 리포트에 없던 파급).
  F-3은 `^### Q[0-9]+ \[`에 맞는 헤더만 문항으로 열어 `### QX` 같은 헤더를 조용히 버렸다. 중간 문항이면 번호 연속성으로 우연히 잡히지만 마지막 문항은 검출되지 않았다.
  F-2·F-4는 스펙 문언 초과라 낮음(LOW)으로 재산출했고, 사용자가 원장 이관 대신 승격을 선택해 함께 반영했다.
  보정 내용은 `## 문항` 아래 모든 `### `를 문항 후보로 열기, 문항 종료 시 접기 깊이 0과 짝 없는 닫기 태그 판정, 힌트·정답의 접기 블록 분리 판정, 선택지 라벨의 `(a)` 시작·연속·중복 없음 판정, `--comment [--head <SHA>] [--base <SHA>]` 모드 신설(위치 행 permalink 전수 검사, URL 규격 대조, 기준 SHA 대조, `## 응답 기록` 부재)이다.
  반례 fixture 13개(전체 18개에서 31개)와 러너 케이스 19건(반례 14, 댓글 모드 정상 2, 사용오류 3)을 더해 27건에서 46건이 되었고 0 실패다. 반례 14건 중 1건은 `valid-comment.md`에 기준과 다른 `--head`를 넘겨 SHA 대조만 격리해 본다.
  스펙은 R1~R7 표·헬퍼 사용법·DoD 5를 함께 고쳤다. DoD 5의 permalink 검사는 `grep -q` 1건 탐침에서 **위치 행 총수 대 permalink 행 총수 대조**로 바꿔, 헬퍼의 `--comment` 검사가 무력화돼도 DoD가 독립적으로 잡게 했다.
  SKILL.md는 6단계 헬퍼 호출에 댓글 모드 명령을 추가하고, 형식 규칙에 "`## 문항` 아래 `### `는 모두 문항", 힌트 규칙에 "힌트와 정답은 독립된 접기"를 명문화했다.

  **Task N audit 2차 보정 (2026-08-30, `--response` 승인 게이트 통과분)**: 2차 리포트 발견 5건(F-1·F-3·F-4 잔여, F-5·F-6 신규) 가운데 4건을 보정하고 잔여 2건을 원장으로 이관했다.
  F-5는 이 Task의 두 번째 완료 기준 명령이 댓글 전용 반례 `invalid-comment-*` 4개를 일반 모드로 돌려 실패했다. 1차 보정에서 댓글 모드 fixture를 넣으며 plan 명령을 갱신하지 않은 누락이며, 명령을 일반 반례·댓글 반례로 분기했다.
  F-1 잔여는 같은 행의 `</details><details><details>`에서 선행 닫기를 놓쳤다. 행 단위 개수 합산을 태그 등장 순서 처리로 바꿨다.
  F-6은 주관식이 힌트·정답 접기 뒤에 둔 선택지를 세지 않아 R5의 0개 규칙을 우회했고, 객관식도 첫 접기 뒤 선택지가 통과했다. 접기 밖 선택지는 첫 접기 앞에만 유효한 것으로 불변식을 통째로 명세했고(주관식은 위치 무관 0개, 객관식은 첫 접기 뒤 선택지를 위치 위반으로 보고) spec R5 문구를 함께 고쳤다.
  F-4 잔여는 permalink의 `http` 허용만 spec 문언(`https://`) 위반으로 보정했다. 확정 저장소·경로·줄 범위 대조는 기능 저하 × 엣지로 낮음(LOW)을 재산출해 원장 K-0007로 이관했다.
  F-3 잔여(형식 어휘 `OX` 반례 fixture 부재)는 헬퍼가 이미 거부하는 동작이라 등급별 기본값대로 원장 K-0008로 이관했다.
  반례 fixture 5개(31개에서 36개)와 러너 케이스 5건(46건에서 51건)을 더해 0 실패다. 지표 3종은 발견 단위 누적이라 F-1·F-3·F-4 잔여는 1차 집계에 포함된 것으로 보고 늘리지 않았고, 신규 F-5·F-6만 `audit 발견`·`보정 반영`에 더했다.

  **Task N audit 3차 보정 (2026-08-30, `--response` 승인 게이트 통과분 1건)**: F-8은 1·2차 보정 뒤 active plan의 서술만 소스·spec과 어긋난 채 남은 건이다.
  Task 2 작업 내용 2번을 "`## 문항` 아래 모든 `### `를 문항 후보로 열고, 접기 깊이는 태그 등장 순서로 센다"로 고쳤고, Task 5 첫 완료 기준 명령을 spec DoD 5와 같은 `--comment --head` 호출과 위치 행 총수 대 permalink 행 총수 대조로 바꿨다.
  낮음(LOW)의 등급별 기본 제시값은 이관이지만, active plan은 `--clear`로 archive에 들어가면 갱신 대상이 아니어서 원장의 재검토 조건을 쓸 수 없다. 이 점을 제시해 사용자가 반영을 선택했다.

  **Task N audit 4차 보정 (2026-08-30, `--response` 승인 게이트 통과분 1건)**: F-9는 헬퍼가 정답 블록의 존재·위치만 보고 내용을 읽지 않는 건이다.
  재현에서 정답 내용 3행을 지운 입력, 정답 문자를 선택지에 없는 `(z)`로 바꾼 입력, `근거:` 행을 지운 입력이 모두 무출력·종료 코드 0이었다.
  위험도는 중간(MEDIUM)에서 낮음(LOW)으로 재산출했다. spec R4·R5 문언이 내용 계약을 담지 않아 영향 축이 스펙 달성 차단이 아니라 기능 저하이며, 2차 F-2·F-4를 같은 이유로 낮춘 기준과 맞췄다. 등급별 기본 제시값은 이관이지만 사용자가 반영을 선택했다.
  보정은 정답 블록 안 첫 내용 행의 형식 판정(객관식 `(<문자>). <해설>`, 주관식 모범 답안), `근거:` 행과 비즈니스 문항 `정책 근거:` 행의 존재 판정을 R4에, 정답 문자와 선택지 라벨의 결속 판정을 R5에 넣는 것이다. 결속 판정은 라벨이 연속일 때만 돌린다. 어긋난 라벨 집합을 기준으로 삼으면 R5가 이미 보고한 사유와 뒤바뀐다.
  정답 블록 내부는 정답 `<summary>`를 만난 시점부터 접기 깊이가 0으로 돌아올 때까지를 블록으로 보고 태그 행·빈 행을 제외해 모은다.
  반례 fixture 6개(36개에서 42개)와 러너 케이스 6건(51건에서 57건)을 더해 0 실패다.
  spec R4·R5 문언과 SKILL.md 형식 규칙, plan Task 2 작업 내용 2·4번을 함께 고쳤다. 3차 F-8과 4차 F-10이 모두 plan 동기화 누락이었으므로, 이번에는 보정과 같은 회차에 plan을 갱신했다.

  **Task N audit 5차 보정 (2026-08-31, `--response` 승인 게이트 통과분 1건)**: F-9 잔여는 4차가 넣은 정답 블록 내용 판정을 두 인접 변형이 빠져나가는 건이다.
  재현에서 객관식 첫 행 `(a).  `(정답 문자 뒤 공백만)와 주관식 첫 행 `<!-- TODO: 모범 답안 -->`가 모두 무출력·종료 코드 0이었다.
  위험도는 중간(MEDIUM)으로 재산출했다. 4차에서 낮음(LOW)으로 낮춘 근거는 spec R4 문언에 내용 계약이 없다는 것이었는데 그 4차 보정이 문언을 넣었으므로, 영향 축이 기능 저하에서 스펙 달성 차단으로 돌아온다. 1단계 요구사항 3 부분 충족(PARTIAL)의 유일한 근거이기도 해 한 항목으로 병합해 1단계로 처리했다.
  보정은 두 가지다. 정답 블록 내용 수집에서 한 행짜리 HTML 주석을 빼 렌더링되지 않는 자리표시자가 내용으로 서지 않게 했고, 객관식 첫 행 판정을 `^\([a-z]\)\. [[:space:]]*[^[:space:]]`로 바꿔 정답 문자 뒤에 실제 해설이 있어야 통과하게 했다. R5 정답 문자 결속 판정도 같은 정규식으로 맞춰 두 판정의 사유가 뒤바뀌지 않게 했다.
  주석을 수집에서 빼면 주관식 케이스는 다음 행 `근거: `가 첫 내용 행이 되어 기존 "모범 답안이 없습니다" 사유로 잡힌다. 새 사유를 만들지 않은 이유다.
  반례 fixture 2개(42개에서 44개)와 러너 케이스 2건(57건에서 59건)을 더해 0 실패다. spec R4에 내용 행의 정의를, SKILL.md 형식 규칙과 plan Task 2 작업 내용 2·4번에 같은 기준을 함께 적었다.
  `audit 발견`·`보정 반영`은 F-9가 4차 집계에 든 값이라 늘리지 않았다. `재시도`는 4차 보정에서 갱신이 빠진 것을 확인해 Task 2를 5회, Task 5를 2회로 맞췄다.

---

### Task 3: SKILL.md 작성

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5[1m])
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: `git-review-quiz/SKILL.md`(258행)를 작성했다. 프론트매터, 표준 섹션 5개, 고유 섹션 3개(`## 실행 절차`·`## 문항 생성 규칙`·`## 대화형 진행`), `## 결과 기록`, 옵션 소절 3개 구성이다. 실행 절차는 대상 식별부터 대화형 진행·게시까지 7단계로 나눴고, 각 단계에 bash 코드 블록(저장소 확정·PR 감지, PR/브랜치 모드 diff, 헬퍼 호출, 댓글 게시)을 뒀다. 완료 기준 4건([D])이 모두 출력 0건으로 통과했다.
- **특이 사항**: Task 2가 넘긴 전제를 `## 문항 생성 규칙`의 "문항 본문에 코드 블록을 두지 않습니다" 소절로 명문화해 헬퍼의 행 접두어 판정과 정합을 맞췄다. 건너뛴 문항은 `## 응답 기록` 표에 행을 추가하지 않고 결과 요약 목록에만 남기게 했다. 템플릿이 판정 값을 정답·부분·오답 3종으로 고정해 두어, 4번째 값을 넣으면 템플릿과 tests 기대값까지 함께 바꿔야 하기 때문이다. em dash 금지 때문에 형제 스킬의 `## 참조 문서` 관용 표기(`파일 — 설명`)를 괄호 병기로 바꿨고, 이 표기만 다른 git-* 스킬과 어긋난다. `gh pr view --json`에 `baseRefName`·`baseRefOid`를 추가했다. git-pr-feedback 원본에 없는 필드이며 삭제 코드 permalink의 base SHA 기준값이라 필요하다.

---

### Task 4: AI-CONTEXT.md와 README.md에 새 스킬 반영

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5[1m])
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: 다섯 위치에 `git-review-quiz` 행을 넣었다. `.ai/AI-CONTEXT.md`는 디렉토리 구조 트리(`git-review-context/` 블록 뒤, 하위 행 `scripts/`·`templates/`·`tests/` 3개), 스킬 목록 표, Git 정책 표 세 곳이고, `README.md`는 Skill 목록 표와 Skill 간 관계도 두 곳이다. 안내도 상단 `last updated`를 2026-08-30으로 갱신했다. 완료 기준 2건([D])이 모두 출력 0건으로 통과했다.
- **특이 사항**: 트리와 관계도의 주석 열을 기존 행과 맞췄다. 안내도 트리는 `#`가 33번째 글자, README 관계도는 `←`가 25번째 글자에 오도록 공백을 계산했다. 박스 문자가 3바이트라 바이트 열로 맞추면 어긋난다. `README.md`는 `readme-sync`를 돌리지 않고 두 행만 손으로 넣었다(spec 전제, 최소 변경). README 본문의 "`git-review`와 `git-review-context`는 함께 쓸 수 있지만" 문장은 새 스킬을 언급하지 않지만, 완료 기준과 spec 범위 밖이라 그대로 뒀다.

---

### Task 5: 실제 PR로 시험 실행

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5[1m])
- **audit 발견**: 2건
- **보정 반영**: 2건
- **재시도**: 2회
- **수행 내용 요약**: 머지된 PR #91(41개 파일, head `e1543ed…`)을 대상으로 두 경로를 실제로 돌렸다. 근거 문서 판정은 `.ai/30_contract/index.md`가 자리표시자 행뿐이고 `.ai/40_domain/`이 `.gitkeep` 3개와 빈 표 템플릿뿐이라 0건이 나와, "비즈니스 문항 없음, 테크 문항만" 경로가 그대로 재현됐다. `--comment` 경로는 테크 객관식 5문항을 `temp_review_quiz_comment.md`에 쓰고 승인 게이트에서 파일 경로·문항 수·관점별 건수·위치 목록을 제시한 뒤 게시하지 않고 중단했다. 대화형 경로는 다른 소재로 5문항을 새로 내고 사용자가 직접 응답해 4/5 정답(Q1 오답, 힌트 1회)으로 끝났다. 힌트 요청·오답 판정·결과 요약 흐름을 모두 거쳤고, 두 파일이 `check-quiz.sh`를 통과했다. 완료 기준 [D] 2건이 출력 0건이다.
- **특이 사항**: SKILL.md 보정 없이 통과해 6단계 재실행이 없었다. 대화형 시험이 한 차례 중단됐다가 재개됐는데, 응답 기록 표에 행이 없으면 DoD 5의 "응답 기록 3행 이상"이 서지 않아 Task가 미완료로 남는다는 점이 실제로 드러났다. 중단·재개 시 문항 파일을 다시 만들지 않고 이어 쓰는 것으로 처리했고, 이 흐름은 SKILL.md "그만" 규칙과 어긋나지 않아 절차 보정은 하지 않았다. 두 산출 파일은 `--clear` 5단계 정리 대상이라 Task N 검증을 `--clear` 전에 마쳐야 한다. Task N audit 보정 뒤 두 산출물을 다시 검사해 통과를 확인했다. 댓글 본문은 새로 생긴 `--comment --head e1543edc…` 경로로 돌렸고, 5개 위치 행의 permalink가 전수로 head SHA와 일치한다. 2차 보정 뒤에도 두 산출물이 일반·댓글 모드 검사를 통과했다.

  **Task N audit 3차 보정 (2026-08-30, `--response` 승인 게이트 통과분 1건)**: F-7은 대화형 Q1의 정답이 실제 동작과 반대인 건이다. 1단계 DoD 8 부분 충족의 근거와 같은 결함이라 한 항목으로 병합해 1단계로 처리했다.
  `verify-submit.sh`는 완전한 패턴 `refs/heads/$branch`를 넘기므로 짧은 `main`에서만 나오는 다중 ref 매칭이 일어나지 않는다. 임시 bare 저장소 재현에서 완전한 패턴은 1행, 짧은 패턴은 2행이었다.
  선택지 (a)도 성립하지 않는다. 행 수 검사를 없애도 빈 출력은 `same_sha`가 짧은 쪽 길이 7 미만으로 종료 코드 2를 내 통과하지 못한다. 세 선택지에 정답이 없는 문항이었고, 이 파급은 audit 리포트에 없었다.
  Q1을 `normalize()`의 `case` 갈래 순서를 묻는 문항으로 교체하고 정답을 갈래를 뒤집은 사본 실행으로 확인했다(원본 `github.com/o/r`, 뒤집은 사본 `https///github.com/o/r`). 사용자가 새 Q1에 다시 응답해 힌트 1회·오답 1건, 4/5 정답으로 응답 기록 5행을 유지했다.
  재발 방지로 SKILL.md `## 문항 생성 규칙` 근거 규칙에 "정답과 해설은 실제 동작으로 확인한다"를 넣었다. 주석이 일반적인 성질을 설명하는데 코드는 그 성질을 피해 가는 인자를 쓰는 경우가 이번 사례다. spec DoD 8에도 "정답·해설의 서술이 소스의 실제 동작과 일치한다"를 더해, audit이 실제로 채점한 기준과 문언을 맞췄다.
  리포트에 없던 항목 1건도 승인을 받아 함께 고쳤다. `git-pr/scripts/verify-submit.sh:10-11` 머리말 주석이 짧은 패턴 기준으로 행 수 검사의 이유를 설명해 퀴즈를 오도했다. 코드가 완전한 패턴으로 다중 매칭을 먼저 막는다는 사실을 주석에 반영했고, `git-pr/tests/run-tests.sh:189-197`이 이미 고정한 회귀(완전 ref 1행)와도 정합이다. 이슈 #92 범위 밖 파일이라 지표에는 세지 않았다.
  보정 뒤 git-pr 테스트 59건, git-review-quiz 테스트 51건이 0 실패이고 spec DoD 2·5·7 명령이 출력 0건이다.

  **Task N audit 4차 보정 (2026-08-30, `--response` 승인 게이트 통과분 1건)**: F-10은 F-7 보정으로 spec DoD 8과 SKILL.md에 실제 동작 대조 기준을 넣은 뒤 active plan만 이전 문구에 머문 건이다. 3차 F-8과 같은 패턴의 재발이다.
  plan Task 3 작업 내용 6번의 근거 규칙에 실제 동작 대조와 최소 재현을 넣고, Task 5 작업 내용 4번의 절차 결함 목록에 정답·해설과 실제 동작의 불일치를 더했으며, Task 5 `[QD]` 완료 기준을 spec DoD 8과 같은 문언으로 맞췄다.
  낮음(LOW)의 기본 제시값은 이관이지만 3차 F-8과 같은 이유로 사용자가 반영을 선택했다. active plan은 `--clear`로 archive에 들어가면 갱신 대상이 아니어서 원장의 재검토 조건이 성립하지 않는다.
  보정 뒤 시험 산출물 두 개가 새 정답 블록 계약을 포함한 일반·댓글 모드 검사를 그대로 통과했고, spec DoD 1·2·3·4·5·7 명령이 모두 출력 0건이다.

---

### Task N (고정): 교차모델 issue-audit 검증 (사용자 수동 수행)

<!--
plan 템플릿의 마지막 고정 Task에 대응하는 블록이다. 삭제하지 말 것.
사용자가 직접 수행하며, 구현 AI는 이 블록을 대신 채우지 않는다.
지표 4종을 두지 않는다 — 수행 모델은 위 `모델 기록` 표의 `audit 모델` 행이 SSoT이고,
audit 발견·보정 반영은 이 Task가 만들어낸 값이라 각 대상 Task 블록에 집계된다.
-->

- **결과**: 완료
- **수행 내용 요약**: 사용자가 OpenAI GPT-5로 `issue-audit`를 6차까지 수행했다. 리포트는 이 이슈 디렉토리의 `./issue-0092-audit-report-1.md`~`./issue-0092-audit-report-5.md`와 `./issue-0092-audit-report.md`(6차)다 (작성 시점 경로는 `.ai/99_workspace/`, --clear로 이관). 누적 발견 10건(F-1~F-10)을 `--response`로 회차마다 검토해 전부 승인·보정했고, 발견의 잔여 수용분 2건은 원장 K-0007·K-0008로 이관했다. 지표 귀속은 Task 2 8건(F-1~F-6, F-8, F-9), Task 5 2건(F-7, F-10)이다. 6차 리포트는 1단계 충족(PASS) 14건·미충족(FAIL) 0건·부분 충족(PARTIAL) 0건, 2단계 발견 0건, 이전 발견 10건 전부 닫힘으로 스펙 충족 판정을 냈고 보정 대상이 없어 `--response` 보정 루프를 열지 않았다. 완료 직전 spec `[D]` 항목 7건과 Task N 게이트 3건을 재실행해 모두 통과(회귀 테스트 59건 0 실패)를 확인했다.
- **특이 사항**: 회차가 늘어난 원인은 두 가지다. 초판 헬퍼가 규칙을 옮기다 만 지점(F-1~F-6, F-9)이 반복해서 인접 변형으로 재발했고, 보정 뒤 active plan 서술만 spec·소스와 어긋나 남는 동기화 누락(F-8, F-10)이 두 번 나왔다. 4차부터는 보정과 같은 회차에 plan을 함께 갱신해 후자를 닫았다. F-7 보정 과정에서 이슈 범위 밖 파일(`git-pr/scripts/verify-submit.sh` 머리말 주석)도 승인을 받아 함께 고쳤고 지표에는 세지 않았다. 6차 리포트는 감사 종료 경계를 명시해(템플릿이 만드는 정규 구조와 spec R1~R7 명시 조건까지) 최초 초안의 과잉 발견을 스스로 철회했다.
