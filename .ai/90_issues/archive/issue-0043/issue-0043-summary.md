# Issue #43 실행요약 issue-work: 모델 분리 운용 지원 — 설계 문서 점검 게이트 및 Task별 모델·보정 지표 기록

> 스펙: [issue-0043-spec.md](./issue-0043-spec.md) | 계획: [issue-0043-plan.md](./issue-0043-plan.md)

## 다음 작업

> ✅ 모든 작업이 완료되었습니다.
>
> 사용자 승인(2026-07-17)으로 Task N을 마감했다 — 7차 audit 최종 판정 PASS, `[D]` 8건 재실행 통과, audit 모델(OpenAI, GPT-5) 기록 완료.
> --clear 수행 완료(2026-07-17) — 이슈 댓글 등록, archive 이관, 99_workspace 정리(audit 리포트는 본 디렉토리로 이관 보존).

## 모델 기록

<!--
이슈 단위 요약(대표값). 세부 SSoT는 아래 `Task별 수행 결과`의 `수행 모델`이다.
형식: "벤더, 모델명". "audit 모델 ≠ 구현 모델" 조건을 나중에 명령으로 확인 가능하도록 기록한다.
-->

| 구분 | 모델 |
|------|------|
| 설계 모델 | Anthropic, Claude Opus 4.8 (claude-opus-4-8) |
| 구현 모델 | Anthropic, Claude Opus 4.8 (claude-opus-4-8) |
| audit 모델 | OpenAI, GPT-5 |

---

## Task별 수행 결과

### Task 1: 결정거리 4건 확정

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 4.8 (claude-opus-4-8)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: 결정거리 4건을 아래와 같이 확정했다.

  **결정 1 — `모델 기록` 표 구조: 쪼개기 + Task별 `수행 모델`과 병존**

  표를 `설계 모델` / `구현 모델` / `audit 모델` 3행으로 분리하고, Task별 `수행 모델` 필드와 병존시킨다.
  표는 이슈 단위 **요약(대표값)**, Task별 `수행 모델`이 **세부 SSoT**임을 표 주석에 명시해 중복 drift를 막는다.

  - 기각 — *Task별 수행 모델로 대체*: Task N 완료 기준이 "구현 모델 ≠ audit 모델"을 확인하는데, 기록이 Task별로만 흩어지면 audit 모델과 **어느 Task의 수행 모델을 비교할지** 모호해져 #29의 audit 기록 규칙이 약해진다.
  - 기각 — *현행 단일 행 유지*: `계획·구현 모델` 한 행은 "설계 모델 ≠ 구현 모델"이라는 본 이슈의 전제와 정면 충돌한다.

  **결정 2 — 구현 시작 게이트 위치: plan `Task 0` 고정 블록 + workflow `작업 진행 중` 한 줄**

  #29 F-1 선례("옵션 + 절차문서 둘 다")를 따른다. plan 고정 블록은 체크박스로 추적되고, workflow 템플릿 한 줄은 컨텍스트 초기화 후에도 절차를 유지한다.
  실행 Task이므로 `Task 0`으로 두어 기존 Task 1~N 번호 체계를 건드리지 않는다.

  **결정 3 — 설계 종료 게이트 형태: `## Tasks` 앞 고정 블록**

  이 게이트는 "plan 작성 절차"라 실행 Task 목록과 층위가 다르다. Task 0은 결정 2가 사용하므로, `## Tasks` 섹션 **앞**에 별도 고정 블록으로 둔다.

  **결정 4 — 전제 섹션: spec 신설 `## 전제(Assumptions)`**

  위치는 `완료의 정의` 다음 / `연관 문서` 앞. DoD를 상단에 유지하고, 전제·연관 문서를 참조 정보로 하단에 묶는다. `연관 문서` 확장안은 표 한 칸에 서술형 전제를 넣게 되어 기각했다.

- **특이 사항**: 이슈 본문은 `모델 기록` 표 참조를 **4곳**으로 봤으나 전수 조사 결과 **5곳**이다 — `SKILL.md:14`(`## 관련 skill`의 issue-audit 항목)가 "summary의 모델 기록·Task별 결과에 반영"으로 같은 표를 참조한다. Task 5의 갱신 범위를 5곳으로 확장한다.
  Task 1의 사용자 승인은 `/goal` 지시("Task N 시작 직전까지 진행")로 위임된 것으로 간주하고 진행했다.

---

### Task 2: spec 템플릿에 전제(Assumptions) 섹션 추가

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 4.8 (claude-opus-4-8)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 1회
- **수행 내용 요약**: 결정 4에 따라 `## 전제 (Assumptions)` 섹션을 `완료의 정의` 다음 / `연관 문서` 앞에 신설했다. 무엇을 적고 무엇을 적지 않을지(적을 것: 코드베이스 관례·버전 제약·버린 대안·대화로만 정해진 값 / 적지 않을 것: 코드에서 드러나는 사실, 연관 문서가 담은 내용)를 주석으로 안내하고, 발견이 없으면 "없음" 한 줄을 남기게 했다 — 빈 섹션과 "확인했고 없음"을 구분하기 위함이다.
- **특이 사항**: 최초 완료 기준 `grep -c '전제(Assumptions)'`가 0을 반환해 **재시도 1회**. 헤더를 기존 템플릿 스타일(`## 범위 (Scope)`)에 맞춰 괄호 앞 공백을 넣었는데, 검증 패턴은 공백 없는 문자열이었다. 헤더 스타일을 유지하고 spec DoD·plan 완료 기준의 패턴을 `^## 전제 (Assumptions)`로 정정했다. 가짜 `[D]`를 실제로 걸러낸 사례다.

---

### Task 3: plan 템플릿에 게이트 2건 추가

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 4.8 (claude-opus-4-8)
- **audit 발견**: 1건
- **보정 반영**: 1건
- **재시도**: 0회
- **수행 내용 요약**: 설계 종료 게이트를 `## Tasks` 앞 고정 블록으로(결정 3), 구현 시작 게이트를 `Task 0` 고정 블록으로(결정 2) 추가했다. 결정 2의 "둘 다"에 따라 `issue-workflow-template.md`의 `작업 진행 중`에도 한 줄을 넣어 컨텍스트 초기화 후에도 절차가 유지되게 했다. Task N 교차모델 audit 고정 블록은 그대로 뒀고, Tasks 안내문을 "첫·마지막 고정 Task는 삭제하지 말 것"으로 갱신했다.
- **특이 사항**: 두 게이트의 역할을 블록 주석에서 구분했다 — 설계 종료 게이트는 **쓰는 쪽**의 자기점검, 구현 시작 게이트는 **읽는 쪽**이 착수 전에 거는 확인이다. 양쪽 모두 "모델이 달라서"가 아니라 "세션이 바뀌면 전제가 유실되어서" 수행한다고 명시해 모델 분리 여부와 무관하게 동작하게 했다(spec DoD의 `[QD]` 항목).

  **6차 audit 발견 1건 보정**:
  1. **6차 F-3** — workflow 게이트 검사(`grep -c '구현 시작 게이트'` ≥ 1)가 비앵커 문구 검색이라, 절차 bullet을 `<!-- 구현 시작 게이트 -->` 주석으로 바꾼 변형도 1로 통과한다 — 역검증: 변형 사본에서 구 명령은 1(가짜 통과), 보정 명령은 FAIL(정상 불통과, bullet을 다른 섹션으로 옮긴 변형도 동일). spec 스스로 세운 "문구가 아니라 행 구조를 센다" 규칙을 이 검사만 따르지 않고 있었다. `## 작업 진행 중` 섹션 추적 + bullet 행 앵커 awk(해당 섹션 1건 + 총계 1건 = PASS)로 spec DoD #2를 교체하고 재실행해 통과를 확인했다. 실제 workflow 문서는 올발라 검증 명령만 교체했다.

---

### Task 4: summary 템플릿에 Task별 지표 필드 추가·모델 기록 표 개편

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 4.8 (claude-opus-4-8)
- **audit 발견**: 8건
- **보정 반영**: 8건
- **재시도**: 0회
- **수행 내용 요약**: Task 블록에 `수행 모델`·`audit 발견`·`보정 반영`·`재시도` 4종을 추가하고, `모델 기록` 표를 결정 1대로 `설계 모델`/`구현 모델`/`audit 모델` 3행으로 쪼갰다. 표 주석에 "표는 이슈 단위 요약(대표값), Task별 `수행 모델`이 세부 SSoT"를 명시해 병존 중복이 drift로 번지지 않게 했다.
  audit 보정으로 **Task 0·Task N 고정 블록을 추가**(F-1)하고 **수치 3종의 `-` 허용을 제거**(F-2)했다.
- **특이 사항**: 값이 없어도 필드는 남기도록 규정했다 — 사후 집계가 표기 고정에 의존하므로, 필드를 지우면 집계에서 그 Task가 조용히 빠진다. 모델명은 주석의 형식 예시로만 남아 절차·규칙에는 없다.
  **audit 발견 2건 보정**:
  1. **F-1** — plan의 고정 Task 0/N에 대응하는 summary 블록이 없었다. plan Task 0 완료 기준과 `SKILL.md:66`이 "summary Task 0에 `전제 누락 없음` 기록"을 요구하는데 정작 기록할 자리를 만들지 않았다. 게이트를 추가한 Task 3과 summary를 개편한 이 Task 사이의 연동 누락이다. Task 0은 지표 4종을 포함해 추가하고, **Task N은 결과·요약·특이 사항만** 뒀다 — audit 모델은 `모델 기록` 표가 SSoT라 중복이고, audit 발견·보정 반영은 이 Task가 *만들어낸* 값이라 각 대상 Task에 집계되므로 자기참조가 된다.
  2. **F-2** — `0 또는 -`를 허용했는데 집계 스니펫은 `[0-9]+`만 뽑는다. 값이 섞이면 `-`인 Task만 분모에서 빠지고(`-`+`3건` → 합계 `3`), 전부 `-`면 합계가 빈 문자열이 되어 `bc`에 넘길 값이 사라진다. 정작 이 필드를 만든 이유가 "조용한 누락 방지"였는데 허용값이 그 구멍을 냈다. 수치 3종은 `0건`/`0회` 숫자로 고정하고 `-`는 비수치 필드(`수행 모델`)에만 남겼다.

  **2차 audit 발견 1건 보정**:
  1. **2차 F-1** — 1차 F-1 보정에서 Task N 지표 제외 예외를 도입하면서 "각 Task 블록" 일반 규칙 문구를 정합화하지 않아 같은 문서 안에서 계약이 충돌했다. summary 템플릿 주석과 `SKILL.md` `작업 진행 중`(Task 5 산출물이나 예외 설계의 근원이 이 Task라 여기 귀속)의 주어를 "Task 0 및 일반 실행 Task(Task N 제외)"로 정합화하고, spec Scope에도 제외·귀속 규칙을 명시했다.

  **3차 audit 발견 1건 보정**:
  1. **3차 F-2** — 지표 필드 검증(전역 합계 `= 12`)이 Task별 완전성을 보장하지 않았다. 필드 행끼리 맞교환하면 합계가 변하지 않아, Task 0의 `재시도`를 중복 `수행 모델`로 바꾼 변형도 통과한다 — 역검증: 변형 사본에서 구 명령은 `12`(가짜 통과), 보정 명령은 `FAIL`(정상 불통과). spec DoD·plan 완료 기준을 블록 단위 awk 검사(Task 0·1·2 각 4종 정확히 1회 + Task N 0건 = PASS)로 교체했다. 필드별 전역 카운트(각 = 3)도 블록 간 중복·누락 상쇄를 못 걸러 기각했다. 템플릿 자체는 정상이라 검증 명령만 교체했다.

  **4차 audit 발견 1건 보정**:
  1. **4차 F-1** — 3차 F-2 보정의 블록 단위 검사가 기대 Task의 정체성을 확인하지 않아, `Task 2` 헤더를 `Task X`로 바꾼 개명 변형도 통과한다 — 역검증: 변형 사본에서 구 명령은 `PASS`(가짜 통과), 보정 명령은 `FAIL`(정상 불통과, Task N 지표 주입·필드 삭제 변형도 동일). 기대 헤더(Task 0·1·2)를 열거해 각 4종 정확히 1회를 판정하고, 열거 외 블록(Task N 포함)의 지표 필드가 1건이라도 있으면 불통과시키는 열거식 awk로 spec DoD·plan을 교체했다. 이로써 검사 불변식(기대 블록 열거 + 그 외 0건)이 전수 명세되어 이 계열(1차 F-3 → 3차 F-2 → 4차 F-1)에서 더 뽑을 반례가 닫힌다. 재발 방지로 plan 템플릿 검증 안내문에 "검증 명령 보정은 반례 격추가 아니라 불변식 전수 명세로" 규칙을 추가했다 — 세 차례 반복의 공통 원인이 감사가 보여준 반례 1건만 막는 보정 방식이었기 때문이다.

  **6차 audit 발견 2건 보정**:
  1. **6차 F-1** — 지표 기본값이 HTML 주석(`<!-- 0건 -->`) 안에 있어 미입력 상태의 주석 수치를 집계기가 실제 값으로 오인하고(미입력 템플릿이 `0/0`으로 집계), 구조 검사도 값 형식을 안 봐 `미기록` 변형이 통과한다 — 역검증: 변형 사본에서 구 명령은 PASS(가짜 통과), 보정 명령은 FAIL(정상 불통과, 주석 placeholder 회귀 변형도 동일). 수치 3종 기본값을 리터럴 `0건`/`0회`(`수행 모델`은 `-`)로 바꾸고 그 근거를 템플릿 주석에 남겼으며, 검사에 수치 형식 `[0-9]+(건|회)` 판정을 추가했다. 집계 검증(DoD #7)도 고정 샘플(발견 3+1건 / 반영 2+0건)과 기대 출력 `2/4` 비교로 고정했다. 한계도 함께 기록한다 — 리터럴 기본값도 "갱신을 잊고 `0건` 방치"까지는 기계로 구분하지 못하며, 이는 표기 계약의 원리적 한계다(형식 검사가 잡는 것은 `미기록`류 형식 위반).
  2. **6차 F-2** — 검사 awk가 기대 헤더의 발생 횟수를 세지 않아, `### Task 1:`을 복제해 필드 4종을 두 블록에 나눈 분할 변형이 통과한다(어느 블록도 4종을 다 갖지 않는데 번호 키 합산으로 상쇄) — 역검증: 변형 사본에서 구 명령은 PASS(가짜 통과), 보정 명령은 FAIL(정상 불통과). 기대 헤더(Task 0·1·2) 발생 횟수 각 1회 판정을 추가했다. 4차 F-1이 명세한 불변식(기대 블록 열거 + 그 외 0건)에 "기대 블록의 유일성"이 빠져 있던 것으로, 6차 F-1의 값 형식 판정과 함께 같은 awk에 반영해 spec DoD #4·plan을 교체했다.

  **7차 audit 발견 1건 보정**:
  1. **7차 F-2** — 검사 awk가 Task 헤더 집합 자체를 판정하지 않아, Task N 블록을 삭제·중복하거나 지표 없는 `### Task 3:` 블록을 추가한 변형이 통과한다 — 역검증: 변형 사본 3종에서 구 명령은 PASS(가짜 통과), 보정 명령은 FAIL(정상 불통과). 4차 F-1이 명세한 불변식(기대 블록 열거 + 그 외 0건 + 유일성)이 지표 필드에만 적용되고 헤더 집합에는 빠져 있었다. 기대 헤더 집합에 Task N을 더해 각 1회·`^### Task ` 총계 4 판정을 추가해 spec DoD #4·plan을 교체했다. 템플릿 자체는 정상이라 검증 명령만 교체했다.

---

### Task 5: 참조 5곳 연동 갱신 (SKILL.md 3곳, plan Task N, workflow 템플릿)

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 4.8 (claude-opus-4-8)
- **audit 발견**: 4건
- **보정 반영**: 4건
- **재시도**: 0회
- **수행 내용 요약**: 모호하던 "summary 모델 칸"을 `summary 모델 기록 표의 <행 이름> 행`으로 정밀화해 5곳(plan Task N 완료 기준, SKILL.md `관련 skill`·`작업 진행 중`·`이슈 완료 시`, workflow 템플릿)을 갱신했다. plan Task N 완료 기준은 표가 3행이 된 만큼 "`구현 모델`·`audit 모델` 두 행이 서로 다른 벤더로 채워진다"로 바꿔 비교 대상을 명시했다. SKILL.md 본문에는 새 이슈 시작 시 4번(설계 종료 게이트), 작업 진행 중(Task 0 게이트·지표 4종 표기 유지), `--response` 5단계(승인 통과분만 `보정 반영`에 집계)를 반영했다.
- **특이 사항**: 이슈 본문이 참조를 4곳으로 봤으나 실제 5곳이었다(Task 1 특이 사항). `--response` 연계에서 `audit 발견`과 `보정 반영`의 차이가 곧 승인 게이트에서 걸러진 양임을 명시해, #31의 게이트와 지표 정의를 연결했다.

  **4차 audit 발견 1건 보정**:
  1. **4차 F-2** — 완료 기준의 "신규 문구가 5곳 모두에서 검출"이 무엇을 어느 파일에서 셀지 정하지 않아, 독립 실행자가 검색식을 임의로 골라야 했다 — spec이 정의한 `[D]`(명령이 합/불 판정, 사람 판단 없음) 위반이다. 참조 5곳의 공통 앵커(`` `모델 기록` 표…`audit 모델` ``)와 대상 파일 3개, 파일별 분포 판정 명령(`3/1/1`)으로 spec DoD·plan을 고정하고 재실행해 통과를 확인했다.

  **5차 audit 발견 1건 보정**:
  1. **5차 F-1** — 4차 F-2 보정의 파일별 분포 검사(`3/1/1`)가 파일 내부의 기대 섹션 정체성을 확인하지 않아, SKILL.md `관련 skill`의 일치를 복제하고 `작업 진행 중`의 일치를 삭제한 변형도 `3/1/1`로 통과한다 — 역검증: 변형 스트림에서 구 명령은 `3`(가짜 통과), 보정 명령은 `FAIL`(정상 불통과). 기대 위치 불변식을 전수 명세하는 섹션 추적 awk(SKILL.md 기대 3섹션 각 1건 + plan 템플릿 `Task N (고정)` 1건 + workflow 템플릿 `이슈 완료 시` 1건, 파일별 총계 고정으로 그 외 위치 0건 = `PASS/PASS/PASS`)로 spec DoD·plan을 교체하고 재실행해 통과를 확인했다. 실제 문서 5곳은 올바라 검증 명령만 교체했다.

  **7차 audit 발견 2건 보정**:
  1. **7차 F-1** — summary 표 주석은 다중 모델 관여 시 대표 모델 기재를 허용하는데 Task N 완료 기준은 표의 `구현 모델`·`audit 모델` 두 행만 비교해, audit 벤더가 구현 Task 일부를 수행해도 교차모델 조건을 통과한다. plan 템플릿 Task N 완료 기준과 summary 템플릿 표 주석에 "audit 모델의 벤더는 Task 0 및 일반 실행 Task의 비어 있지 않은 모든 `수행 모델` 벤더와도 달라야 한다"를 명시해, 세부 SSoT(Task별 `수행 모델`)가 조건 판정에도 쓰이게 했다. DoD #5의 앵커 행은 유지되어 검증 명령 변경은 없다.
  2. **7차 F-3** — DoD #5가 참조 문구 5곳만 검사하고 참조 대상인 `모델 기록` 표 3행 자체는 보지 않아, `audit 모델` 행을 삭제해도 전 검증이 통과한다 — 역검증: 변형 사본에서 행 삭제·행 중복 모두 보정 검사는 FAIL(정상 불통과), 구 체인은 표를 아예 읽지 않아 불변(가짜 통과). summary 템플릿 `## 모델 기록` 섹션의 3행 각 1회·그 외 위치 0건 판정을 체인에 추가해(`PASS/PASS/PASS/PASS`) spec DoD #5·plan을 교체했다. 템플릿 정상, 검증만 보강했다.

---

### Task 6: 검증 — 모델명 하드코딩 부재·grep 집계 가능성·소급 변경 부재

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 4.8 (claude-opus-4-8)
- **audit 발견**: 6건
- **보정 반영**: 6건
- **재시도**: 2회
- **수행 내용 요약**: spec DoD의 `[D]` 3건을 명령으로 확인했다.
  1. **모델명 하드코딩** — 검출 1건이 `issue-summary-template.md:22`의 주석 형식 예시뿐이라 절차·규칙 위반 0건.
  2. **grep 집계 가능성** — 샘플 summary(Task 2건, 발견 3+1건 / 반영 2+0건)에서 보정률 `2/4` 추출을 확인했다. 검증한 합계 집계 스니펫을 summary 템플릿 주석에 첨부하고, 스니펫이 의존하는 표기(`- **<필드>**: <숫자><단위>`)를 함께 명시했다.
  3. **소급 변경 부재** — 변경된 `90_issues` 경로에 `issue-0043` 외 항목 0건.
- **특이 사항**: **재시도 2회** — 검증이 두 번 가짜 통과를 냈고 둘 다 실제 결함을 덮고 있었다.
  1. 첫 실행이 이전 명령의 `cd issue-work/templates`를 물려받아 `issue-work/` 경로를 찾지 못한 채 "검출 없음"을 냈다. 절대 경로로 재실행하자 4건이 나왔다 — **검증 명령 자체가 작업 디렉토리에 의존하면 가짜 `[D]`가 된다.**
  2. 재실행에서 `Non-Anthropic 포함`이 SKILL.md 2곳·plan 템플릿 1곳의 **절차 문장**에 있음을 발견했다(이슈 제약 위반). #29에서 온 기존 문구지만 "다른 벤더 모델"이라는 요구가 이미 있어 벤더명은 사족이었고, 규칙의 의미를 바꾸지 않고 제거했다. 판정을 사람 눈에 맡기지 않도록 허용 라인 수를 빼는 카운트 방식으로 바꿨다.

  **audit 발견 2건 보정**:
  1. **F-3** — 이 Task가 통과시킨 `[D]` 명령들이 구조가 아니라 **문구**를 세고 있었다. 지표 DoD는 18을 반환했지만 실제 필드 행은 8행뿐이고 나머지 10건은 주석이라, 필드를 전부 지워도 주석의 단어 4종만으로 `≥ 4`를 통과한다. Task 2에서 가짜 `[D]` 하나를 걸러낸 것을 성과로 적어놓고 같은 함정을 다른 DoD에서 놓쳤다. spec DoD·plan 완료 기준의 grep을 `^## `·`^### Task `·`^- \*\*…\*\*:` 행 앵커로 고정하고, 임계값도 `≥ 4` 대신 정확한 개수(`= 12`)로 바꿨다. **역검증** — 필드 행만 제거한 사본에서 구 명령은 13건(가짜 통과), 보정 명령은 0건(정상 불통과)이다. 재발 방지로 plan 템플릿 Tasks 안내문에 "문구가 아니라 행 구조를 센다"와 작업 디렉토리 비의존 규칙을 넣어 신규 이슈에 자동 반영되게 했다. audit 권장안의 결정적 스크립트는 채택하지 않았다 — ADR 0001상 `scripts/`는 `tests/`를 동반해야 해서 문서 템플릿 이슈에 헬퍼+테스트를 들이는 스코프 확대가 된다.
  2. **F-4** — 3번 검증이 `90_issues` 하위 **이슈 파일**만 봐서 `.ai/AI-CONTEXT.md` 변경(커밋 `f83dbae`)을 놓쳤다. 스펙 `범위 (Scope)` 밖이 맞다. 별도 커밋으로 분리되어 커밋 메시지에 근거가 있으므로 되돌리지 않고, summary 하단 `## 범위 밖 변경` 섹션에 근거를 기재해 추적성을 보완했다.

  **2차 audit 발견 1건 보정**:
  1. **2차 F-2** — 3번 검증의 `git diff main...`이 커밋 차이만 비교해 미커밋·미추적 소급 변경을 못 본다(재현: `issue-work/` 미커밋 5건을 `main...`은 0건으로 보고). 추적 변경은 `git diff --name-only main -- .ai/90_issues/active`, 미추적 파일은 `git ls-files --others --exclude-standard -- .ai/90_issues/active`로 합산 검사하도록 spec DoD·plan을 교체하고 재실행해 통과를 확인했다.

  2차 F-3(INFO, 1차 F-4의 잔여 사항)은 `--response` 항목별 승인에서 **보류(현상 유지)** — 추적성이 이미 확보됐고 audit도 추가 보정 불요로 판정했다. 새 발견이 아니라 1차 F-4로 이미 집계된 건이라 지표에 중복 집계하지 않는다.

  **3차 audit 발견 1건 보정**:
  1. **3차 F-1** — 위 재시도 2 기록에는 "허용 라인 수를 빼는 카운트 방식으로 바꿨다"고 남겼으나 그 명령을 spec DoD·plan에 반영하지 않아, 모델명 검사가 여전히 출력을 사람이 판별하는 형태로 남았다 — spec 스스로 정의한 `[D]`(명령이 합/불 판정, 사람 판단 없음) 위반이자 기록·문서 불일치. 허용 예시 행을 결정적으로 제외하는 `grep -rnE '…' issue-work/ | grep -cvE 'issue-summary-template\.md:[0-9]+:\(예: '` = 0 판정을 spec DoD·plan에 명시해 기록과 문서를 정합화했다. 제외 패턴은 summary 템플릿의 `(예: ` 행에만 조여, 다른 파일에 위반이 생기면 잔여 건수로 검출된다.

  3차 F-3(INFO, 2차 F-3과 동일 건)은 audit 스스로 추가 보정 불요로 판정해 **보류(현상 유지)** 유지 — 기존 승인 건이라 지표에 중복 집계하지 않는다. 4차 F-3(INFO)·5차 F-2(INFO)·6차 F-5(INFO)·7차 F-5(INFO)도 동일 건으로 같은 판정을 유지하며 지표에 중복 집계하지 않는다.

  **6차 audit 발견 1건 보정**:
  1. **6차 F-4** — 모델명 검사가 5종 denylist인데 DoD 문구는 "특정 모델명이 없다"로 열린 집합을 주장해, 목록 밖 `Llama 4`를 넣은 변형도 잔여 0건으로 통과한다. 열린 집합의 전수 검증은 어떤 `[D]` 명령으로도 원리상 불가하므로 검사 결함이 아니라 **문구 과대 주장**으로 판정, DoD 주장을 "열거된 모델명 5종(Claude·GPT·Gemini·Opus·Sonnet) 부재"로 축소해 `[D]`를 유지했다 — audit이 제시한 `[QD]` 강등 대안은 동작하는 결정적 게이트를 버리는 손해가 커 기각했다. 목록 밖 신규 모델명은 `[ND]` 사람 리뷰 영역임을 spec DoD #6·plan에 명시했다.

  **7차 audit 발견 1건 보정**:
  1. **7차 F-4** — DoD #7의 고정 샘플 검사는 형식 계약만 방어해, 템플릿에 첨부한 실제 집계 스니펫을 삭제한 변형도 통과한다 — 역검증: 변형 사본에서 보정 검사는 FAIL(정상 불통과), 구 명령은 스니펫을 읽지 않아 불변(가짜 통과). 스니펫 핵심 2행(`found=$(`·`fixed=$(`)의 존재·유일성 awk 판정을 spec DoD #7·plan에 추가했다. audit이 언급한 결정적 헬퍼 승격은 ADR 0001상 `tests/` 동반이 필요한 스코프 확대라 기각했다(F-3 보정 때와 같은 근거).

---

### Task N: 교차모델 issue-audit 검증 — 사용자 수동 수행

- **결과**: 완료
- **수행 내용 요약**: 사용자가 구현 모델(Anthropic)과 다른 벤더 모델(OpenAI, GPT-5)로 `issue-audit`를 7차에 걸쳐 수행했다. 7차(최종 재검증) 판정은 **PASS** — 요구사항 7건·DoD 10건 전부 통과, 직전 보정 4건 전부 "해소", 잔여 위험은 기승인 INFO 1건(범위 밖 `.ai/AI-CONTEXT.md` 변경, 현상 유지)뿐이다. 마감 시점에 spec `[D]` 검증 명령 8건을 전부 재실행해 통과를 확인했고, audit 모델을 `모델 기록` 표에 기록했다. 리포트: [issue-0043-audit-report.md](./issue-0043-audit-report.md) (작성 시점 경로는 `.ai/99_workspace/issue-0043-audit-report.md`, --clear로 이관).
- **특이 사항**: 차수별 발견·보정은 각 대상 Task(3·4·5·6)의 지표에 귀속했다(합계 발견 19건·보정 19건, INFO 계열 보류 1건은 중복 집계 제외). 교차모델 조건은 강화 조건(audit 벤더가 모든 비어 있지 않은 `수행 모델` 벤더와 상이)까지 충족한다 — Task 1~6 전부 Anthropic, audit은 OpenAI. Task N 마감은 사용자 승인(2026-07-17) 후 수행했다.

---

## 범위 밖 변경 (참고)

Task 흐름에 속하지 않지만 같은 브랜치에 포함된 변경이다. 스펙 `범위 (Scope)`에는 없다.

| 파일 | 커밋 | 근거 |
|------|------|------|
| `.ai/AI-CONTEXT.md` | `f83dbae` | `ai-workspace` dev update 실행 중 `## 디렉토리 구조`의 표준 placeholder 주석 누락을 발견해 삽입하고 `last updated`를 갱신했다. 본 이슈와 무관한 선행 유지보수라 별도 커밋으로 분리했다. 기능 영향 없음(안내 주석·날짜). |
