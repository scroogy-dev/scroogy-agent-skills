# Issue #100 감사 리포트 요구사항 승인 게이트·계획 감사 모드·추적성 헬퍼

> 감사 일시: 2026-09-09
> 감사 모델: OpenAI, GPT-6
> 감사 회차: 3차 (이전 리포트: [1차](./issue-0100-audit-report-1.md), [2차](./issue-0100-audit-report-2.md))
> 감사 모드: 최종 감사
> 감사 대상 브랜치: `issue-0100` (`4c0371b`)
> 비교 기준: `main` (`a16fba1`) 대비 18개 변경 파일. F-1~F-3 보정 커밋 포함
> 스펙 출처: [issue-0100-spec.md](./issue-0100-spec.md)
> 기록 정정: 2026-09-09, 커밋별 재현으로 기존 F-4의 발생 시점을 구분했다. F-2 잔여분은 F-4로 유지하고 보정 중 유입된 회귀는 F-5로 분리했다. 같은 3차 감사 리포트의 정정이다.

---

## 종합 의견

🟡 조건부 적합(CONDITIONAL)

<details>
<summary>종합 의견 펼치기</summary>

- F-1·F-3의 완료 기준은 충족했다. 최초 구현부터 있던 주석 속 연결 오집계는 F-4(F-2 잔여), 보정 중 유입된 정상 본문 누락은 F-5(신규)로 구분했다.
- 요구사항 승인·계획 감사·템플릿·용어 변경은 스펙과 일치하며 지정 검증과 전체 테스트 595건도 통과했다.
- `issue-work --response`에서 F-4·F-5를 R6의 적합성 발견으로 먼저 검토해 처리 방향을 확정한다.

</details>

## 요약

- 1단계 적합성: 🟡 부분 충족(PARTIAL) · 충족(PASS) 22건 / 미충족(FAIL) 0건 / 부분 충족(PARTIAL) 1건 / 판정 불가(N/A) 0건
- 2단계 위험도: 🟡 주의(WARN) · 높음(HIGH) 0건 / 중간(MEDIUM) 2건 / 낮음(LOW) 0건 / 정보(INFO) 0건
- 기등재 참조: 1건 (K-0009, 위험도·카테고리 집계 제외)
- 이전 발견: 닫힘 2건 / 잔여 1건 (F-1·F-3 닫힘, F-2 → F-4)
- 신규 발견: 1건 (F-5, 보정 커밋에서 유입된 회귀)

| 카테고리 | 건수 |
|----------|------|
| 기능 | 0 |
| 아키텍처 | 0 |
| 버그 | 중간(MEDIUM) 2 |
| 보안 | 0 |
| 코드품질 | 0 |
| 성능 | 0 |
| 테스트 | 0 |

---

## 1단계: 적합성 검증 (Compliance Check)

### 요구사항 대조

| # | 요구사항 | 판정 | 근거 |
|---|----------|------|------|
| R1 | 요구사항 승인 후 DoD·plan·summary 작성 | 🟢 충족(PASS) | `issue-work/SKILL.md:51`의 3-1·3-2가 작성 순서, 경로·요약 제시, 이슈 본문 근거 없는 항목의 별도 질의를 규정한다. |
| R2 | 계획 종료 게이트 뒤 계획 감사 여부 질의 | 🟢 충족(PASS) | 같은 파일 58행부터 기본값 없는 선택, 타벤더 수동 감사, `--response` 후 Task 0, 생략 기록을 규정한다. |
| R3 | 계획 리포트 탐색·승인분 보정·별도 건수 기록 | 🟢 충족(PASS) | 같은 파일 159·166행에 리포트 선택, spec·plan 영향 파일 명시, 승인분 반영, summary `계획 감사` 줄 기록이 있다. |
| R4 | spec·summary·workflow 템플릿 반영 | 🟢 충족(PASS) | 근거 표기 주석·모델 행·계획 감사 줄·Task 0 전 검토 시점이 있다. 생략·수행 fixture 모두 지표 헬퍼를 통과한다. |
| R5 | `issue-audit --plan` 모드와 리포트 규약 | 🟢 충족(PASS) | `issue-audit/SKILL.md:407`부터 대상·대조 기준·1단계 매핑·5관점·공통 규칙·회차 보존·번호 축 분리를 규정하고 템플릿 주석도 일치한다. |
| R6 | R·DoD·Task 추적성 위반 4종 탐지 | 🟡 부분 충족(PARTIAL) | 기존 지정 반례는 보정됐다. 연속 주석 속 연결을 인정하는 잔여 문제(F-4)와 정상 본문을 누락으로 판정하는 보정 후 회귀(F-5)가 있다. |
| R7 | 안내도에 헬퍼·계획 감사 반영 | 🟢 충족(PASS) | `.ai/AI-CONTEXT.md`의 scripts 행·스킬 목록·워크플로우 표가 실제 변경을 가리킨다. |
| R8 | 계획·최종 감사 용어 통일 | 🟢 충족(PASS) | 지정 범위의 옛 표기 0건, 모델 행 4개의 순서·개수와 게이트 헤더가 일치한다. |

### 완료의 정의(DoD) 대조

| # | DoD 항목 | 판정 | 근거 |
|---|----------|------|------|
| D1 (R1) | 승인 게이트 앵커·작성 순서 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 1, 위반 출력 0건. |
| D2 (R1) | `--clear`와 승인 요청 제시 방식 동등 `[QD]` | 🟢 충족(PASS) | 두 절차 모두 파일을 승인 대상으로 삼고 경로·요약만 제시하며 전문을 재출력하지 않는다. |
| D3 (R2) | 계획 감사 질의 순서·앵커 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 2, 위반 출력 0건. |
| D4 (R3) | `--response` 5단계 유지·탐색·기록 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 3, 위반 출력 0건·러너 통과. |
| D5 (R4) | 템플릿 안내·행 개수·구조 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 4, 위반 출력 0건·러너 통과. |
| D6 (R4) | 생략·수행 값의 지표 집계 호환 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 5, 두 fixture 모두 종료 0. |
| D7 (R5) | `--plan` 소절·앵커·템플릿 주석 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 6, 위반 출력 0건. |
| D8 (R5) | 판정 매핑·5관점 정합·회귀 방지 예외 `[QD]` | 🟢 충족(PASS) | 모드별 대상·표 행을 구분하고 위험도·판정을 공유한다. 회귀 방지 항목의 표시 방법과 수동 판별 한계를 서술했다. |
| D9 (R6) | 정상·반례 4종·사용오류 fixture `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 7, 지정 케이스·출력·종료 코드 검사 통과. F-4·F-5는 이 fixture 집합 바깥의 반례다. |
| D10 (R6) | #100 spec·plan 자체 추적성 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 8, 무출력·종료 0. |
| D11 (R7) | 안내도 설명 행 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 9, 위반 출력 0건. |
| D12 (R8) | 옛 표기 0건 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 10, 일치 출력 0건. |
| D13 (R8) | 새 모델 행·게이트·모드 표기 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 11, 위반 출력 0건. |
| D14 (공통) | 전체 스킬 테스트 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 12와 전체 러너 실행 모두 통과. 10개 러너 595건, 실패 0건. |
| D15 (공통) | 변경 md의 줄 끝 공백 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 13, 위반 출력 0건. `git diff --check main`도 종료 0. |

- 집계 단위는 요구사항 8개와 스펙 DoD 15개다. plan의 고유 검증·Task N 선행 조건은 검증 기록에 별도로 남겼다.
- D12·D15는 검색 불일치로 종료 1이지만 stdout·stderr가 모두 비었다. 스펙의 “출력 0건” 기준으로 판정했다.

### 경계 검증

- **제외 목록 침범**: 없음. 새 스킬·README 변경·archive 소급 치환·위험도 표 변경·계획 감사 자동 실행·지표 집계 편입이 없다.
- **추가 구현**: 공집합 진단은 입력 오지정에 따른 거짓 통과를 막는 보강이다. K-0009 등재와 F-1~F-3 보정은 spec·summary에 승인 근거가 있다.
- **판정 범위**: F-4는 R6의 연결 누락 탐지, F-5는 정상 입력의 무출력·종료 0 보장에 결속한다. 지정 fixture와 실제 #100 문서 자체는 통과하므로 D9·D10의 판정은 유지한다.

### 도메인/계약/ADR 정합성

- 계약·도메인 index와 용어 사전에 이번 변경을 제한하는 개별 명세가 없다. 코드베이스 index에도 기능 색인이 없어 갱신할 항목이 없다.
- 결정적 검사는 `scripts/`, 조율·판단은 `SKILL.md`, 회귀 검증은 같은 스킬의 `tests/`에 있어 아키텍처 원칙·ADR 0001과 일치한다.
- 가짜 `[D]` 판별의 수동 절차는 승인된 제외 범위다. K-0009의 수용 사유·재검토 조건이 유지된다.

---

## 2단계: 비판적 검증 (Critical Review)

### 발견 사항

| # | 위험도 | 영향 | 발생확률 | 카테고리 | 계보 | 설명 | 관련 파일 |
|---|--------|------|----------|----------|------|------|-----------|
| F-4 | 🟡 중간(MEDIUM) | 스펙·기능 달성 차단 | 특수 조건·엣지 | 버그 | F-2 잔여 | 최초 구현부터 있던 주석 속 연결 오집계가 연속 주석에서 남아 있다. | [check-plan.sh](../../../../issue-audit/scripts/check-plan.sh) 75행 |
| F-5 | 🟡 중간(MEDIUM) | 스펙·기능 달성 차단 | 특수 조건·엣지 | 버그 | 신규 (보정 중 유입된 회귀) | 보정 커밋 `8a36ee7`에서 주석 앞 정상 필드와 인라인 코드 구분자 뒤 본문을 누락으로 판정하게 됐다. | [check-plan.sh](../../../../issue-audit/scripts/check-plan.sh) 65~66·78행 |

### 상세 분석

#### F-4: 연속 주석 속 연결 오집계가 최초 구현부터 남아 있다

- **위험도**: 🟡 중간(MEDIUM)
- **영향 축**: 스펙·기능 달성 차단
- **발생확률 축**: 특수 조건·엣지
- **카테고리**: 버그
- **관점**: 엣지케이스·암묵적 가정·누락된 검증
- **계보**: F-2 잔여. 주석 속 예시를 실제 구조로 집계하지 말라는 1차 완료 기준의 미해소 부분이다. 최초 구현 `2236680`과 보정 커밋 `8a36ee7` 모두에서 같은 세 반례가 재현됐다.
- **스펙 결속**: R6의 `DoD 없는 R`·`Task 없는 R`·`대상 요구사항 없는 일반 Task`·`R 없는 DoD 그룹` 판정.
- **권장 조치**: 주석 종료 뒤 같은 행의 나머지를 계속 처리해 두 번째 주석의 시작을 반영한다.
- **기본 처리**: `--response` 승인 게이트에서 사용자 판단. R6 부분 충족과 같은 결함이므로 `--response`에서는 1단계 발견으로 병합해 먼저 검토한다.
- **완료 기준**:
  - [ ] `--> <!--`로 이어진 두 번째 주석 안에만 DoD 그룹·Task 필드를 남기면 각각 `DoD 없는 R`, `Task 없는 R`·Task별 필드 누락을 출력하고 종료 1을 낸다.
  - [ ] 같은 두 번째 주석 안의 `R99` 그룹은 무시하고, 주석 밖 실제 `R99` 그룹은 계속 위반으로 출력한다.
  - [ ] 연속 주석 반례와 실제 본문 대조군을 러너에 추가하고 F-2의 기존 정상 사례와 실제 #100 spec·plan이 계속 기대 결과를 낸다.

<details>
<summary>근거·영향 펼치기</summary>

- 75행은 주석 상태에서 `-->`를 만나면 상태를 끄고 즉시 `next`한다. 그 뒤의 `<!--`를 처리하지 않아 다음 행부터 두 번째 주석의 내용을 실제 앵커로 읽는다.
- 실제 #100 문서 사본에서 두 번째 주석에만 둔 DoD·필드는 각각 무출력·종료 0이었다. 같은 위치의 숨겨진 `R99`는 반대로 유령 위반을 만들었다.
- 최초 구현은 주석을 전혀 제외하지 않아 같은 오판정을 냈다. 보정은 기본 주석을 처리하지만 이 형태는 놓치므로 기존 오류의 잔여다.
- 같은 행에서 주석을 닫고 다시 여는 조건에서 실제 연결 누락을 통과시킨다. 영향은 스펙·기능 달성 차단, 발생확률은 특수 조건으로 판정했다.

</details>

#### F-5: 주석 제외 보정이 정상 필드·인라인 코드 설명을 누락으로 판정한다

- **위험도**: 🟡 중간(MEDIUM)
- **영향 축**: 스펙·기능 달성 차단
- **발생확률 축**: 특수 조건·엣지
- **카테고리**: 버그
- **관점**: 부작용·엣지케이스·누락된 검증
- **계보**: 신규. F-1~F-3 보정 커밋 `8a36ee7`에서 유입된 회귀다. 최초 구현 `2236680`에서는 두 입력 모두 정상 통과했으며, 이전 발견이 닫혔다가 재등장한 경우가 아니므로 `F-n 재발`로 표기하지 않는다.
- **스펙 결속**: R6의 정상 입력 무출력·종료 코드 0 보장.
- **권장 조치**: 여러 줄 주석이 행 중간에서 시작해도 앞의 실제 본문을 보존한다. 인라인 코드에 적힌 주석 구분자는 실제 주석 시작과 구분한다.
- **기본 처리**: `--response` 승인 게이트에서 사용자 판단. R6 부분 충족에 결속하므로 1단계 발견으로 먼저 검토한다.
- **완료 기준**:
  - [ ] 유효한 R 필드 뒤에 여러 줄 주석을 붙여도 주석 앞의 R 참조를 유지해 정상 문서가 무출력·종료 0을 낸다.
  - [ ] 일반 설명의 인라인 코드 `` `<!--` ``가 뒤의 실제 R·DoD·Task를 숨기지 않아 정상 문서가 무출력·종료 0을 낸다.
  - [ ] 두 회귀 반례와 정상 대조군을 러너에 추가하고 F-1·F-3의 닫힌 사례와 실제 #100 spec·plan이 계속 기대 결과를 낸다.

<details>
<summary>근거·영향 펼치기</summary>

- 78행은 행 중간에서 닫히지 않은 주석이 시작돼도 행 전체를 버린다. `- **대상 요구사항**: R1, R2 <!--`의 유효한 접두까지 사라져 정상 Task에 누락을 출력한다.
- 65~66행은 인라인 코드 범위를 구분하지 않는다. `` `<!--`는 검사할 구분자다. ``라는 설명도 주석을 열어 이후의 실제 필드·Task를 건너뛴다.
- 같은 #100 문서 사본으로 두 커밋을 비교했다. 최초 구현에서는 두 사례 모두 무출력·종료 0, 보정 뒤에는 Task·R 누락 출력·종료 1이었다.
- 주석·인라인 코드가 포함된 특정 정상 문서에서 통과 보장이 깨진다. 영향은 스펙·기능 달성 차단, 발생확률은 특수 조건으로 판정했다.
- 두 사례는 보정에서 추가한 `unclosed`와 행 전체 제외 경로의 회귀로 묶었다. F-4와는 발생 시점·실패 방향·완료 기준을 구분하며 테스트 누락을 별도로 집계하지 않는다.

</details>

### 기등재 참조 항목

<details>
<summary>기등재 참조 표 펼치기</summary>

| 원장 항목 | 상태 | 대조한 발견 내용 | 재검토 조건 충족 | 처리 |
|-----------|------|------------------|------------------|------|
| [K-0009](../../../70_ledger/active/K-0009-plan-audit-fake-d-check-manual.md) | 수용 | 가짜 `[D]` 사전 판별이 수동 절차로 남아 있다. | 미충족. 계획 감사에서 모델 간 판별 불일치 2회 기록이나 템플릿의 기계 판독용 예외 표기 도입이 없다. | 기등재 K-0009 참조. 신규 번호·집계에서 제외한다. |

- K-0002·K-0004도 대조했다. 위험도 축·매트릭스와 `--clear` 보존 목적지 규정에 실질 변경이 없어 재제기하지 않는다.
- 이번 발견과 일치하는 archive 원장 항목은 index에 없다.

</details>

### 이전 발견 추적

<details>
<summary>이전 발견 추적 표 펼치기</summary>

| 이전 발견 | 완료 기준 항목 | 판정 (닫힘/잔여) | 근거 |
|-----------|----------------|------------------|------|
| F-1 | 다른 Task가 모든 R을 참조해도 빈 값·공백·`-`를 누락 처리 | 닫힘 | 실제 plan에 일반 Task 8을 추가한 세 반례 모두 Task별 누락·종료 1. 러너에도 세 fixture가 있다. |
| F-1 | 라벨 누락과 `R1`·`R1, R2` 정상 입력 회귀 검증 | 닫힘 | 라벨 누락은 Task별 누락·종료 1, 유효 값 두 형태는 무출력·종료 0. |
| F-2 | DoD를 전제·코드 예시·주석에만 남겨도 누락 탐지 | 잔여 | 전제·기본/긴/물결표 펜스·독립 블록 주석은 정상. 같은 행에서 다시 연 두 번째 주석의 DoD는 누락을 놓친다(F-4). |
| F-2 | 실제 필드를 없애고 예시·주석에만 남겨도 Task·R 누락 탐지 | 잔여 | 기존 형태는 정상. 연속 주석의 두 번째 주석 속 필드는 실제 연결로 센다(F-4). |
| F-2 | 예시·주석의 R99는 무시하고 실제 유령 그룹은 탐지 | 잔여 | 기본/긴/물결표 펜스·독립 주석 속 R99는 무시하고 실제 그룹은 탐지한다. 연속 주석 속 R99는 유령 위반을 만든다(F-4). |
| F-3 | 한 줄 주석만 있는 필드는 누락, 실제 R 뒤 짧은 주석은 유지 | 닫힘 | `<!-- R1, R2 -->`는 Task·R 누락·종료 1, `R1, R2 <!-- 메모 -->`는 무출력·종료 0. |
| F-3 | 백틱 4개 안의 백틱 3개·DoD·필드·R99를 구분 | 닫힘 | DoD·필드 누락은 종료 1, 숨겨진 R99는 종료 0, 실제 R99는 종료 1. |
| F-3 | 물결표 펜스 속 필드는 Task·R 누락으로 판정 | 닫힘 | 실제 plan 사본에서 Task 2·R1·R2 누락을 함께 출력하고 종료 1. |
| F-3 | 닫힌 주석 안의 홀수 펜스 행이 이후 본문에 영향 없음 | 닫힘 | 주석 안 펜스 행 1개를 spec 앞에 추가해도 무출력·종료 0. 반대 방향인 펜스 속 주석 시작도 정상. |
| F-3 | 명시 반례·정상 대조군 러너 추가, 기존 회귀·실제 문서 통과 | 닫힘 | 러너에 F-3 사례 10건이 추가됐고 179건 전부 통과. 실제 #100 spec·plan과 과거 재현 사례도 기대 결과 충족. |

- 보존된 두 리포트의 모든 완료 기준 10개를 대조했다. F-3에서 지정한 보정의 완료와 F-2의 더 넓은 주석 제외 기준에 남은 오류를 구분했다.
- F-5의 두 회귀 사례는 이전 완료 기준의 잔여로 세지 않는다. 보정 커밋에서 새로 생긴 결함이며 F-1·F-3의 닫힘 판정은 유지한다.
- 최초 작성 시 `next-finding-number.sh`에 1·2차 리포트를 넘겨 번호 `4`를 얻었다. 이번 정정에서는 기존 3차 리포트까지 넘겨 번호 `5`를 얻었고, 기존 잔여는 F-4, 보정 중 유입된 회귀는 신규 F-5로 기록했다.

</details>

---

## 검증 기록

- 지정 검증과 반례는 작업 트리 또는 임시 사본에서 실행했다. 감사 산출물은 이 리포트와 보존한 2차 리포트다.
- summary의 최종 audit 모델은 `OpenAI, GPT-6`이다. Task N 결과 기록은 F-4·F-5 처리 검토 후 확정할 후속 작업이다.
- 이번 정정은 발생 시점·계보·집계를 수정한 것이다. 전체 테스트 595건은 3차 감사 때의 실행 결과이며, 정정 시에는 아래 커밋별 반례 비교와 리포트 정합성을 확인했다.

<details>
<summary>명령 실행·교차모델 확인 내역 펼치기</summary>

| 검증 | 결과 |
|------|------|
| 스펙 DoD bash 블록 13개 | 모두 스펙의 출력 기준 충족. stdout·stderr 0건. 10·13번은 검색 불일치 종료 1. |
| plan 고유 bash 블록 8개 | 1~6번 무출력, 7~8번 `0` 출력. 전부 종료 0. |
| 전체 스킬 러너 | ai-workspace 33, ai-workspace-directory 29, git-commit 44, git-pr 59, git-pr-feedback 24, git-review 54, git-review-quiz 59, install-skills 20, issue-audit 179, issue-work 94. 합계 595, 실패 0. |
| 이전 발견 반례·대조군 | 실제 #100 문서 사본의 기존 형태 24건이 기대 결과를 냈다. F-4·F-5는 아래 별도 반례로 확인했다. |
| 발생 시점 검증 | 같은 반례·대조군 7개를 `2236680`·`8a36ee7`에서 각각 실행했다. F-4 세 사례는 최초 구현부터 동일 오판정, F-5 두 사례는 보정 전 통과·보정 후 실패다. |
| workflow 사본 비교 | 템플릿과 동일. |
| 변경 공백 확인 | `git diff --check main` 종료 0. |
| Task N 선행 조건 | plan·summary의 Task 헤더 집합 일치, Task 0~7의 결과·수행 모델 게이트 통과. |
| 감사 독립성 | 계획·구현 대표값과 Task 0~7 수행 모델은 Anthropic이다. 이번 감사는 OpenAI로 기록상 벤더가 다르다. |
| 상태·판정 산출 | 정정 시 설치 스킬의 `classify-risk.sh`에 요구사항·DoD 판정 23개와 발견 위험도 2개를 넘겼다. 조건부 적합 판정은 유지된다. |
| F-4·F-5 위험도 산출 | 두 발견 모두 `--impact '스펙·기능 달성 차단' --likelihood '특수 조건·엣지'`와 `--treatment` 출력을 적용했다. |
| 이력 보존 | 2차 리포트를 `issue-0100-audit-report-2.md`로 이동하고 SHA-256 동일성을 확인했다. |

</details>

<details>
<summary>F-4·F-5 커밋별 재현 명령과 결과 (repo 루트에서 실행)</summary>

````bash
python3 - <<'PY'
from pathlib import Path
from tempfile import TemporaryDirectory
import subprocess

root = Path('.ai/90_issues/archive/issue-0100')
spec = (root / 'issue-0100-spec.md').read_text()
plan = (root / 'issue-0100-plan.md').read_text()
group = '### R1: 요구사항 승인 게이트'
field = '- **대상 요구사항**: R1, R2'
assert spec.count(group) == 1 and plan.count(field) == 1

def comments(value):
    return '<!--\n앞 메모\n--> <!--\n' + value + '\n-->'

cases = [
    ('comment-reopen-field', spec, plan.replace(field, comments(field))),
    ('comment-reopen-dod', spec.replace(group, comments(group)), plan),
    ('comment-reopen-phantom', spec.replace('### 공통',
        comments('### R99: 폐기된 대안') + '\n\n### 공통', 1), plan),
    ('visible-before-multiline', spec,
        plan.replace(field, field + ' <!--\n다음 줄 설명\n-->')),
    ('inline-code-marker', spec,
        plan.replace(field, '`<!--`는 검사할 구분자다.\n\n' + field)),
    ('inline-code-balanced', spec,
        plan.replace(field, '`<!--`와 `-->`는 검사할 구분자다.\n\n' + field)),
    ('baseline', spec, plan),
]
with TemporaryDirectory(prefix='issue-0100-audit3-origin-') as tmp:
    a, b = Path(tmp) / 'spec.md', Path(tmp) / 'plan.md'
    helper = Path(tmp) / 'check-plan.sh'
    for revision in ('2236680', '8a36ee7'):
        helper.write_text(subprocess.check_output(
            ['git', 'show', revision + ':issue-audit/scripts/check-plan.sh'], text=True))
        for label, s, p in cases:
            a.write_text(s)
            b.write_text(p)
            result = subprocess.run(
                ['bash', str(helper), '--trace', str(a), str(b)],
                capture_output=True, text=True)
            print(revision, label, 'exit=' + str(result.returncode),
                  'stdout=' + repr(result.stdout))
PY
````

| 사례 | 기대 결과 | 최초 구현 `2236680` | 보정 후 `8a36ee7` | 귀속 |
|------|-----------|--------------------|-------------------|------|
| comment-reopen-field | R1·R2 Task 누락과 Task 2 필드 누락, 종료 1 | 무출력, 종료 0 | 무출력, 종료 0 | F-4: F-2 잔여 |
| comment-reopen-dod | `DoD 없는 R: R1`, 종료 1 | 무출력, 종료 0 | 무출력, 종료 0 | F-4: F-2 잔여 |
| comment-reopen-phantom | 무출력, 종료 0 | `R 없는 DoD 그룹: R99`, 종료 1 | 같은 위반, 종료 1 | F-4: F-2 잔여 |
| visible-before-multiline | 무출력, 종료 0 | 무출력, 종료 0 | R1·R2 Task 누락과 Task 2 필드 누락, 종료 1 | F-5: 보정 중 유입된 회귀 |
| inline-code-marker | 무출력, 종료 0 | 무출력, 종료 0 | R1~R7 Task 누락과 Task 2 필드 누락, 종료 1 | F-5: 보정 중 유입된 회귀 |
| inline-code-balanced | 무출력, 종료 0 | 무출력, 종료 0 | 무출력, 종료 0 | 정상 대조군 |
| baseline | 무출력, 종료 0 | 무출력, 종료 0 | 무출력, 종료 0 | 정상 대조군 |

</details>
