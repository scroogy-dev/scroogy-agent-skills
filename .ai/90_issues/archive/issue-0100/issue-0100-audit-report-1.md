# Issue #100 감사 리포트 요구사항 승인 게이트·계획 감사 모드·추적성 헬퍼

> 감사 일시: 2026-09-09
> 감사 모델: OpenAI, GPT-6
> 감사 회차: 1차 (이전 리포트: 없음)
> 감사 모드: 최종 감사
> 감사 대상 브랜치: `issue-0100` (`7b52584`)
> 비교 기준: `main` (`a16fba1`)부터 HEAD까지의 18개 변경 파일
> 스펙 출처: [issue-0100-spec.md](./issue-0100-spec.md)

---

## 종합 의견

🟡 조건부 적합(CONDITIONAL)

<details>
<summary>종합 의견 펼치기</summary>

- 승인 절차·계획 감사 모드·템플릿·용어 변경은 스펙과 일치하고, 명시된 검증 명령과 전체 테스트가 통과했다.
- 추적성 헬퍼는 빈 대상 필드와 문서 예시·주석을 구분하지 못해 실제 연결 누락을 통과시키므로 R6은 부분 충족이다.
- `issue-work --response`에서 F-1·F-2를 R6의 적합성 발견으로 먼저 검토하고 처리 방향을 확정해야 한다.

</details>

## 요약

- 1단계 적합성: 🟡 부분 충족(PARTIAL) · 충족(PASS) 22건 / 미충족(FAIL) 0건 / 부분 충족(PARTIAL) 1건 / 판정 불가(N/A) 0건
- 2단계 위험도: 🟡 주의(WARN) · 높음(HIGH) 0건 / 중간(MEDIUM) 2건 / 낮음(LOW) 0건 / 정보(INFO) 0건
- 기등재 참조: 1건 (K-0009, 위험도·카테고리 집계 제외)
- 이전 발견: 해당 없음
- 검증: 스펙 `[D]` 13개·plan 검증 블록 8개 통과, 전체 테스트 10개 러너 575건 통과, 추가 반례에서 결함 2종 재현.

| 카테고리 | 건수 |
|----------|------|
| 기능 | 0 |
| 아키텍처 | 0 |
| 버그 | 중간(MEDIUM) 2 |
| 보안 | 0 |
| 코드품질 | 0 |
| 성능 | 0 |
| 테스트 | 0 |

---

## 1단계: 적합성 검증 (Compliance Check)

### 요구사항 대조

| # | 요구사항 | 판정 | 근거 |
|---|----------|------|------|
| R1 | 요구사항 승인 후 DoD·plan·summary 작성 | 🟢 충족(PASS) | `issue-work/SKILL.md:51`의 3-1·3-2가 작성 순서, 파일 경로와 요약 제시, 이슈 본문 근거 없는 항목의 별도 질의를 규정한다. |
| R2 | 계획 종료 게이트 뒤 계획 감사 여부 질의 | 🟢 충족(PASS) | 같은 파일 58행의 5단계에 기본값 없는 선택, 타벤더 수동 감사, `--response` 후 Task 0, 생략 기록이 있다. |
| R3 | 계획 리포트 탐색·승인분 보정·별도 건수 기록 | 🟢 충족(PASS) | 같은 파일 159·165행에 두 리포트 선택, spec·plan 영향 파일, 승인분 반영, summary `계획 감사` 줄 기록이 있다. |
| R4 | spec·summary·workflow 템플릿 반영 | 🟢 충족(PASS) | 근거 표기 주석·모델 행·계획 감사 줄·Task 0 전 검토 시점이 존재한다. 생략·수행 fixture 모두 지표 헬퍼를 통과한다. |
| R5 | `issue-audit --plan` 모드와 리포트 규약 | 🟢 충족(PASS) | `issue-audit/SKILL.md:407`에 입력·대조 기준·1단계 매핑·5관점·공통 판정·회차 보존·번호 축 분리가 있고 템플릿 주석도 일치한다. |
| R6 | R·DoD·Task 추적성 위반 4종 탐지 | 🟡 부분 충족(PARTIAL) | 명시 fixture와 실제 #100 문서는 통과한다. 빈 대상 필드와 예시·주석 때문에 연결 누락을 놓치는 반례가 재현됐다(F-1·F-2). |
| R7 | 안내도에 헬퍼·계획 감사 반영 | 🟢 충족(PASS) | `.ai/AI-CONTEXT.md`의 scripts 행·스킬 목록·워크플로우 표가 실제 변경을 가리킨다. |
| R8 | 계획·최종 감사 용어 통일 | 🟢 충족(PASS) | 지정 범위에서 옛 표기 0건, 모델 행 4개 순서·개수와 게이트 헤더가 일치한다. |

### 완료의 정의(DoD) 대조

| # | DoD 항목 | 판정 | 근거 |
|---|----------|------|------|
| D1 (R1) | 승인 게이트 앵커·작성 순서 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 1, 위반 출력 0건. |
| D2 (R1) | `--clear`와 승인 요청 제시 방식 동등 `[QD]` | 🟢 충족(PASS) | 두 절차 모두 파일을 승인 대상으로 삼고 경로·요약만 제시하며 전문을 재출력하지 않는다. |
| D3 (R2) | 계획 감사 질의 순서·앵커 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 2, 위반 출력 0건. |
| D4 (R3) | `--response` 5단계 유지·탐색·기록 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 3, 위반 출력 0건·러너 통과. |
| D5 (R4) | 템플릿 안내·행 개수·구조 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 4, 위반 출력 0건·러너 통과. |
| D6 (R4) | 생략·수행 값의 지표 집계 호환 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 5, 두 fixture 모두 종료 0. |
| D7 (R5) | `--plan` 소절·앵커·템플릿 주석 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 6, 위반 출력 0건. |
| D8 (R5) | 판정 매핑·5관점 정합·회귀 방지 예외 `[QD]` | 🟢 충족(PASS) | 기본 절차의 대상·표 행을 모드별로 치환하고 위험도·판정은 공유한다. 회귀 방지 항목의 명시적 예외 표기와 수동 판별 한계를 서술했다. |
| D9 (R6) | 정상·반례 4종·사용오류 fixture `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 7, 지정 케이스·출력·종료 코드 검사 통과. F-1·F-2는 이 fixture 집합 바깥의 반례다. |
| D10 (R6) | #100 spec·plan 자체 추적성 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 8, 무출력·종료 0. |
| D11 (R7) | 안내도 설명 행 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 9, 위반 출력 0건. |
| D12 (R8) | 옛 표기 0건 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 10, 일치 출력 0건. |
| D13 (R8) | 새 모델 행·게이트·모드 표기 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 11, 위반 출력 0건. |
| D14 (공통) | 전체 스킬 테스트 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 12, 10개 러너 모두 종료 0. |
| D15 (공통) | 변경 md의 줄 끝 공백 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 13, 위반 출력 0건. `git diff --check main...HEAD`도 종료 0. |

- 집계 단위는 요구사항 8개와 스펙 DoD 15개다. plan의 고유 검증은 아래 검증 기록에 별도로 남겼다.
- D12·D15는 스펙이 정한 판정 기준이 “출력 0건”이다. `grep` 불일치로 종료 1이지만 stderr가 없고 위반도 없어 통과로 판정했다.

### 경계 검증

- **제외 목록 침범**: 없음. 새 스킬·README 변경·archive 소급 치환·위험도 표 변경·계획 감사 자동 실행·지표 집계 편입을 하지 않았다.
- **추가 구현**: 헬퍼의 공집합 진단 2종은 입력을 잘못 지정했을 때 거짓 통과를 막는 보강이며 기존 종료 코드 체계 안에 있다. K-0009 등재는 스펙 전제·Task 7에 명시된 작업이다.
- **판정 범위**: F-1·F-2는 R6의 연결 누락 탐지에 결속했다. 지정 fixture 자체가 실패한 것으로 바꾸어 D9·D10을 낮추지는 않았다.

### 도메인/계약/ADR 정합성

- 계약·도메인 index에 이번 변경을 제한하는 개별 명세는 없다. 코드베이스 index도 비어 있어 별도 갱신 의견은 없다.
- 결정적 추적성 검사는 `scripts/`, 조율·판단은 `SKILL.md`, 회귀 검증은 동일 스킬의 `tests/`에 있어 아키텍처 원칙·ADR 0001과 일치한다.
- 가짜 `[D]` 판별의 수동 절차는 승인된 제외 범위이고 K-0009에 수용 사유·재검토 조건이 기록되어 있다.

---

## 2단계: 비판적 검증 (Critical Review)

### 발견 사항

| # | 위험도 | 영향 | 발생확률 | 카테고리 | 계보 | 설명 | 관련 파일 |
|---|--------|------|----------|----------|------|------|-----------|
| F-1 | 🟡 중간(MEDIUM) | 스펙·기능 달성 차단 | 특수 조건·엣지 | 버그 | 신규 | 대상 필드의 값이 비어 있어도 일반 Task를 연결된 것으로 처리한다. | [check-plan.sh](../../../../issue-audit/scripts/check-plan.sh) 66~71행 |
| F-2 | 🟡 중간(MEDIUM) | 스펙·기능 달성 차단 | 특수 조건·엣지 | 버그 | 신규 | 다른 섹션·코드 예시·주석의 앵커를 실제 추적성 구조로 집계한다. | [check-plan.sh](../../../../issue-audit/scripts/check-plan.sh) 56~74행 |

### 상세 분석

#### F-1: 대상 요구사항을 비운 일반 Task가 통과한다

- **위험도**: 🟡 중간(MEDIUM)
- **영향 축**: 스펙·기능 달성 차단
- **발생확률 축**: 특수 조건·엣지
- **카테고리**: 버그
- **관점**: 엣지케이스·누락된 검증
- **계보**: 신규
- **스펙 결속**: R6의 “대상 요구사항 없는 일반 Task” 탐지.
- **권장 조치**: 필드 라벨 존재와 유효한 R 참조 존재를 구분하고, 값이 빈 필드를 연결로 인정하지 않는다.
- **완료 기준**:
  - [ ] 다른 Task가 모든 R을 참조하더라도 별도 일반 Task의 빈 값·공백 값·`-` 값을 누락으로 출력하고 종료 1을 낸다.
  - [ ] 라벨 전체 누락 반례와 유효한 `R1`·`R1, R2` 정상 입력도 함께 회귀 검증한다.

<details>
<summary>근거·상세 설명 펼치기</summary>

- 69행에서 필드 값을 읽기 전에 `c++`하고, 66행은 `c == 0`만 검사한다. 값에 R이 하나도 없어도 필드가 존재하면 해당 Task의 누락 진단이 사라진다.
- 실제 #100 plan의 Task N 앞에 Task 8과 `- **대상 요구사항**:`만 추가하면 무출력·종료 0이다. 값이 `-`여도 같으며, 라벨 자체를 없애면 종료 1로 탐지한다.
- 기존 Task가 R1~R8을 이미 모두 참조하므로 `Task 없는 R` 검사도 이를 보완하지 못한다. 기존 러너 676행의 반례는 필드 행 전체를 삭제해 이 차이를 검사하지 않는다.
- 특수 조건은 미완성 필드가 남고 다른 Task가 R을 모두 담당하는 조합이다. 이 조합에서 헬퍼가 보장하는 Task별 연결 누락 탐지가 무너져 영향 축은 기능 차단으로 판정했다.

</details>

#### F-2: 문서 예시·주석·다른 섹션의 앵커가 실제 연결을 대신한다

- **위험도**: 🟡 중간(MEDIUM)
- **영향 축**: 스펙·기능 달성 차단
- **발생확률 축**: 특수 조건·엣지
- **카테고리**: 버그
- **관점**: 암묵적 가정·엣지케이스·누락된 검증
- **계보**: 신규
- **스펙 결속**: R6의 “DoD 없는 R”·“Task 없는 R”·“대상 요구사항 없는 일반 Task” 탐지.
- **권장 조치**: 앵커를 해당 본문 섹션에서만 읽고, fenced code block과 HTML 주석 안의 예시를 집계에서 제외한다.
- **완료 기준**:
  - [ ] 실제 DoD 그룹을 없앤 뒤 같은 헤더를 전제 섹션·코드 예시·주석에 남겨도 `DoD 없는 R`과 종료 1이 나온다.
  - [ ] 일반 Task의 실제 필드를 없앤 뒤 같은 필드를 코드 예시·주석에만 남겨도 Task별 누락을 탐지하고, 다른 실제 참조가 없는 R은 `Task 없는 R`도 출력한다.
  - [ ] 정상 문서의 코드 예시·주석에 폐기한 `R99` 그룹을 넣어도 유령 DoD 위반을 만들지 않는다. 실제 본문의 유령 그룹은 계속 탐지한다.

<details>
<summary>근거·상세 설명 펼치기</summary>

- spec 파서는 60행에서 문서 전체의 `### R<n>:`를 센다. plan 파서도 주석·코드 블록 상태 없이 67~71행의 헤더·필드를 읽는다.
- 실제 #100 spec의 R1 DoD 헤더를 삭제하고 문서 끝 코드 예시에만 남겨도 무출력·종료 0이다. 최소 fixture에서는 헤더를 `## 전제 (Assumptions)` 아래로 옮기거나 HTML 주석에만 남겼을 때도 통과했다.
- 실제 #100 plan의 Task 2 대상 필드를 HTML 주석으로 감춰도 R1·R2 참조가 유지된 것으로 계산해 무출력·종료 0이다. 코드 예시에만 필드를 둔 최소 fixture도 같다.
- 반대로 정상 spec 끝의 HTML 주석에 `### R99: 폐기된 대안`을 넣으면 `R 없는 DoD 그룹: R99`를 출력하고 종료 1이다. 예시가 실제 누락을 가리거나 거짓 발견을 만드는 두 방향의 오류가 있다.

</details>

### 기등재 참조 항목

<details>
<summary>기등재 참조 표 펼치기</summary>

| 원장 항목 | 상태 | 대조한 발견 내용 | 재검토 조건 충족 | 처리 |
|-----------|------|------------------|------------------|------|
| [K-0009](../../../70_ledger/active/K-0009-plan-audit-fake-d-check-manual.md) | 수용 | 가짜 `[D]` 사전 판별이 수동 절차로 남아 있다. | 미충족. 모델 간 판별 불일치 2회나 spec·plan 템플릿의 예외 표기 도입이 확인되지 않았다. | 기등재 K-0009 참조. 신규 번호·집계에서 제외한다. |

- K-0002·K-0004도 관련 원장으로 대조했다. 위험도 축·매트릭스와 `--clear` 보존 목적지 규정의 실질 변경이 없어 재제기하지 않았다.

</details>

### 이전 발견 추적

해당 없음. workspace·이슈 경로에서 이전 #0100 감사 리포트가 없었고, `next-finding-number.sh`가 첫 번호 `1`을 반환했다.

---

## 검증 기록

- 추가 반례는 임시 디렉토리의 사본에서 실행했다. 감사 산출물은 이 리포트이며, 구현·spec·plan·summary의 내용을 보정하지 않았다.
- Task N 완료 기록은 별도 후속 작업이다. summary의 최종 audit 모델 행·Task N 결과는 아직 미기록이며, F-1·F-2 처리 검토와 함께 갱신해야 한다.

<details>
<summary>명령 실행·교차모델 확인 내역 펼치기</summary>

| 검증 | 결과 |
|------|------|
| 스펙 DoD의 bash 블록 13개 | 전부 스펙의 출력 기준 충족. stderr 0건. |
| plan 고유 bash 블록 8개 | 1~6번은 무출력, 7~8번은 `0` 출력. 전부 종료 0. |
| 전체 스킬 러너 | ai-workspace 33, ai-workspace-directory 29, git-commit 44, git-pr 59, git-pr-feedback 24, git-review 54, git-review-quiz 59, install-skills 20, issue-audit 159, issue-work 94. 합계 575, 실패 0. |
| workflow 사본 비교 | 템플릿과 동일. |
| 변경 공백 확인 | `git diff --check main...HEAD` 종료 0. |
| 감사 독립성 | summary의 계획·구현 대표값과 Task 0~7 수행 모델은 모두 Anthropic이다. 이번 감사는 OpenAI이며 벤더가 다르다. |
| 위험도·상태·판정 산출 | 실행 스킬의 `classify-risk.sh`에 항목 23개·위험도 2개를 넘겨 본문의 라벨을 얻었다. 두 발견은 `--impact '스펙·기능 달성 차단' --likelihood '특수 조건·엣지'`, 기본 처리는 `--response` 승인 게이트에서 사용자 판단이다. |

</details>

<details>
<summary>실제 #100 문서 사본으로 반례 재현 (repo 루트에서 실행)</summary>

```bash
python3 - <<'PY'
from pathlib import Path
from tempfile import TemporaryDirectory
import subprocess

root = Path('.ai/90_issues/archive/issue-0100')
spec = (root / 'issue-0100-spec.md').read_text()
plan = (root / 'issue-0100-plan.md').read_text()
group = '### R1: 요구사항 승인 게이트'
field = '- **대상 요구사항**: R1, R2'
assert spec.count(group) == 1 and plan.count(field) == 1
cases = [
    ('F1-empty', spec, plan.replace(
        '### Task N (고정):',
        '### Task 8: 연결 미정\n\n- **대상 요구사항**:\n\n### Task N (고정):', 1)),
    ('F1-dash', spec, plan.replace(
        '### Task N (고정):',
        '### Task 8: 연결 미정\n\n- **대상 요구사항**: -\n\n### Task N (고정):', 1)),
    ('F2-fenced-DoD', spec.replace(group, '', 1)
        + '\n```markdown\n' + group + '\n```\n', plan),
    ('F2-comment-field', spec,
        plan.replace(field, '<!--\n' + field + '\n-->', 1)),
    ('F2-comment-phantom', spec + '\n<!--\n### R99: 폐기된 대안\n-->\n', plan),
]
with TemporaryDirectory(prefix='issue-0100-audit-') as tmp:
    for label, s, p in cases:
        a, b = Path(tmp) / 'spec.md', Path(tmp) / 'plan.md'
        a.write_text(s)
        b.write_text(p)
        result = subprocess.run(
            ['issue-audit/scripts/check-plan.sh', '--trace', str(a), str(b)],
            capture_output=True, text=True)
        print(label, 'exit=' + str(result.returncode), 'stdout=' + repr(result.stdout))
PY
```

| 반례 | 기대 결과 | 실제 결과 |
|------|-----------|-----------|
| F1-empty | Task 8 누락, 종료 1 | 무출력, 종료 0 |
| F1-dash | Task 8 누락, 종료 1 | 무출력, 종료 0 |
| F2-fenced-DoD | `DoD 없는 R: R1`, 종료 1 | 무출력, 종료 0 |
| F2-comment-field | Task 2 필드 누락·R1/R2 Task 누락, 종료 1 | 무출력, 종료 0 |
| F2-comment-phantom | 무출력, 종료 0 | `R 없는 DoD 그룹: R99`, 종료 1 |

</details>
