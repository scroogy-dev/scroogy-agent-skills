# Issue #100 감사 리포트 요구사항 승인 게이트·계획 감사 모드·추적성 헬퍼

> 감사 일시: 2026-09-09
> 감사 모델: OpenAI, GPT-6
> 감사 회차: 2차 (이전 리포트: [1차 리포트](./issue-0100-audit-report-1.md))
> 감사 모드: 최종 감사
> 감사 대상 브랜치: `issue-0100` (`7b52584` + 미커밋 보정)
> 비교 기준: `main` (`a16fba1`) 대비 작업 트리의 18개 변경 파일. 1차 이후의 `check-plan.sh`·러너·summary 보정 포함
> 스펙 출처: [issue-0100-spec.md](./issue-0100-spec.md)

---

## 종합 의견

🟡 조건부 적합(CONDITIONAL)

<details>
<summary>종합 의견 펼치기</summary>

- F-1은 완료 기준을 충족해 닫았다. F-2는 기본 반례를 해결했으나 주석·코드 펜스의 다른 형태에서 같은 추적성 오류가 남아 F-3으로 계승했다.
- 승인 절차·계획 감사 모드·템플릿·용어는 스펙과 일치하고 지정 검증도 통과했지만, R6은 부분 충족이다.
- `issue-work --response`에서 F-3을 R6의 적합성 발견으로 먼저 검토하고 처리 방향을 확정한다.

</details>

## 요약

- 1단계 적합성: 🟡 부분 충족(PARTIAL) · 충족(PASS) 22건 / 미충족(FAIL) 0건 / 부분 충족(PARTIAL) 1건 / 판정 불가(N/A) 0건
- 2단계 위험도: 🟡 주의(WARN) · 높음(HIGH) 0건 / 중간(MEDIUM) 1건 / 낮음(LOW) 0건 / 정보(INFO) 0건
- 기등재 참조: 1건 (K-0009, 위험도·카테고리 집계 제외)
- 이전 발견: 닫힘 1건 / 잔여 1건 (F-2 → F-3)

| 카테고리 | 건수 |
|----------|------|
| 기능 | 0 |
| 아키텍처 | 0 |
| 버그 | 중간(MEDIUM) 1 |
| 보안 | 0 |
| 코드품질 | 0 |
| 성능 | 0 |
| 테스트 | 0 |

---

## 1단계: 적합성 검증 (Compliance Check)

### 요구사항 대조

| # | 요구사항 | 판정 | 근거 |
|---|----------|------|------|
| R1 | 요구사항 승인 후 DoD·plan·summary 작성 | 🟢 충족(PASS) | `issue-work/SKILL.md` 51~57행에 작성 순서, 경로와 요약 제시, 이슈 본문 근거 없는 항목의 별도 질의가 있다. |
| R2 | 계획 종료 게이트 뒤 계획 감사 여부 질의 | 🟢 충족(PASS) | 같은 파일 58~60행에 기본값 없는 선택, 타벤더 수동 감사, `--response` 후 Task 0, 생략 기록이 있다. |
| R3 | 계획 리포트 탐색·승인분 보정·별도 건수 기록 | 🟢 충족(PASS) | 같은 파일 159~166행에 리포트 선택, 영향 파일의 spec·plan 명시, 승인분 반영, summary `계획 감사` 줄 기록이 있다. |
| R4 | spec·summary·workflow 템플릿 반영 | 🟢 충족(PASS) | 근거 표기 주석·모델 행·계획 감사 줄·Task 0 전 검토 시점이 있다. 생략·수행 fixture 모두 지표 헬퍼를 통과한다. |
| R5 | `issue-audit --plan` 모드와 리포트 규약 | 🟢 충족(PASS) | `issue-audit/SKILL.md` 407~443행의 대상·대조 기준·1단계 매핑·5관점·공통 규칙·회차 보존·번호 축 분리와 템플릿 주석이 일치한다. |
| R6 | R·DoD·Task 추적성 위반 4종 탐지 | 🟡 부분 충족(PARTIAL) | 빈 값·기본 코드 펜스·블록 주석·DoD 절 경계는 보정됐다. 한 줄 주석과 길이가 다른 코드 펜스 안의 예시가 실제 연결을 대신하는 F-3이 남았다. |
| R7 | 안내도에 헬퍼·계획 감사 반영 | 🟢 충족(PASS) | `.ai/AI-CONTEXT.md`의 scripts 행·스킬 목록·워크플로우 표가 실제 변경을 가리킨다. |
| R8 | 계획·최종 감사 용어 통일 | 🟢 충족(PASS) | 지정 범위의 옛 표기가 0건이고 모델 행 4개의 순서·개수와 게이트 헤더가 일치한다. |

### 완료의 정의(DoD) 대조

| # | DoD 항목 | 판정 | 근거 |
|---|----------|------|------|
| D1 (R1) | 승인 게이트 앵커·작성 순서 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 1, 위반 출력 0건. |
| D2 (R1) | `--clear`와 승인 요청 제시 방식 동등 `[QD]` | 🟢 충족(PASS) | 두 절차 모두 파일을 승인 대상으로 삼고 경로·요약만 제시하며 전문을 재출력하지 않는다. |
| D3 (R2) | 계획 감사 질의 순서·앵커 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 2, 위반 출력 0건. |
| D4 (R3) | `--response` 5단계 유지·탐색·기록 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 3, 위반 출력 0건·러너 통과. |
| D5 (R4) | 템플릿 안내·행 개수·구조 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 4, 위반 출력 0건·러너 통과. |
| D6 (R4) | 생략·수행 값의 지표 집계 호환 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 5, 두 fixture 모두 종료 0. |
| D7 (R5) | `--plan` 소절·앵커·템플릿 주석 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 6, 위반 출력 0건. |
| D8 (R5) | 판정 매핑·5관점 정합·회귀 방지 예외 `[QD]` | 🟢 충족(PASS) | 모드별 대상·표 행을 구분하고 위험도·판정을 공유한다. 회귀 방지 항목의 표시 방법과 수동 판별 한계를 서술했다. |
| D9 (R6) | 정상·반례 4종·사용오류 fixture `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 7, 지정 케이스·출력·종료 코드 검사 통과. F-3은 이 fixture 집합 바깥의 반례다. |
| D10 (R6) | #100 spec·plan 자체 추적성 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 8, 무출력·종료 0. |
| D11 (R7) | 안내도 설명 행 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 9, 위반 출력 0건. |
| D12 (R8) | 옛 표기 0건 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 10, 일치 출력 0건. |
| D13 (R8) | 새 모델 행·게이트·모드 표기 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 11, 위반 출력 0건. |
| D14 (공통) | 전체 스킬 테스트 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 12와 전체 러너 실행 모두 통과. 10개 러너 585건, 실패 0건. |
| D15 (공통) | 변경 md의 줄 끝 공백 `[D]` | 🟢 충족(PASS) | 스펙 검증 블록 13, 위반 출력 0건. `git diff --check main`도 종료 0. |

- 집계 단위는 요구사항 8개와 스펙 DoD 15개다. plan의 고유 검증과 Task N 선행 조건은 검증 기록에 별도로 남겼다.
- D12·D15의 명령은 검색 불일치로 종료 1이지만 stdout·stderr가 모두 비었다. 스펙에 명시된 “출력 0건” 기준으로 판정했다.

### 경계 검증

- **제외 목록 침범**: 없음. 새 스킬·README 변경·archive 소급 치환·위험도 표 변경·계획 감사 자동 실행·지표 집계 편입이 없다.
- **추가 구현**: 공집합 진단은 입력 오지정에 따른 거짓 통과를 막는 보강이다. K-0009 등재와 F-1·F-2 보정은 spec·summary에 승인 근거가 있다.
- **판정 범위**: F-3을 R6의 연결 누락 탐지에 결속했다. 스펙이 정한 fixture 자체는 통과하므로 D9·D10은 낮추지 않았다.

### 도메인/계약/ADR 정합성

- 계약·도메인 index에 이번 변경을 제한하는 개별 명세가 없다. 코드베이스 index에도 기능 색인이 없어 갱신할 항목이 없다.
- 결정적 검사는 `scripts/`, 조율·판단은 `SKILL.md`, 회귀 검증은 같은 스킬의 `tests/`에 두어 아키텍처 원칙·ADR 0001과 일치한다.
- 가짜 `[D]` 판별의 수동 절차는 승인된 제외 범위다. K-0009의 수용 사유·재검토 조건이 유지된다.

---

## 2단계: 비판적 검증 (Critical Review)

### 발견 사항

| # | 위험도 | 영향 | 발생확률 | 카테고리 | 계보 | 설명 | 관련 파일 |
|---|--------|------|----------|----------|------|------|-----------|
| F-3 | 🟡 중간(MEDIUM) | 스펙·기능 달성 차단 | 특수 조건·엣지 | 버그 | F-2 잔여 | 한 줄 주석과 코드 펜스의 종류·길이·주석 내부 위치를 구분하지 못해 추적성을 잘못 판정한다. | [check-plan.sh](../../../../issue-audit/scripts/check-plan.sh) 58~66·85~87행 |

### 상세 분석

#### F-3: 한 줄 주석·긴 코드 펜스 안의 예시가 실제 요구사항 연결로 집계된다

- **위험도**: 🟡 중간(MEDIUM)
- **영향 축**: 스펙·기능 달성 차단
- **발생확률 축**: 특수 조건·엣지
- **카테고리**: 버그
- **관점**: 엣지케이스·암묵적 가정·부작용·누락된 검증
- **계보**: F-2 잔여. 코드 예시·주석을 실제 구조로 집계하지 말라는 이전 완료 기준의 미해소 부분이다.
- **스펙 결속**: R6의 `DoD 없는 R`·`Task 없는 R`·`대상 요구사항 없는 일반 Task` 탐지.
- **권장 조치**: 본문을 읽기 전에 주석 내용을 제외하고, 코드 펜스는 여는 문자의 종류·길이와 닫는 조건을 구분한다. 주석 안의 펜스가 본문 파서의 상태를 바꾸지 않도록 처리한다.
- **완료 기준**:
  - [ ] `- **대상 요구사항**: <!-- R1, R2 -->`처럼 R이 주석에만 있으면 Task별 누락과 다른 참조가 없는 R의 누락을 출력하고 종료 1을 낸다. 실제 R 뒤의 짧은 주석은 정상 참조를 유지한다.
  - [ ] 백틱 4개로 감싼 예시 안에 백틱 3개가 있어도 예시의 DoD·Task 필드를 집계하지 않는다. 같은 예시에만 둔 `R99`는 유령 위반을 만들지 않고 실제 본문의 `R99`는 계속 탐지한다.
  - [ ] `~~~` 코드 펜스 안에만 남긴 대상 필드도 연결로 세지 않아 Task별 누락과 R의 누락을 출력하고 종료 1을 낸다.
  - [ ] 닫힌 HTML 주석 안에 홀수 개의 펜스 행이 있어도 이후의 정상 본문을 동일하게 판정한다.
  - [ ] 위 반례와 정상 대조군을 러너에 추가하고, F-1·F-2의 기존 회귀 케이스와 실제 #100 spec·plan도 통과한다.

<details>
<summary>근거·영향 펼치기</summary>

- 59~66행은 닫히지 않은 주석만 건너뛴다. 한 줄에서 닫힌 주석의 내용은 남아 87행의 `/R[0-9]+/`가 주석 속 R도 연결로 센다.
- 63행은 백틱 3개로 시작하는 모든 행에서 상태를 반전한다. 백틱 4개로 연 블록 안의 백틱 3개도 닫힘으로 오인하고, 물결표 펜스는 인식하지 않는다.
- 같은 행이 주석 상태 검사보다 먼저 실행돼 HTML 주석 안의 백틱도 상태를 바꾼다. 주석 안에 펜스 행 하나만 남기면 이후의 실제 포함 목록을 놓쳐 정상 문서에 `추적 불가`를 낸다.
- 실제 #100 문서 사본에서 주석 속 R만 남긴 Task 2와 긴 펜스 예시로 옮긴 R1 DoD가 각각 무출력·종료 0이었다. 긴 펜스 예시의 `R99`는 반대로 유령 위반을 만들었다.
- 이런 문서 형태에서만 발생하므로 발생확률은 특수 조건이다. 실제 연결 누락을 통과시키므로 영향은 R6의 기능 달성 차단이다.
- 여러 반례가 공통 전처리의 경계 처리와 이전 F-2 완료 기준을 가리켜 1건으로 집계했다. 별도 테스트 발견으로 중복 집계하지 않는다.

</details>

### 기등재 참조 항목

<details>
<summary>기등재 참조 표 펼치기</summary>

| 원장 항목 | 상태 | 대조한 발견 내용 | 재검토 조건 충족 | 처리 |
|-----------|------|------------------|------------------|------|
| [K-0009](../../../70_ledger/active/K-0009-plan-audit-fake-d-check-manual.md) | 수용 | 가짜 `[D]` 사전 판별이 수동 절차로 남아 있다. | 미충족. 계획 감사에서 모델 간 판별 불일치가 2회 관측된 기록이나 템플릿 예외 표기 도입이 없다. | 기등재 K-0009 참조. 신규 번호·집계에서 제외한다. |

- K-0002·K-0004도 대조했다. 위험도 축·매트릭스와 `--clear` 보존 목적지 규정에 실질 변경이 없어 재제기하지 않았다.
- 이번 발견과 일치하는 archive 원장 항목은 index에 없다.

</details>

### 이전 발견 추적

<details>
<summary>이전 발견 추적 표 펼치기</summary>

| 이전 발견 | 완료 기준 항목 | 판정 (닫힘/잔여) | 근거 |
|-----------|----------------|------------------|------|
| F-1 | 다른 Task가 모든 R을 참조해도 빈 값·공백·`-`를 누락 처리 | 닫힘 | 실제 plan에 일반 Task 8을 추가한 세 반례 모두 Task별 누락을 출력하고 종료 1. 러너에도 세 fixture가 있다. |
| F-1 | 라벨 누락 반례와 `R1`·`R1, R2` 정상 입력 회귀 검증 | 닫힘 | 기존 라벨 누락 반례와 새 다중 참조 fixture가 통과했다. 실제 문서 사본의 유효 값 두 형태도 무출력·종료 0. |
| F-2 | DoD를 전제·코드 예시·주석에만 남겨도 누락 탐지 | 잔여 | 전제·백틱 3개·일반 블록 주석은 탐지한다. 백틱 4개 안에 백틱 3개가 있는 예시로만 옮기면 누락을 놓친다(F-3). |
| F-2 | 실제 필드를 없애고 코드 예시·주석에만 남겨도 Task·R 누락 탐지 | 잔여 | 기본 백틱 펜스·블록 주석은 탐지한다. 한 줄 주석의 R, 긴 펜스·물결표 펜스의 필드는 참조로 집계한다(F-3). |
| F-2 | 예시·주석의 R99는 무시하고 실제 유령 그룹은 탐지 | 잔여 | 단순 블록 주석은 무시하고 실제 유령 그룹은 탐지한다. 긴 펜스 안의 R99는 여전히 유령 위반을 만든다(F-3). |

- 이전 리포트의 모든 완료 기준 5개를 재검증했다. F-1은 닫힘, F-2는 부분 보정 상태다.
- 보존 전 기존 리포트 전체를 `next-finding-number.sh`에 넘겨 다음 번호 `3`을 얻었다. 이번 계보는 `F-2 잔여`이고 번호는 F-3이다.

</details>

---

## 검증 기록

- 지정 검증 명령과 추가 반례는 작업 트리 또는 임시 사본에서 실행했다. 감사로 변경한 파일은 이 리포트와 보존한 1차 리포트뿐이다.
- summary에는 최종 audit 모델이 `OpenAI, GPT-6`으로 기록돼 있다. Task N 결과는 아직 미기록이며 F-3 처리 검토 후 완료 여부를 정해야 한다.

<details>
<summary>명령 실행·교차모델 확인 내역 펼치기</summary>

| 검증 | 결과 |
|------|------|
| 스펙 DoD bash 블록 13개 | 전부 스펙의 출력 기준 충족. stdout·stderr 0건. |
| plan 고유 bash 블록 8개 | 1~6번 무출력, 7~8번 `0` 출력. 전부 종료 0. |
| 전체 스킬 러너 | ai-workspace 33, ai-workspace-directory 29, git-commit 44, git-pr 59, git-pr-feedback 24, git-review 54, git-review-quiz 59, install-skills 20, issue-audit 169, issue-work 94. 합계 585, 실패 0. |
| workflow 사본 비교 | 템플릿과 동일. |
| 변경 공백 확인 | `git diff --check main` 종료 0. |
| Task N 선행 조건 | plan·summary의 Task 헤더 집합 일치, Task 0~7의 결과·수행 모델 게이트 통과. |
| 감사 독립성 | 계획·구현 대표값과 Task 0~7의 수행 모델은 Anthropic이다. 이번 감사는 OpenAI이며 기록상 벤더가 다르다. |
| 상태·판정 산출 | 설치된 스킬의 `classify-risk.sh`에 요구사항·DoD 판정 23개와 위험도 1개를 넘겨 산출했다. |
| F-3 등급·처리 산출 | `--impact '스펙·기능 달성 차단' --likelihood '특수 조건·엣지'`는 중간(MEDIUM), `--treatment`는 `--response` 승인 게이트에서 사용자 판단. R6 부분 충족에 결속돼 처리 순서는 1단계 우선이다. |
| 이력 보존 | 1차 리포트를 `issue-0100-audit-report-1.md`로 이동하고 SHA-256 동일성을 확인했다. |

</details>

<details>
<summary>F-3 재현 명령과 기대·실제 결과 (repo 루트에서 실행)</summary>

`````bash
python3 - <<'PY'
from pathlib import Path
from tempfile import TemporaryDirectory
import subprocess

root = Path('.ai/90_issues/archive/issue-0100')
spec = (root / 'issue-0100-spec.md').read_text()
plan = (root / 'issue-0100-plan.md').read_text()
group = '### R1: 요구사항 승인 게이트'
field = '- **대상 요구사항**: R1, R2'
assert spec.count(group) == 1 and plan.count(field) == 1
example = '````markdown\n```markdown\n{}\n```\n````'
cases = [
    ('inline-only', spec,
     plan.replace(field, '- **대상 요구사항**: <!-- R1, R2 -->')),
    ('inline-note', spec, plan.replace(field, field + ' <!-- 메모 -->')),
    ('long-fence-dod', spec.replace(group, '').replace(
        '### 공통', example.format(group) + '\n\n### 공통', 1), plan),
    ('long-fence-field', spec, plan.replace(field, example.format(field))),
    ('long-fence-phantom', spec.replace(
        '### 공통', example.format('### R99: 폐기된 대안') + '\n\n### 공통', 1), plan),
    ('tilde-field', spec, plan.replace(field, '~~~markdown\n' + field + '\n~~~')),
    ('comment-fence', '<!--\n```markdown\n-->\n' + spec, plan),
]
with TemporaryDirectory(prefix='issue-0100-audit2-') as tmp:
    a, b = Path(tmp) / 'spec.md', Path(tmp) / 'plan.md'
    for label, s, p in cases:
        a.write_text(s)
        b.write_text(p)
        result = subprocess.run(
            ['issue-audit/scripts/check-plan.sh', '--trace', str(a), str(b)],
            capture_output=True, text=True)
        print(label, 'exit=' + str(result.returncode), 'stdout=' + repr(result.stdout))
PY
`````

| 사례 | 기대 결과 | 실제 결과 |
|------|-----------|-----------|
| inline-only | R1·R2 Task 누락과 Task 2 필드 누락, 종료 1 | 무출력, 종료 0 |
| inline-note | 무출력, 종료 0 | 무출력, 종료 0 |
| long-fence-dod | `DoD 없는 R: R1`, 종료 1 | 무출력, 종료 0 |
| long-fence-field | R1·R2 Task 누락과 Task 2 필드 누락, 종료 1 | 무출력, 종료 0 |
| long-fence-phantom | 무출력, 종료 0 | `R 없는 DoD 그룹: R99`, 종료 1 |
| tilde-field | R1·R2 Task 누락과 Task 2 필드 누락, 종료 1 | 무출력, 종료 0 |
| comment-fence | 무출력, 종료 0 | `추적 불가: spec 포함 목록에 R<n> 항목이 없습니다`, 종료 1 |

</details>
