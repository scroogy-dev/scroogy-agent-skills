# Issue #96 실행요약 git-review: 결과 산출물에 신호등 판정 한 줄·상태 이모지·항목별 볼드 제목과 접기 도입

> 스펙: [issue-0096-spec.md](./issue-0096-spec.md) | 계획: [issue-0096-plan.md](./issue-0096-plan.md)

## 다음 작업

> ✅ 모든 작업이 완료되었습니다.

## 모델 기록

<!--
이슈 단위 요약(대표값)이다. 세부 SSoT는 아래 `Task별 수행 결과`의 `수행 모델`이며,
한 구분에 여러 모델이 관여했으면 대표 모델을 적고 세부는 Task별 기록을 본다.

형식: "벤더, 모델명". 설계·구현·audit 어느 칸이든 어느 벤더·모델이든 가능하다
(예: Anthropic, Claude Opus 4.8 (claude-opus-4-8) / OpenAI, GPT-5.x / Google, Gemini 3.x).
설계 모델과 구현 모델은 다를 수 있어 칸을 나눈다 — 같으면 같은 값을 적는다.
"audit 모델 ≠ 구현 모델" 조건을 나중에 명령으로 확인 가능하도록 기록한다. 모델 전환은 사람이 수행하므로 기계로 강제하기 어렵다.
구현에 여러 벤더가 관여했으면 audit 모델의 벤더는 Task별 `수행 모델`의 모든 벤더와도 달라야 한다
— 대표 행 비교만으로는 audit 벤더가 구현 일부에 참여한 경우를 놓친다.
-->

| 구분 | 모델 |
|------|------|
| 설계 모델 | Anthropic, Claude Fable 5.1 (claude-fable-5-1) |
| 구현 모델 | Anthropic, Claude Opus 5 (claude-opus-5) |
| audit 모델 | OpenAI, GPT-6 |

---

## Task별 수행 결과

<!--
Task 0 및 일반 실행 Task(Task N 제외) 블록의 지표 4종은 사후 집계용이라 표기를 고정한다
(값이 없어도 필드는 지우지 않는다. Task N 예외의 근거는 아래 Task N 블록 주석).
- 수행 모델: "벤더, 모델명" — `모델 기록` 표와 같은 형식. 한 Task를 모델 전환·재시도로 둘 이상이 수행했으면
  관여한 모델을 전부 ` / `로 나열한다 — 하나만 남기면 누락된 벤더가 audit을 맡아도 Task N의 강화 조건을 통과한다.
  `-`는 미착수·스킵 Task 전용이며, `결과`가 완료·부분 완료면 실제 값을 필수로 기록한다
  (`-`로 남기면 Task N의 교차모델 강화 조건이 공집합이 되어 우회가 된다)
- audit 발견: 이 Task와 관련된 audit 발견사항 건수 — 없으면 `0건` (`-` 금지)
- 보정 반영: `--response` 항목별 승인을 통과해 실제로 보정한 건수 — 없으면 `0건` (`-` 금지)
- 재시도: 같은 Task에서 재수정·재시도한 횟수 — 없으면 `0회` (`-` 금지)

한 발견이 여러 Task 산출물에 걸쳐도 가장 직접 수정된 주 Task 하나에만 귀속한다 —
`audit 발견`·`보정 반영` 모두 그 주 Task에서 1회만 세고 관련된 다른 Task에는 세지 않는다.
Task 축에서도 이슈 단위 합계가 중복으로 부풀지 않게 하기 위함이다(아래 회차 축 중복 제외와 같은 취지).

수치 3종은 회차별 값이 아니라 이슈 전체 누적이다 — 여러 차수 감사가 같은 발견을 재확인해도
1건으로 세고(중복 제외), 새 회차 값으로 덮어쓰지 않는다. 보정률 사후 집계의 분모·분자가 회차 반복으로 부풀지 않게 하기 위함이다.

수치 3종에 `-`를 쓰지 않는 이유: 아래 스니펫이 `[0-9]+`만 추출하므로 `-`인 Task는
합계에서 조용히 빠지고, 전부 `-`면 합계 자체가 빈 문자열이 된다. `-` 허용은 비수치 필드(`수행 모델`)에만 둔다.

수치 3종의 기본값은 주석이 아니라 리터럴 `0건`/`0회`로 둔다: 주석 placeholder는 렌더링 값이
비어 있는데도 집계기가 주석 안 숫자를 실제 값으로 오인하고, `미기록` 같은 형식 위반도 걸러지지 않는다.

보정률은 별도 필드로 두지 않고 `보정 반영 / audit 발견`으로 사후 집계한다.
아래 스니펫이 동작하도록 `- **<필드>**: <숫자><단위>` 표기를 지킨다 (줄 시작, 굵게, 콜론 뒤 숫자).

  # 이슈 하나의 보정률
  found=$(grep -E '^- \*\*audit 발견\*\*:' issue-0096-summary.md | grep -oE '[0-9]+' | paste -sd+ - | bc)
  fixed=$(grep -E '^- \*\*보정 반영\*\*:' issue-0096-summary.md | grep -oE '[0-9]+' | paste -sd+ - | bc)
  echo "보정률: ${fixed}/${found}"

  # 모델별 집계는 `수행 모델` 행과 함께 뽑는다
  grep -E '^- \*\*(수행 모델|audit 발견|보정 반영|재시도)\*\*:' issue-0096-summary.md
-->

### Task 0 (고정): 구현 시작 게이트 (전제·모호점 확인)

<!--
plan 템플릿의 첫 고정 Task에 대응하는 블록이다. 삭제하지 말 것.
질의로 해소한 전제는 spec `## 전제 (Assumptions)`에 남기고, 여기에는 수행 사실만 적는다.
나열한 항목이 없었으면 `수행 내용 요약`에 `전제 누락 없음` 한 줄을 기록한다.
-->

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: spec·plan과 대상 파일 5종(`git-review/` SKILL.md·템플릿·헬퍼·러너, `issue-audit/tests/run-tests.sh`), 이슈 #96 본문을 대조해 확인 대상 8건을 나열하고 사용자 질의로 전부 확정했다. 이슈가 스펙 단계로 미룬 입력 호환은 "둘 다 받는다"(접두 4종만 허용, 그 밖은 종료 코드 2), 설계 중 정한 값 6건과 범위 판단 1건(issue-audit 러너 사본 대조 정규화)은 spec `## 전제`에 적힌 그대로 확정했다. 확정 사실을 spec `## 전제`의 두 묶음 머리말에 기재했다.
- **특이 사항**: 코드 대조에서 새로 드러난 모호점은 없다. plan Task 1~4의 작업 내용이 현재 파일 구조와 어긋나지 않고, spec DoD의 검증 명령이 짚는 앵커가 모두 실재한다. 기본값으로 둔 항목 2건 — 커밋은 요청 시에만 하며 Task 2·3 시점에 러너 2개가 실패 상태인 것은 plan에 명시된 계획이다. `install-skills` 홈 재설치는 범위 밖이다(대상은 repo 파일).

---

### Task 1: SKILL.md 규칙 정비 (판정 산출·이모지 대응표·결과 기록 형식)

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: `git-review/SKILL.md`에 `## 판정 산출` 섹션을 신설해 판정 표 3행, 이모지 대응표 10행(등급 4·상태 3·판정 3), 산출 규칙(비즈니스·테크 상태 2개의 최고값, Self 리뷰 승인은 제출 가능, 판정은 우선순위 신호이고 보정은 작성자 판단)을 두었다. `## 결과 기록 형식`을 새 형식(판정 한 줄 → 최종 의견 접기 → 요약 → 비즈니스 리뷰 → 테크 리뷰)으로 다시 쓰고 옛 항목 형식 문구를 제거했으며, 대화창 보고 첫 줄 규칙을 같은 섹션에 두었다. `## 개요` 문구, 위험도 분류·상태 산출의 헬퍼 예시 출력과 SSoT 문장, `## 산출물 접기 기준`의 항목별 접기·최종 의견 접기 2항목을 함께 정렬했다.
- **특이 사항**: spec DoD R5 첫 `[D]` 명령의 SKILL.md 대상 판정 0건, 상태 문자열 실재 검사 통과, 기존 git-review 러너 27건 통과. 템플릿 대상 옛 형식 grep은 Task 3 전이라 잔존 행을 낸다(plan에 명시된 계획 상태). R5의 `[QD]` 2항목은 별도 세션 채점이라 이 시점 미판정이다.

---

### Task 2: 헬퍼 확장 (이모지 출력·`--verdict`·입력 호환)

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 1건
- **보정 반영**: 1건
- **재시도**: 0회
- **수행 내용 요약**: `git-review/scripts/classify-risk.sh`에 대응표 10개 매핑을 옮긴 `emoji`, 접두 4종만 걷어내는 `strip_emoji`, 상태 서열 `srank` 세 함수를 추가했다. `--impact/--likelihood`와 `--status` 출력을 `<이모지> <값>` 형식으로 바꾸고 두 모드의 입력에 `strip_emoji`를 적용했으며, 위치 인자 2개를 받는 `--verdict` 모드를 신설해 상태 최고값으로 판정 3종을 산출하게 했다. 헤더 주석의 사용법·옵션·종료 코드와 `usage()`의 `sed` 범위(3,25)를 갱신하고 SSoT 문장에 판정 표·이모지 대응표를 포함시켰다.
- **특이 사항**: spec DoD R1·R2·R4의 `[D]` 명령과 기존 사용오류 8케이스가 전부 판정 0건으로 통과했다. `--verdict`는 위치 인자를 끝까지 흡수하므로 개수 위반·모드 혼용이 같은 경로에서 걸린다. Task 4 전이라 러너 2개는 실패 상태다(git-review 14/27, issue-audit 37/43). 이모지 출력에 맞춘 기대값 갱신과 사본 대조 정규화가 Task 4 범위이며 plan에 명시된 계획 상태다.
- **audit 보정 (2026-09-05, 1차 F-1)**: 모드 혼용 검사가 축 옵션의 값이 비어 있는지로 판정해 `--impact '' --verdict '통과(PASS)' '통과(PASS)'`가 exit 0으로 판정을 산출했다. 축 옵션의 등장 여부를 추적하는 `impact_set`·`likelihood_set` 플래그를 도입해 `--verdict`·`--status` 두 혼용 검사와 축 모드의 필수 인자 검사를 플래그 기준으로 바꿨다. 러너에 축 옵션 2종 × 값 형태(빈 값·일반 값) × 배치(전치·후치) 회귀 케이스 11건을 더하고 `assert_usage_error`가 표준 출력 공집합까지 보도록 강화했으며, spec DoD R1 첫 `[D]`를 같은 전수 명세로 다시 썼다. `--status` 쪽 빈 값 문제는 `main` 시점부터 있던 기존 동작이며 사용자 승인으로 함께 보정했다. 보정 후 git-review 러너 54건, repo 전체 러너 10개 전부 통과다.

---

### Task 3: 결과 템플릿 개편 (판정 줄·상태 이모지·리뷰 포인트 블록)

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: `git-review/templates/review-result-template.md`를 새 순서(판정 한 줄 → 최종 의견 접기 → 요약 → 비즈니스 리뷰 → 테크 리뷰)로 다시 썼다. `## 최종 의견` H2를 `최종 의견 펼치기` 접기로 바꾸고, 요약 상태 줄 2개·카테고리 표 상태 열·테크 리뷰 소절 제목에 `<이모지> <상태>` 자리표시자를 두었으며 건수 열 안내에 텍스트 등급만 쓴다는 문구를 넣었다. 비즈니스 리뷰의 `### 리뷰 포인트` 소제목과 두 섹션의 카테고리 단위 접기를 없애고 블록 형식(볼드 제목·일반체 지적·항목별 `근거 펼치기` 접기)으로 교체했으며, 문단 배치 이유와 접기 안·밖 경계를 안내 주석에 적었다.
- **특이 사항**: spec DoD R1 둘째·R2 둘째·R3 `[D]` 명령과 H2 집합·순서 검사가 전부 판정 0건으로 통과했다. 템플릿의 옛 항목 형식 문구가 사라져 R5 첫 `[D]`의 잔존 grep도 SKILL.md·템플릿 양쪽 0건이 되었다. R3의 `[QD]` 주석 서술 항목은 별도 세션 채점이라 이 시점 미판정이다.

---

### Task 4: 러너·안내도 정합 (git-review 러너·issue-audit 사본 대조·AI-CONTEXT)

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: `git-review/tests/run-tests.sh`에 SKILL.md `**이모지 대응표**`에서 데이터 10행을 뽑는 `emoji_rows`·`emoji_of`·`labeled`를 두고, 매트릭스 6조합과 상태 7케이스의 기대값을 `<대응표 이모지> <값>`으로 합성하도록 바꿨다. 판정 표(`| 최고 상태 | 판정 |`) 5행 추출과 `assert_verdict` 4조합(순서 무관 동시 대조), `assert_input_compat` 4케이스(접두·혼합 입력), `--verdict` 사용오류 5케이스와 미지 접두 1케이스를 추가했다. `issue-audit/tests/run-tests.sh` 사본 대조는 비교 직전에 git-review 출력의 첫 공백까지 떼는 정규화 1행과 사유 주석을 넣었고, `.ai/AI-CONTEXT.md`의 git-review `scripts/` 행을 "위험도·상태·판정 산출 헬퍼"로 바꾸고 `last updated`를 2026-09-04로 갱신했다.
- **특이 사항**: git-review 러너 43건, issue-audit 러너 38건 전부 통과했고 repo 전체 러너도 통과다. spec DoD R6 3건과 `### 공통` 2건, plan의 이모지 리터럴 0건 grep이 모두 판정 0건이다. 순서 무관 검사는 두 호출 출력끼리 비교하지 않고 양쪽 모두 표에서 합성한 기대값과 대조해, 둘 다 오류로 비면 통과하는 함정을 막았다. issue-audit 쪽 변경은 러너 1파일뿐이며 SKILL.md·헬퍼는 `git diff main` 기준 무변경이다.

---

### Task N (고정): 교차모델 issue-audit 검증 (사용자 수동 수행)

<!--
plan 템플릿의 마지막 고정 Task에 대응하는 블록이다. 삭제하지 말 것.
사용자가 직접 수행하며, 구현 AI는 이 블록을 대신 채우지 않는다.
지표 4종을 두지 않는다 — 수행 모델은 위 `모델 기록` 표의 `audit 모델` 행이 SSoT이고,
audit 발견·보정 반영은 이 Task가 만들어낸 값이라 각 대상 Task 블록에 집계된다.
-->

- **결과**: 완료
- **수행 내용 요약**: 사용자가 OpenAI GPT-6으로 `issue-audit`를 2회 수행했다. 1차 리포트([issue-0096-audit-report-1.md](./issue-0096-audit-report-1.md))는 1단계 충족(PASS) 19건·부분 충족(PARTIAL) 2건, 2단계 중간(MEDIUM) 1건(F-1: 빈 축 값으로 `--verdict` 모드 혼용 검사 우회)을 냈다. `--response`로 F-1 하나를 항목별 승인 검토해 승인분 1건을 Task 2에 보정했고, 2차 리포트([issue-0096-audit-report.md](./issue-0096-audit-report.md))에서 요구사항 6건·DoD 15건 전부 충족(PASS), F-1 완료 기준 3개 닫힘, 신규 발견 0건을 확인했다. 이슈 누적 audit 발견 1건 / 보정 반영 1건이다.
- **특이 사항**: 감사 모델 벤더(OpenAI)가 구현·설계 모델 벤더(Anthropic)와 달라 교차모델 조건을 만족한다. 기등재 K-0002(위험도 축 경계 판정 규칙 부재)는 재검토 조건 미충족으로 수용 상태를 유지했고 신규 번호·집계에서 제외했다. GitHub·IDE 실제 렌더링 확인과 홈 경로 재설치는 감사 범위 밖이다.
