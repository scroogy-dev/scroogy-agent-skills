# Issue #61 감사 리포트 — 기술부채·known issue 원장 도입

> 감사 일시: 2026-08-03  
> 감사 모델: OpenAI, GPT-5.6Sol  
> 감사 회차: 4차 (기존 리포트의 1~3차 결과를 누적 갱신)  
> 번호 기준선: F-3 (기존 리포트의 마지막 발견 번호, 이번 신규 발견은 F-4부터 시작)  
> 감사 대상 브랜치: `issue-0061` (`main...3c19c06`, 감사 리포트 외 미커밋 구현 변경 없음)  
> 스펙 출처: `./issue-0061-spec.md`

---

## 1단계: 적합성 검증 (Compliance Check)

### 요구사항 대조

| # | 요구사항 | 판정 | 근거 |
|---|----------|------|------|
| 1 | `.ai/70_ledger/` 원장 구조와 항목별 파일 규칙 정의 | 충족(PASS) | repo·공유 템플릿에 `index.md`, `active/`, `archive/`가 있고 첫 실항목 `K-0001`도 규칙에 맞게 등재됨 |
| 2 | 원장 항목 템플릿과 필수 필드 7종 신설 | 충족(PASS) | `ledger-entry-template.md`와 `K-0001`에서 필수 필드 7종을 확인함 |
| 3 | 원장 index 골격과 수용→승격/해소→archive 수명 주기 정의 | 충족(PASS) | 항목 목록·수명 주기·채번·상태별 처리 행렬이 repo와 공유 템플릿에 같은 규칙으로 존재함 |
| 4 | issue-work `--response`의 이관 목적지와 필수 필드 표준화 | 충족(PASS) | 신규 등재, 기등재 재제기 3갈래, 종결 시 기존 항목 갱신 규칙이 명시됨 |
| 5 | issue-audit의 원장 적재·기등재 참조·집계 제외·재제기 규칙 | 충족(PASS) | `active/수용`과 관련 `archive/` 항목 적재, 조건부 재제기, 승격·해소 분기, 별도 참조 섹션이 연결됨 |
| 6 | git-pr-feedback에 `수용 — 원장 등재` 선택지와 필수 필드 추가 | 충족(PASS) | 선택지·경로·채번·필수 필드·상태별 재검토 규칙이 반영됨. 기존 항목을 코드 수정으로 종결하는 조회 시점 문제는 DoD #7과 F-4에서 판정함 |
| 7 | ai-workspace 설치·갱신 구조에 `70_ledger/` 반영 | 충족(PASS) | 템플릿 골격, 컨텍스트 규칙, dev/doc 안내도, update-3 전파·분류 규칙이 반영됨 |
| 8 | 이 repo의 `.ai/`에 첫 원장 인스턴스와 안내도 반영 | 충족(PASS) | `.ai/70_ledger/` 실체·첫 항목과 `.ai/AI-CONTEXT.md`·`context-loading.md` 갱신을 확인함 |

### 완료의 정의(DoD) 대조

| # | DoD 항목 | 판정 | 근거 |
|---|----------|------|------|
| 1 | 항목 템플릿 필수 필드 7종 | 충족(PASS) | 스펙 검증 명령 통과 |
| 2 | index 골격의 수명 주기·승격·해소·archive 명시 | 충족(PASS) | 스펙 검증 명령 통과 |
| 3 | issue-work 원장 목적지·필수 필드 2종 | 충족(PASS) | 스펙 검증 명령 통과 |
| 4 | issue-audit 선택 적재·기등재 규칙·리포트 섹션 | 충족(PASS) | 스펙 검증 명령 통과 |
| 5 | git-pr-feedback 원장 선택지·필수 필드 2종 | 충족(PASS) | 스펙 검증 명령 통과 |
| 6 | ai-workspace 산출 구조와 이 repo 반영 | 충족(PASS) | 스펙 검증 명령 통과; live 항목 행을 제외한 repo·공유 템플릿 원장 규칙이 일치함 |
| 7 | 발견→등재→재검토→승격/해소 순환이 반복 보고를 실제 차단 | 부분 충족(PARTIAL) | 상태 행렬 자체는 정합하지만 git-pr-feedback의 원장 적재가 `수용 — 원장 등재` 선택에만 걸려, 별도 선택지인 `코드 수정`으로 기존 K의 원인을 없앤 경로에서는 종결 대조가 보장되지 않음(F-4) |

### 범위 검증

- **스펙 비포함(Out) 침범 여부**: 신규 스킬, 심각도 체계 개편, git-review 연계, 과거 archive 발견의 소급 등재는 확인되지 않음.
- **스펙에 없는 추가 구현 여부**: 선행 감사 보정으로 루트 제어 파일 제외, archive 상태 행렬과 첫 원장 항목이 추가됨. summary에 승인된 `--response` 결과로 기록돼 있으며 비포함 범위를 침범하지 않음.

### 도메인/계약 정합성

- 관련 계약·도메인 문서 없음.
- ADR 0001과 충돌 없음. 결정적 helper를 새로 도입하지 않았고 기존 테스트 배치도 변경하지 않음.
- `.ai/60_codebase/index.md`는 비어 있어 실제 파일과 `main...HEAD` diff를 기준으로 감사함. 코드맵 불일치는 발견되지 않음.
- `K-0001`의 재검토 조건은 등재 이후 update-3 이동 판단 기준을 다시 수정할 때다. 등재 커밋에서 해당 규칙을 다시 수정하지 않아 조건 미충족으로 판정함.

---

## 2단계: 비판적 검증 (Critical Review)

### 발견 사항

| # | 위험도 | 분류 | 계보 | 설명 | 관련 파일 |
|---|--------|------|------|------|-----------|
| F-4 | 중간(MEDIUM) | 부작용 | 신규 | git-pr-feedback이 원장 등재 선택 시에만 원장을 읽어, 별도 선택지인 코드 수정으로 기존 K의 원인을 없애도 해당 항목을 종결하지 못할 수 있음 | `git-pr-feedback/SKILL.md`, `.ai/70_ledger/index.md` |
| F-5 | 낮음(LOW) | 누락된 검증 | 신규 | summary가 동일 계보 F-3을 Task 2와 Task 4에 각각 1건으로 집계해 고유 발견 3건을 4건으로 부풀림 | `./issue-0061-summary.md` |

### 기등재 참조 항목

| 원장 항목 | 상태 | 대조한 발견 내용 | 재검토 조건 충족 | 처리 |
|-----------|------|------------------|------------------|------|
| K-0001 | 수용 | update-3 루트 상주 파일 제외 규칙의 회귀 fixture 부재 | 미충족 | 기등재 참조만, 위험도 집계 제외 |

### 이전 발견 추적

| 이전 발견 | 완료 기준 항목 | 판정 | 근거 |
|-----------|----------------|------|------|
| F-1 | update-3이 원장 `index.md`를 이동 대상에서 제외 | 닫힘 | `ai-workspace/SKILL.md`가 각 콘텐츠 디렉토리의 루트 `index.md`를 이동·legacy 대상에서 제외함 |
| F-1 | update-3 상태별 회귀 fixture | 기등재(K-0001) | 사용자 승인으로 원장에 수용됐고 재검토 조건은 미충족이라 신규 발견·위험도 집계에서 제외함 |
| F-2 | `active/수용` + 조건 미충족은 참조·집계 제외 | 닫힘 | issue-audit 본문·템플릿·원장 상태 행렬이 같은 분기를 가리킴 |
| F-2 | `archive/해소` 재발은 신규 발견·새 K 계보 | 닫힘 | issue-audit·git-pr-feedback·원장 상태 행렬에 새 번호와 `K-n 재발` 규칙이 연결됨 |
| F-2 | `archive/승격`의 열린 이슈·닫힌 이슈 분기 | 닫힘 | 열림 또는 확인 불가는 참조, 닫힘 확인 시 재발로 분기함 |
| F-2 | 관련 archive 적재와 리포트 계보 선택지 | 닫힘 | issue-audit 0단계와 리포트 템플릿에 archive 적재·`K-n 재발`이 반영됨 |
| F-2 | git-pr-feedback의 active 한정 대조와 archive 처리 | 닫힘 | `active/수용`, `archive/해소`, `archive/승격` 분기가 상태 행렬과 일치함 |
| F-3 | issue-work에서 계속 수용 시 기존 K 이력·조건 갱신 | 닫힘 | `issue-work/SKILL.md`가 이력과 다음 재검토 조건을 함께 갱신하도록 명시함 |
| F-3 | git-pr-feedback에서 조건 미충족·계속 수용·종결 분기 | 닫힘 | 조건 미충족은 이력만, 조건 충족 후 계속 수용은 이력·다음 조건 갱신, 종결은 archive 이관으로 분기함 |
| F-3 | 승격·해소 시 기존 K 상태·위치·index 갱신 | 닫힘 | issue-work와 git-pr-feedback 모두 기존 항목 종결·archive 이관·index 갱신을 명시함. 단, 코드 수정 선택에서 원장 조회가 보장되지 않는 새 결함은 F-4로 분리함 |

### 상세 분석

#### F-4: 코드 수정 선택에서는 기존 원장 항목의 종결 대조가 보장되지 않음

- **위험도**: 중간(MEDIUM)
- **분류**: 부작용
- **계보**: 신규
- **권장 조치**: git-pr-feedback이 원장 등재 여부와 무관하게 기존 관련 항목을 대조하도록 적재 시점을 코멘트 분류 전 또는 조치 실행 전으로 옮기고, 코드 수정으로 원인이 사라진 `active/수용` 항목을 종결한다.
- **완료 기준**:
  - `.ai/70_ledger/index.md` 적재 조건이 `수용 — 원장 등재` 선택에만 묶이지 않고, 코드 수정 대상과 관련된 기존 K 대조에도 적용된다.
  - `코드 수정`으로 기존 K의 원인이 사라지면 선택한 처리 방식과 무관하게 상태를 `해소(PR #N)`으로 바꾸고 `archive/` 이관·index 갱신을 수행한다.
  - 위 경로가 git-pr-feedback 본문과 원장 index의 “원인을 고친 작업의 스킬이 종결” 규칙에서 동일하다.

<details>
<summary>근거·상세 설명 펼치기</summary>

- **설명**: `git-pr-feedback/SKILL.md:31`은 원장 등재를 선택한 항목이 있을 때만 index를 읽게 한다. `코드 수정`은 별도 선택지(`:165`)인데 기존 항목 종결 규칙은 `수용 — 원장 등재` 전용 블록(`:173-198`) 안에 있어, 코드 수정만 선택하면 관련 K를 발견할 입력 경로가 없다.
- **영향**: 해결된 K가 `active/수용`으로 남으면 issue-audit은 같은 결함을 기등재 억제 대상으로 취급한다. 이후 결함이 다시 나타나도 재검토 조건이 충족될 때까지 실제 회귀가 집계에서 빠질 수 있어 원장의 청산 상태와 반복 보고 차단이 함께 어긋난다.

</details>

#### F-5: summary의 audit 발견·보정 집계가 F-3을 중복 계산함

- **위험도**: 낮음(LOW)
- **분류**: 누락된 검증
- **계보**: 신규
- **권장 조치**: F-3의 주 Task를 하나로 확정하고 Task 2 또는 Task 4 중 다른 한쪽의 `audit 발견`·`보정 반영`을 0건으로 되돌려 고유 발견 단위 집계와 맞춘다.
- **완료 기준**:
  - summary의 Task별 `audit 발견` 합계가 고유 발견 F-1~F-3의 3건과 일치한다.
  - 동일 발견이 여러 Task 또는 여러 감사 회차에 걸쳐도 주 Task 한 곳에만 귀속된다는 summary 규칙을 만족한다.

<details>
<summary>근거·상세 설명 펼치기</summary>

- **설명**: summary는 같은 발견이 여러 Task에 걸쳐도 주 Task 하나에만 귀속하고 여러 회차의 재확인도 중복 제외한다고 규정한다. 현재 Task 2와 Task 4가 모두 F-3을 `audit 발견 1건 / 보정 반영 1건`으로 세어 합계가 4/4지만, 보존된 발견 번호는 F-1~F-3 세 건뿐이다.
- **영향**: 보정률 자체는 100%로 같지만 발견·보정 총량과 Task별 기여 지표가 부풀어 후속 회고·모델별 집계의 신뢰도가 낮아진다.

</details>

---

## 검증 근거

<details>
<summary>실행 결과 펼치기</summary>

- 스펙·plan의 모든 `[D]` 검증 명령: 위반 출력 0건.
- `bash -n issue-work/tests/run-tests.sh`: 통과.
- `bash issue-work/tests/run-tests.sh`: 28 통과, 0 실패.
- `bash -n install-skills/tests/run-tests.sh`: 통과.
- `bash install-skills/tests/run-tests.sh`: 20 통과, 0 실패.
- `git diff --check main...HEAD`: 위반 없음.
- `issue-workflow-template.md`와 active 사본: byte 단위 일치.
- repo 원장 index와 공유 템플릿 index: live `K-0001` 행을 골격 placeholder로 정규화하면 일치.
- 감사 범위: `main...3c19c06`의 tracked 23개 파일. 미추적 감사 리포트는 구현 범위에서 제외함.

</details>

## 종합 의견

- **판정**: 부분 적합 — 충족(PASS) 14건 / 미충족(FAIL) 0건 / 부분 충족(PARTIAL) 1건 / 판정 불가(N/A) 0건.
- **위험도 집계**: 높음(HIGH) 0건 / 중간(MEDIUM) 1건 / 낮음(LOW) 1건 / 정보(INFO) 0건.
- **기등재 참조**: 1건 (`K-0001`, 집계 제외).
- 기존 F-1~F-3의 기능 보정은 닫혔다. 다만 git-pr-feedback의 코드 수정 경로가 기존 K를 조회·종결하지 못할 수 있어 원장 순환 DoD는 아직 부분 충족이다.
- **사용자 후속 액션**: issue-work `--response`로 F-4·F-5의 반영·이관·보류를 검토한다. 이 감사는 summary의 audit 모델·Task N 결과를 자동 수정하지 않았다.
