# Issue #98 실행요약 issue-audit: 감사 리포트에 신호등 판정 한 줄·항목별 판정·위험도 이모지와 종합 의견 접기 도입

> 스펙: [issue-0098-spec.md](./issue-0098-spec.md) | 계획: [issue-0098-plan.md](./issue-0098-plan.md)

## 다음 작업

> ✅ 모든 작업이 완료되었습니다.

## 모델 기록

<!--
이슈 단위 요약(대표값)이다. 세부 SSoT는 아래 `Task별 수행 결과`의 `수행 모델`이며,
한 구분에 여러 모델이 관여했으면 대표 모델을 적고 세부는 Task별 기록을 본다.

형식: "벤더, 모델명". 설계·구현·audit 어느 칸이든 어느 벤더·모델이든 가능하다
(예: Anthropic, Claude Opus 4.8 (claude-opus-4-8) / OpenAI, GPT-5.x / Google, Gemini 3.x).
설계 모델과 구현 모델은 다를 수 있어 칸을 나눈다 — 같으면 같은 값을 적는다.
"audit 모델 ≠ 구현 모델" 조건을 나중에 명령으로 확인 가능하도록 기록한다. 모델 전환은 사람이 수행하므로 기계로 강제하기 어렵다.
구현에 여러 벤더가 관여했으면 audit 모델의 벤더는 Task별 `수행 모델`의 모든 벤더와도 달라야 한다
— 대표 행 비교만으로는 audit 벤더가 구현 일부에 참여한 경우를 놓친다.
-->

| 구분 | 모델 |
|------|------|
| 설계 모델 | Anthropic, Claude Fable 5.1 (claude-fable-5-1) |
| 구현 모델 | Anthropic, Claude Opus 5 (claude-opus-5) |
| audit 모델 | OpenAI, GPT-6 |

---

## Task별 수행 결과

<!--
Task 0 및 일반 실행 Task(Task N 제외) 블록의 지표 4종은 사후 집계용이라 표기를 고정한다
(값이 없어도 필드는 지우지 않는다. Task N 예외의 근거는 아래 Task N 블록 주석).
- 수행 모델: "벤더, 모델명" — `모델 기록` 표와 같은 형식. 한 Task를 모델 전환·재시도로 둘 이상이 수행했으면
  관여한 모델을 전부 ` / `로 나열한다 — 하나만 남기면 누락된 벤더가 audit을 맡아도 Task N의 강화 조건을 통과한다.
  `-`는 미착수·스킵 Task 전용이며, `결과`가 완료·부분 완료면 실제 값을 필수로 기록한다
  (`-`로 남기면 Task N의 교차모델 강화 조건이 공집합이 되어 우회가 된다)
- audit 발견: 이 Task와 관련된 audit 발견사항 건수 — 없으면 `0건` (`-` 금지)
- 보정 반영: `--response` 항목별 승인을 통과해 실제로 보정한 건수 — 없으면 `0건` (`-` 금지)
- 재시도: 같은 Task에서 재수정·재시도한 횟수 — 없으면 `0회` (`-` 금지)

한 발견이 여러 Task 산출물에 걸쳐도 가장 직접 수정된 주 Task 하나에만 귀속한다 —
`audit 발견`·`보정 반영` 모두 그 주 Task에서 1회만 세고 관련된 다른 Task에는 세지 않는다.
Task 축에서도 이슈 단위 합계가 중복으로 부풀지 않게 하기 위함이다(아래 회차 축 중복 제외와 같은 취지).

수치 3종은 회차별 값이 아니라 이슈 전체 누적이다 — 여러 차수 감사가 같은 발견을 재확인해도
1건으로 세고(중복 제외), 새 회차 값으로 덮어쓰지 않는다. 보정률 사후 집계의 분모·분자가 회차 반복으로 부풀지 않게 하기 위함이다.

수치 3종에 `-`를 쓰지 않는 이유: 아래 스니펫이 `[0-9]+`만 추출하므로 `-`인 Task는
합계에서 조용히 빠지고, 전부 `-`면 합계 자체가 빈 문자열이 된다. `-` 허용은 비수치 필드(`수행 모델`)에만 둔다.

수치 3종의 기본값은 주석이 아니라 리터럴 `0건`/`0회`로 둔다: 주석 placeholder는 렌더링 값이
비어 있는데도 집계기가 주석 안 숫자를 실제 값으로 오인하고, `미기록` 같은 형식 위반도 걸러지지 않는다.

보정률은 별도 필드로 두지 않고 `보정 반영 / audit 발견`으로 사후 집계한다.
아래 스니펫이 동작하도록 `- **<필드>**: <숫자><단위>` 표기를 지킨다 (줄 시작, 굵게, 콜론 뒤 숫자).

  # 이슈 하나의 보정률
  found=$(grep -E '^- \*\*audit 발견\*\*:' issue-0098-summary.md | grep -oE '[0-9]+' | paste -sd+ - | bc)
  fixed=$(grep -E '^- \*\*보정 반영\*\*:' issue-0098-summary.md | grep -oE '[0-9]+' | paste -sd+ - | bc)
  echo "보정률: ${fixed}/${found}"

  # 모델별 집계는 `수행 모델` 행과 함께 뽑는다
  grep -E '^- \*\*(수행 모델|audit 발견|보정 반영|재시도)\*\*:' issue-0098-summary.md
-->

### Task 0 (고정): 구현 시작 게이트 (전제·모호점 확인)

<!--
plan 템플릿의 첫 고정 Task에 대응하는 블록이다. 삭제하지 말 것.
질의로 해소한 전제는 spec `## 전제 (Assumptions)`에 남기고, 여기에는 수행 사실만 적는다.
나열한 항목이 없었으면 `수행 내용 요약`에 `전제 누락 없음` 한 줄을 기록한다.
-->

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: spec `## 전제`의 설계 단계 결정 10항목(`--verdict` 인자·색 서열, `--compliance` 서열·인자 0개, `--status` git-review 동일, 모드 혼용 판정 기준, 이모지 SSoT 14행, 2단계 상태 표, 판정 표 셀 나열, 소절 H4 배치, 템플릿 구조, 출력 요약·사본 대조)을 사용자에게 확인받아 전부 확정값 그대로 진행하기로 했다. 문서에 없던 구현 세부 2건(러너 `verdict_for`의 ` · ` 분할 후 정확 일치, `--treatment`의 `shift 2` 유지 + 입력 `strip_emoji`)을 spec 전제에 추가했다. 미해소 0건.
- **특이 사항**: 착수 전 기준선 확인 — issue-audit·git-review 러너 양쪽 통과, spec 전제가 지목한 현행 결함(`--impact '' --treatment '높음(HIGH)'`가 종료 코드 0으로 처리 문구 출력)을 재현했다.

---

### Task 1: SKILL.md 규칙 정비 (상태 산출·판정 산출·이모지 대응표·결과 기록)

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: `### 3단계: 결과 기록` 1번 항목에 기록 규칙 하위 목록(판정 한 줄·종합 의견 접기·상태 등급 표기 위치 4곳·건수 텍스트·발견 헤더 불변)을, 3번 항목에 대화창 보고의 판정 한 줄 문장을 넣었다. 같은 3단계 아래에 `#### 상태 산출`(1단계 서열 문장 + 2단계 상태 표 3행 + 헬퍼 예 4개)과 `#### 판정 산출`(판정 표 3행 + 판정 3종 의미 + 헬퍼 예 + 이모지 대응표 14행)을 신설했다. `#### 위험도 분류` 예시 출력을 `# → 🟡 중간(MEDIUM)`으로, `--treatment` 예시를 접두 입력으로 바꾸고 SSoT 문장에 대응표를 포함시켰다. `## 산출물 접기 기준`에 종합 의견 접기 해석을, `## 출력 요약 형식` 코드 블록 둘째 줄에 `<판정>`을 추가했다.
- **특이 사항**: spec DoD R6 `[D]`·러너 통과·표 불변 `[D]` 3건 모두 출력 0건. 매트릭스·축·등급별 처리 표 셀은 텍스트로 유지해 러너의 기대값 추출이 그대로 동작한다.

---

### Task 2: 헬퍼 확장 (이모지 출력·`--compliance`·`--status`·`--verdict`·입력 호환·모드 혼용 플래그)

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 1회
- **수행 내용 요약**: `classify-risk.sh`에 `emoji`(대응표 14행)·`strip_emoji`(접두 4종)·`crank`(1단계 판정 서열)·`srank`(2단계 상태 서열)를 두고, 매트릭스 출력을 `<이모지> <등급>`으로 바꿨다. `--compliance`(1개 이상 흡수, 최고값)·`--status`(0개 이상, git-review 동일 규칙)·`--verdict`(위치 인자 2개, `crank`/`srank` 성공으로 단계 판별, 같은 단계 2개 거부)를 신설하고, 값 모드 4종에 `strip_emoji`를 적용했다. 모드 혼용 검사를 `impact_set`·`likelihood_set` 플래그와 값 모드 카운터로 바꿔 빈 축 값과 값 모드 간 혼용을 함께 거부하고, 모든 산출 `echo`를 검증 뒤로 옮겨 사용오류의 표준 출력을 비웠다. 헤더 주석·`usage()` 범위(3~34행)를 갱신했다.
- **특이 사항**: 재시도 1회 — `--verdict` 색 서열을 이모지 리터럴 `case`로 처음 구현해 "이모지 리터럴이 두 함수 밖에 없다" 완료 기준에 걸렸다. `red="$(emoji '미충족(FAIL)')"`·`yellow="$(emoji '부분 충족(PARTIAL)')"`를 앵커로 삼아 리터럴 없이 대응표 색을 따르도록 고쳤다. spec DoD R1~R5 `[D]` 6건 모두 출력 0건(R5 전수 검증은 `read -a`를 쓰므로 bash로 실행).

---

### Task 3: 리포트 템플릿 개편 (판정 줄·종합 의견 접기·상태·등급 자리표시자)

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: `## 종합 의견` H2를 유지한 채 그 아래에 `<판정>` 한 줄과 판정 안내 주석, `종합 의견 펼치기` 접기를 두었다. 요약 두 줄을 `<이모지> <상태> · <텍스트 건수>` 형식으로 바꾸고 `--compliance`·`--status` 안내 주석을 달았다. 카테고리 표 건수 안내에 "텍스트 등급만"을 넣고, 1단계 두 대조 표 아래에 판정 열 안내(값 하나를 넘긴 `--compliance` 출력)를, 2단계 발견 사항 표 위험도 주석과 상세 분석 `- **위험도**: <이모지> <등급>` 필드에 매트릭스 헬퍼 출력 안내를 넣었다. `#### F-<번호>: <제목>` 헤더는 그대로 두었다.
- **특이 사항**: spec DoD R1 둘째 `[D]`(구조 순서)·R7 `[D]` 출력 0건. Task 4 전이라 러너 전체는 헬퍼 이모지 출력 때문에 실패 상태이며, 리포트 구조 케이스만 잘라 통과를 확인했다.

---

### Task 4: 러너·안내도 정합 (대응표 추출·상태·판정·입력 호환·사용오류·구조 스모크·사본 대조·AI-CONTEXT)

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: 러너에 `emoji_rows`·`emoji_of`·`labeled`를 두어 대응표 14행을 추출하고, 매트릭스 6조합 기대값을 `labeled`로 합성하도록 바꿨다. 사본 대조에서 정규화 1행을 걷어내 git-review 출력과 전체를 비교하고 `ok` 행 문구를 `매트릭스·이모지 일치`로 바꿨다. `입력 호환`(`--treatment` 접두 4종 + `--compliance`·`--status`·`--verdict` 6종), `1단계 상태`(값 실재 4 + 매핑 9), `2단계 상태`(값 실재 3 + 매핑 7), `판정`(표 추출 + 7조합 양방향, `verdict_for`는 ` · ` 구분자를 양끝에 붙여 정확 일치) 케이스를 추가했다. `assert_usage_error`를 표준 출력 공집합까지 보도록 강화하고 혼용 전수(축 2 × 값 형태 2 × 값 모드 4 × 배치 2 = 32) + 값 모드 간 혼용 6 + 값 모드 고유 사용오류를 넣었다. 리포트 구조 스모크에 판정·접기·요약 줄 판정을 넣고 반례 fixture 5종을 실제 템플릿의 awk 변형으로 만들었다. `.ai/AI-CONTEXT.md`의 issue-audit scripts 행과 `last updated`를 갱신했다.
- **특이 사항**: 러너 143건 통과(변경 전 대비 +100건 남짓). spec DoD R8 `[D]` 3건, Task 4 고유 `[D]` 2건, 공통 `[D]` 3건(repo 전체 러너 9종 통과, 매트릭스·축·등급별 처리 표 `main`과 동일, git-review 무변경) 모두 출력 0건.

---

### Task N (고정): 교차모델 issue-audit 검증 (사용자 수동 수행)

<!--
plan 템플릿의 마지막 고정 Task에 대응하는 블록이다. 삭제하지 말 것.
사용자가 직접 수행하며, 구현 AI는 이 블록을 대신 채우지 않는다.
지표 4종을 두지 않는다 — 수행 모델은 위 `모델 기록` 표의 `audit 모델` 행이 SSoT이고,
audit 발견·보정 반영은 이 Task가 만들어낸 값이라 각 대상 Task 블록에 집계된다.
-->

- **결과**: 완료
- **수행 내용 요약**: 사용자가 OpenAI GPT-6로 1차 교차모델 audit을 수행했다. 리포트는 [issue-0098-audit-report.md](./issue-0098-audit-report.md)이며 종합 판정 적합(PASS), 1단계 충족(PASS) 26건(미충족·부분 충족 0건), 2단계 신규 발견 0건이다. 스펙 `[D]` 명령 15개와 전체 스킬 러너 10종을 재실행해 통과를 확인했고, 헬퍼 입력 941건을 독립 대조해 상태·판정 산출의 불일치가 없음을 확인했다. `--response` 검토 대상 발견이 0건이라 앞 Task의 `audit 발견`·`보정 반영`은 모두 0건으로 유지했다.
- **특이 사항**: 기등재 K-0002(영향·발생확률 축의 경계 판정 우선 규칙 부재)는 재검토 조건 미충족으로 참조만 하고 재제기하지 않아 원장 갱신이 없다. 감사 벤더 OpenAI는 설계·구현 모델과 Task 0~4 `수행 모델`의 벤더(모두 Anthropic)와 달라 교차 벤더 조건을 충족한다.
