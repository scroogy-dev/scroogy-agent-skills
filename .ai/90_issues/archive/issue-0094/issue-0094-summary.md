# Issue #94 실행요약 issue-work: 스펙에 요구사항(Requirements) 섹션 신설과 범위(Scope) 통합, DoD·Task를 요구사항 단위로 연결

> 스펙: [issue-0094-spec.md](./issue-0094-spec.md) | 계획: [issue-0094-plan.md](./issue-0094-plan.md)

## 다음 작업

> ✅ 모든 작업이 완료되었습니다.

## 모델 기록

<!--
이슈 단위 요약(대표값)이다. 세부 SSoT는 아래 `Task별 수행 결과`의 `수행 모델`이며,
한 구분에 여러 모델이 관여했으면 대표 모델을 적고 세부는 Task별 기록을 본다.

형식: "벤더, 모델명". 설계·구현·audit 어느 칸이든 어느 벤더·모델이든 가능하다
(예: Anthropic, Claude Opus 4.8 (claude-opus-4-8) / OpenAI, GPT-5.x / Google, Gemini 3.x).
설계 모델과 구현 모델은 다를 수 있어 칸을 나눈다 — 같으면 같은 값을 적는다.
"audit 모델 ≠ 구현 모델" 조건을 나중에 명령으로 확인 가능하도록 기록한다. 모델 전환은 사람이 수행하므로 기계로 강제하기 어렵다.
구현에 여러 벤더가 관여했으면 audit 모델의 벤더는 Task별 `수행 모델`의 모든 벤더와도 달라야 한다
— 대표 행 비교만으로는 audit 벤더가 구현 일부에 참여한 경우를 놓친다.
-->

| 구분 | 모델 |
|------|------|
| 설계 모델 | Anthropic, Claude Fable 5 (claude-fable-5) |
| 구현 모델 | Anthropic, Claude Fable 5 (claude-fable-5) |
| audit 모델 | OpenAI, GPT-5 |

---

## Task별 수행 결과

<!--
Task 0 및 일반 실행 Task(Task N 제외) 블록의 지표 4종은 사후 집계용이라 표기를 고정한다
(값이 없어도 필드는 지우지 않는다. Task N 예외의 근거는 아래 Task N 블록 주석).
- 수행 모델: "벤더, 모델명" — `모델 기록` 표와 같은 형식. 한 Task를 모델 전환·재시도로 둘 이상이 수행했으면
  관여한 모델을 전부 ` / `로 나열한다 — 하나만 남기면 누락된 벤더가 audit을 맡아도 Task N의 강화 조건을 통과한다.
  `-`는 미착수·스킵 Task 전용이며, `결과`가 완료·부분 완료면 실제 값을 필수로 기록한다
  (`-`로 남기면 Task N의 교차모델 강화 조건이 공집합이 되어 우회가 된다)
- audit 발견: 이 Task와 관련된 audit 발견사항 건수 — 없으면 `0건` (`-` 금지)
- 보정 반영: `--response` 항목별 승인을 통과해 실제로 보정한 건수 — 없으면 `0건` (`-` 금지)
- 재시도: 같은 Task에서 재수정·재시도한 횟수 — 없으면 `0회` (`-` 금지)

한 발견이 여러 Task 산출물에 걸쳐도 가장 직접 수정된 주 Task 하나에만 귀속한다 —
`audit 발견`·`보정 반영` 모두 그 주 Task에서 1회만 세고 관련된 다른 Task에는 세지 않는다.
Task 축에서도 이슈 단위 합계가 중복으로 부풀지 않게 하기 위함이다(아래 회차 축 중복 제외와 같은 취지).

수치 3종은 회차별 값이 아니라 이슈 전체 누적이다 — 여러 차수 감사가 같은 발견을 재확인해도
1건으로 세고(중복 제외), 새 회차 값으로 덮어쓰지 않는다. 보정률 사후 집계의 분모·분자가 회차 반복으로 부풀지 않게 하기 위함이다.

수치 3종에 `-`를 쓰지 않는 이유: 아래 스니펫이 `[0-9]+`만 추출하므로 `-`인 Task는
합계에서 조용히 빠지고, 전부 `-`면 합계 자체가 빈 문자열이 된다. `-` 허용은 비수치 필드(`수행 모델`)에만 둔다.

수치 3종의 기본값은 주석이 아니라 리터럴 `0건`/`0회`로 둔다: 주석 placeholder는 렌더링 값이
비어 있는데도 집계기가 주석 안 숫자를 실제 값으로 오인하고, `미기록` 같은 형식 위반도 걸러지지 않는다.

보정률은 별도 필드로 두지 않고 `보정 반영 / audit 발견`으로 사후 집계한다.
아래 스니펫이 동작하도록 `- **<필드>**: <숫자><단위>` 표기를 지킨다 (줄 시작, 굵게, 콜론 뒤 숫자).

  # 이슈 하나의 보정률
  found=$(grep -E '^- \*\*audit 발견\*\*:' issue-0094-summary.md | grep -oE '[0-9]+' | paste -sd+ - | bc)
  fixed=$(grep -E '^- \*\*보정 반영\*\*:' issue-0094-summary.md | grep -oE '[0-9]+' | paste -sd+ - | bc)
  echo "보정률: ${fixed}/${found}"

  # 모델별 집계는 `수행 모델` 행과 함께 뽑는다
  grep -E '^- \*\*(수행 모델|audit 발견|보정 반영|재시도)\*\*:' issue-0094-summary.md
-->

### Task 0 (고정): 구현 시작 게이트 (전제·모호점 확인)

<!--
plan 템플릿의 첫 고정 Task에 대응하는 블록이다. 삭제하지 말 것.
질의로 해소한 전제는 spec `## 전제 (Assumptions)`에 남기고, 여기에는 수행 사실만 적는다.
나열한 항목이 없었으면 `수행 내용 요약`에 `전제 누락 없음` 한 줄을 기록한다.
-->

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Fable 5 (claude-fable-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: 구현 세부 모호점 2건(issue-audit 3단계 명칭 처리, `--response` 1단계 문구)을 사용자에게 질의해 확정하고 spec `## 전제`의 "구현 세부 결정"에 반영. 3단계 명칭은 "경계 검증"으로 개명하기로 결정
- **특이 사항**: 형식 확정값(요구사항 표기·DoD 그룹·대상 요구사항 필드)은 스펙 작성 논의에서 이미 합의되어 spec `## 전제`에 기재됨

---

### Task 1: 스펙 템플릿 개편 (요구사항 신설·범위 통합·DoD 그룹화)

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Fable 5 (claude-fable-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: `issue-spec-template.md`에 `## 요구사항 (Requirements)` 섹션(포함 R<n> 목록·제외 목록·작성 안내 주석) 신설, `## 범위 (Scope)` 삭제, DoD를 그룹 구조(`### R<n>: <짧은 이름>` + `### 공통`)로 개편하고 안내 주석에 그룹 규칙(상위 무태그, 판정 하위 전담, 커버리지 잔여 명시, 공통 그룹 용도) 반영. spec DoD R1·R2·R3 `[D]` 명령 통과
- **특이 사항**:

---

### Task 2: 계획 템플릿 개편 (대상 요구사항 필드)

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Fable 5 (claude-fable-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: `issue-plan-template.md`의 Task 1·Task 2 예시에 `- **대상 요구사항**: R<n>` 필드를 `목표` 다음 행에 추가하고, Tasks 안내 블록에 필드 규칙(일반 Task 필수, 복수 나열, 고정 Task 제외) 신설. spec DoD R4 `[D]` 명령 통과
- **특이 사항**:

---

### Task 3: 스킬 본문·안내 문구 정합

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Fable 5 (claude-fable-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: issue-work SKILL.md 스펙 구성 서술 2곳과 `--response` 1단계 문구("요구사항(포함·제외)·DoD 대조"), 워크플로우 템플릿·상주 사본의 구성 주석 갱신. issue-audit SKILL.md 3단계 명칭을 "경계 검증"으로 개명하고 대조 원천을 "요구사항의 제외 목록"으로 교체, 요구사항 대조의 원천을 포함 목록(R<n>)으로 명시, 리포트 템플릿 `### 경계 검증` 헤더·필드 갱신. spec DoD R5 `[D]` 명령 통과
- **특이 사항**: 감사 리포트 템플릿의 1단계 섹션 헤더가 바뀌므로, 신규 audit부터 새 헤더로 산출됨 (기존 archive 리포트는 소급하지 않음)

---

### Task 4: 헬퍼·테스트 영향 확인

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Fable 5 (claude-fable-5)
- **audit 발견**: 2건
- **보정 반영**: 2건
- **재시도**: 0회
- **수행 내용 요약**: `check-clear.sh`·`summarize-metrics.sh`·`issue-work/tests/`·`issue-audit/tests/`에서 스펙·계획의 옛 구조 전제 탐색 결과 0건(계획 쪽 전제는 게이트 체크박스·Task 블록·지표 필드뿐이라 필드 추가와 무관). issue-work 테스트와 repo 전체 10개 스킬 테스트 통과. spec DoD R6·공통 `[D]` 명령 통과
- **특이 사항**: 1차 audit F-1(새 템플릿 계약의 정규 회귀 검사 부재, 정보(INFO))을 사용자 승인으로 반영 — `issue-work/tests/run-tests.sh`에 spec·plan 템플릿 구조 계약 스모크(정상 통과 + 반례 12건 격추), `issue-audit/tests/run-tests.sh`에 리포트 템플릿 구조 스모크(정상 통과 + 반례 3건 격추)를 편입. 편입 후 issue-work 68건·issue-audit 35건·repo 전체 테스트 통과. 2차 audit F-2(구조 스모크가 헤더 중복·필드 위치 이동·옛 표기 공존을 통과시킴, 정보(INFO))도 사용자 승인으로 반영 — 세 검사 함수를 개수·소속·순서·값 형식 판정으로 강화하고 반례 10건(spec 5·plan 3·리포트 2) 추가. 강화 후 issue-work 76건·issue-audit 37건·repo 전체 테스트 통과

---

### Task N (고정): 교차모델 issue-audit 검증 (사용자 수동 수행)

<!--
plan 템플릿의 마지막 고정 Task에 대응하는 블록이다. 삭제하지 말 것.
사용자가 직접 수행하며, 구현 AI는 이 블록을 대신 채우지 않는다.
지표 4종을 두지 않는다 — 수행 모델은 위 `모델 기록` 표의 `audit 모델` 행이 SSoT이고,
audit 발견·보정 반영은 이 Task가 만들어낸 값이라 각 대상 Task 블록에 집계된다.
-->

- **결과**: 완료
- **수행 내용 요약**: 1·2차 audit 리포트(`./issue-0094-audit-report-1.md`·`./issue-0094-audit-report.md`, OpenAI GPT-5; 작성 시점 경로는 `.ai/99_workspace/`, --clear로 이관)에서 발견 각 1건(F-1·F-2, 모두 정보(INFO))을 `--response`로 검토해 사용자 승인 후 반영. 2차에서 1단계 적합성 16건 전부 충족(PASS), 이전 발견 F-1 닫힘 판정
- **특이 사항**: 두 발견 모두 정보(INFO) 등급 기본값(보류)을 사용자가 반영으로 승격해 테스트 스모크 편입·강화로 종결. F-2 보정 후 3차 재감사 없이 종결(사용자 판단)
