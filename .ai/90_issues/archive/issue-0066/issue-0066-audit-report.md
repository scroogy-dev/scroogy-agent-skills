# Issue #66 감사 리포트 — issue-work 완료 기준 가독성 개선 (2차 보정 후 3차 재감사)

> 감사 일시: 2026-08-02  
> 감사 모델: OpenAI, GPT-5  
> 감사 대상 브랜치: `issue-0066` (`main...a43a80a`)  
> 스펙 출처: `./issue-0066-spec.md`

---

## 1단계: 적합성 검증 (Compliance Check)

### 요구사항 대조

| # | 요구사항 | 판정 | 근거 |
|---|----------|------|------|
| 1 | spec 템플릿 `완료의 정의`를 문장 본문 + 검증 상세 접기로 변경 | 충족(PASS) | `[D]` 본문·접기와 `[QD]`·`[ND]` 단문 예시가 있고, 검증 명령·기대 출력·설계 주의가 접기에 배치됨 |
| 2 | plan 템플릿의 일반 Task·Task 0·Task N에 형식 적용 | 충족(PASS) | 일반 Task 2건과 Task N 3건에 접기가 적용되고 Task 0 단문은 본문에 유지됨. Task N 본문 3건은 보장 내용·의존 관계·우회 결과를 각각 한 문장에 담음 |
| 3 | `issue-work/SKILL.md`에 완료 기준 형식 규칙 명시 | 충족(PASS) | `### 완료 기준 형식` 절과 작업 진행 중 진입 포인터가 있으며 본문·접기·강등 사유·의존 관계 규칙을 명시 |
| 4 | `run-tests.sh` 게이트 추출을 접기 안 bash 코드 블록 형식에 대응 | 충족(PASS) | `extract_gate`가 앵커 뒤 코드 블록을 추출하고 `P=`/`S=`를 제외함. 추출·정상·반례·response 스모크 28건 통과 |
| 5 | plan `## Tasks` 안내문을 새 형식과 일치시킴 | 충족(PASS) | 본문/접기 배치, 기대 출력, 접지 않는 경우, 선행 조건의 결과·메커니즘 배치 규칙이 안내 blockquote에 반영됨 |

### 완료의 정의(DoD) 대조

| # | DoD 항목 | 판정 | 근거 |
|---|----------|------|------|
| 1 | spec 예시에 접기 구조가 실재하고 여닫이 수가 같음 | 충족(PASS) | `<details>` 1건, `</details>` 1건 |
| 2 | plan 일반 Task와 Task N 양쪽에 접기 구조가 실재 | 충족(PASS) | Task N 앞 2쌍, Task N 3쌍. 지정 awk 게이트 출력 `0` |
| 3 | SKILL.md에 `완료 기준 형식` 앵커가 실재 | 충족(PASS) | 2건 확인 |
| 4 | 기존 `[D]` 앵커 기반 회귀 테스트가 새 형식에서도 통과 | 충족(PASS) | `bash issue-work/tests/run-tests.sh`: `passed: 28, failed: 0` |
| 5 | 본문·접기 배치가 제안 형식 및 접기 금지 기준과 불일치 없음 | 충족(PASS) | 보장 내용·리스크는 본문, 검증 상세·발생 메커니즘은 접기에 유지됨. 보정된 Task N 본문 3건도 한 문장 규칙을 충족 |
| 6 | GitHub 파일 뷰에서 접힘/펼침과 리스트 구조가 정상 | 충족(PASS) | summary에 사용자 화면 확인과 GFM HTML 구조 검증이 기록됨. 이후 보정은 태그·들여쓰기·코드 블록 구조를 바꾸지 않음 |

### 범위 검증

- **스펙 비포함(Out) 침범 여부**: 없음. 기존 active/archive 이슈 문서 소급 수정, summary/workflow 템플릿 수정, 설치본 반영은 수행하지 않음.
- **스펙에 없는 추가 구현 여부**: 없음. 이슈 문서 3종 추가와 범위 내 4개 파일 수정만 확인됨.

### 도메인/계약 정합성

- 관련 계약·도메인 문서 없음.
- ADR 0001 정합: 테스트는 `issue-work/tests/`에 유지되고 외부 의존성을 추가하지 않았으며, 인스턴스화된 plan 명령을 별도 helper로 분리하지 않음.
- `.ai/60_codebase/index.md`는 비어 있어 실제 소스와 `main...HEAD` diff를 기준으로 감사함. 색인과 코드의 불일치는 발견되지 않음.

### 이전 감사 대응 확인

- **이전 F-1(LOW, Task N 본문 두 문장)**: 해소. 커밋 `0e1d177`에서 마침표를 연결 표현으로 바꿔 3개 항목을 각각 한 문장으로 복원했고, 우회 결과는 본문에 유지함.
- **기존 구조 게이트 한계(LOW)**: 미보정·수용 유지. 현재 실제 구조는 올바르지만 일회성 DoD 명령 자체의 검사 범위는 좁음(F-1).

---

## 2단계: 비판적 검증 (Critical Review)

### 발견 사항

| # | 위험도 | 분류 | 설명 | 관련 파일 |
|---|--------|------|------|-----------|
| F-1 | 낮음(LOW) | 누락된 검증 | plan 접기 구조 `[D]` 게이트가 대상 완료 기준의 하위 구조를 검증하지 않아 잘못된 위치의 태그만으로도 통과 가능 | `./issue-0066-spec.md` |

### 상세 분석

#### F-1: 접기 구조 검증 게이트가 정확한 위치를 판정하지 않음

- **위험도**: 낮음(LOW)
- **분류**: 누락된 검증
- **권장 조치**: 현재의 known issue 수용을 유지할 수 있다. 이 게이트를 재사용·상시 회귀 게이트로 승격할 때는 일반 Task `완료 기준` 하위와 Task N `[D]` 항목 하위로 범위를 제한하고, 주석·잘못된 Task에만 태그가 있는 반례를 추가한다.

<details>
<summary>근거·영향 펼치기</summary>

- **설명**: 현재 awk는 `### Task N` 전후의 임의 `<details>` 존재만 센다. 일반 Task 완료 기준과 Task N 완료 기준에 접기가 없어도 다른 위치에 태그가 있으면 출력 `0`이 될 수 있다.
- **영향**: 현재 템플릿의 실제 구조는 올바르고 Task N 게이트 추출은 별도 회귀 테스트로 보호된다. 따라서 현 구현의 기능 오류는 아니며, 일회성 DoD 명령의 판정력 한계다.

</details>

---

## 검증 근거

<details>
<summary>실행 결과 펼치기</summary>

- `bash issue-work/tests/run-tests.sh`: 28 통과, 0 실패
- `bash -n issue-work/tests/run-tests.sh`: 통과
- spec 접기 수: 여는 태그 1, 닫는 태그 1
- plan 접기 수: Task N 앞 2쌍, Task N 3쌍; 구조 게이트 출력 `0`
- 실제 Issue #66 Task N 선행 게이트: Task 헤더 diff 0건, `결과` 게이트 `0`, `수행 모델` 게이트 `0`
- `git diff --check main...HEAD`: 위반 없음
- 변경 범위: 이슈 문서 3종 추가, `issue-work` 범위 내 파일 4종 수정

</details>

## 종합 의견

- **판정**: 적합 — 충족(PASS) 11건 / 미충족(FAIL) 0건 / 부분 충족(PARTIAL) 0건 / 판정 불가(N/A) 0건.
- 이전 재감사의 본문 한 문장 위반은 해소됐고, 요구사항·DoD·결정적 회귀 테스트가 모두 통과한다.
- 남은 낮은 위험 1건은 이미 수용된 일회성 검증 명령의 한계이며 현재 구현 완료를 막지 않는다.
- **사용자 후속 액션**: summary의 `audit 모델`과 Task N 결과에 이 리포트 경로·발견 1건을 기록한다. 이 감사는 workflow 규칙에 따라 summary를 대신 수정하거나 Task N을 자동 완료 처리하지 않았다.

