# Issue #66 실행요약 — issue-work 완료 기준 가독성 개선: 항목은 문장으로, 검증 상세는 접기로

> 스펙: [issue-0066-spec.md](./issue-0066-spec.md) | 계획: [issue-0066-plan.md](./issue-0066-plan.md)

## 다음 작업

> ✅ 모든 작업이 완료되었습니다.

## 모델 기록

<!--
이슈 단위 요약(대표값)이다. 세부 SSoT는 아래 `Task별 수행 결과`의 `수행 모델`이며,
한 구분에 여러 모델이 관여했으면 대표 모델을 적고 세부는 Task별 기록을 본다.

형식: "벤더, 모델명". 설계·구현·audit 어느 칸이든 어느 벤더·모델이든 가능하다
(예: Anthropic, Claude Opus 4.8 (claude-opus-4-8) / OpenAI, GPT-5.x / Google, Gemini 3.x).
설계 모델과 구현 모델은 다를 수 있어 칸을 나눈다 — 같으면 같은 값을 적는다.
"audit 모델 ≠ 구현 모델" 조건을 나중에 명령으로 확인 가능하도록 기록한다. 모델 전환은 사람이 수행하므로 기계로 강제하기 어렵다.
구현에 여러 벤더가 관여했으면 audit 모델의 벤더는 Task별 `수행 모델`의 모든 벤더와도 달라야 한다
— 대표 행 비교만으로는 audit 벤더가 구현 일부에 참여한 경우를 놓친다.
-->

| 구분 | 모델 |
|------|------|
| 설계 모델 | Anthropic, Claude Fable 5 (claude-fable-5) |
| 구현 모델 | Anthropic, Claude Opus 5 (claude-opus-5) |
| audit 모델 | OpenAI, GPT-5 |

---

## Task별 수행 결과

<!--
Task 0 및 일반 실행 Task(Task N 제외) 블록의 지표 4종은 사후 집계용이라 표기를 고정한다
(값이 없어도 필드는 지우지 않는다. Task N 예외의 근거는 아래 Task N 블록 주석).
- 수행 모델: "벤더, 모델명" — `모델 기록` 표와 같은 형식. 한 Task를 모델 전환·재시도로 둘 이상이 수행했으면
  관여한 모델을 전부 ` / `로 나열한다 — 하나만 남기면 누락된 벤더가 audit을 맡아도 Task N의 강화 조건을 통과한다.
  `-`는 미착수·스킵 Task 전용이며, `결과`가 완료·부분 완료면 실제 값을 필수로 기록한다
  (`-`로 남기면 Task N의 교차모델 강화 조건이 공집합이 되어 우회가 된다)
- audit 발견: 이 Task와 관련된 audit 발견사항 건수 — 없으면 `0건` (`-` 금지)
- 보정 반영: `--response` 항목별 승인을 통과해 실제로 보정한 건수 — 없으면 `0건` (`-` 금지)
- 재시도: 같은 Task에서 재수정·재시도한 횟수 — 없으면 `0회` (`-` 금지)

한 발견이 여러 Task 산출물에 걸쳐도 가장 직접 수정된 주 Task 하나에만 귀속한다 —
`audit 발견`·`보정 반영` 모두 그 주 Task에서 1회만 세고 관련된 다른 Task에는 세지 않는다.
Task 축에서도 이슈 단위 합계가 중복으로 부풀지 않게 하기 위함이다(아래 회차 축 중복 제외와 같은 취지).

수치 3종은 회차별 값이 아니라 이슈 전체 누적이다 — 여러 차수 감사가 같은 발견을 재확인해도
1건으로 세고(중복 제외), 새 회차 값으로 덮어쓰지 않는다. 보정률 사후 집계의 분모·분자가 회차 반복으로 부풀지 않게 하기 위함이다.

수치 3종에 `-`를 쓰지 않는 이유: 아래 스니펫이 `[0-9]+`만 추출하므로 `-`인 Task는
합계에서 조용히 빠지고, 전부 `-`면 합계 자체가 빈 문자열이 된다. `-` 허용은 비수치 필드(`수행 모델`)에만 둔다.

수치 3종의 기본값은 주석이 아니라 리터럴 `0건`/`0회`로 둔다: 주석 placeholder는 렌더링 값이
비어 있는데도 집계기가 주석 안 숫자를 실제 값으로 오인하고, `미기록` 같은 형식 위반도 걸러지지 않는다.

보정률은 별도 필드로 두지 않고 `보정 반영 / audit 발견`으로 사후 집계한다.
아래 스니펫이 동작하도록 `- **<필드>**: <숫자><단위>` 표기를 지킨다 (줄 시작, 굵게, 콜론 뒤 숫자).

  # 이슈 하나의 보정률
  found=$(grep -E '^- \*\*audit 발견\*\*:' issue-0066-summary.md | grep -oE '[0-9]+' | paste -sd+ - | bc)
  fixed=$(grep -E '^- \*\*보정 반영\*\*:' issue-0066-summary.md | grep -oE '[0-9]+' | paste -sd+ - | bc)
  echo "보정률: ${fixed}/${found}"

  # 모델별 집계는 `수행 모델` 행과 함께 뽑는다
  grep -E '^- \*\*(수행 모델|audit 발견|보정 반영|재시도)\*\*:' issue-0066-summary.md
-->

### Task 0 (고정): 구현 시작 게이트 — 전제·모호점 확인

<!--
plan 템플릿의 첫 고정 Task에 대응하는 블록이다. 삭제하지 말 것.
질의로 해소한 전제는 spec `## 전제 (Assumptions)`에 남기고, 여기에는 수행 사실만 적는다.
나열한 항목이 없었으면 `수행 내용 요약`에 `전제 누락 없음` 한 줄을 기록한다.
-->

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: 전제·모호점 5건을 나열해 2건은 사용자 질의로 확정, 3건은 문서·코드 확인으로 해소했다. 확정분 3건(게이트 추출 방식, 템플릿 예시의 접기 내용, 안내 blockquote 갱신)을 spec `## 전제 (Assumptions)`에 반영해 미해소 0건.
- **특이 사항**: Task 4(GitHub 렌더링 확인)는 원격 브랜치·PR이 아직 없어 푸시가 선행되어야 한다 — 해당 시점에 별도 승인 필요.

---

### Task 1: spec 템플릿 `완료의 정의` 형식 변경

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: 안내 주석에 형식 규칙 3줄(본문=문장·레벨 태그·강등 사유 / 접기=검증 상세 / 접을 상세 없으면 본문 한 줄)을 추가하고, `[D]` 예시를 문장 본문 + 접기(bash 코드 블록 + 설계 주의)로 교체했다. 접기 제목 표준 문구는 `검증 명령 — 출력 0건이면 통과`. `[QD]`·`[ND]` 예시는 접을 상세가 없어 본문 한 줄로 남겨 두 형태를 함께 예시화했다.
- **특이 사항**: 주석·안내문에서는 여는 태그 단독 표기를 피했다 — DoD의 여닫이 짝 검증(`<details>` 개수 = `</details>` 개수)이 설명문의 태그 언급까지 세기 때문이다. 접기 안 예시 명령은 임시 디렉토리에서 실행해 동작을 확인했다(두 칸 공백 제외, 한 칸 공백 검출).

---

### Task 2: plan 템플릿 완료 기준 형식 적용 + `run-tests.sh` 추출 대응

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 3건
- **보정 반영**: 2건
- **재시도**: 2회
- **수행 내용 요약**: `## Tasks` 안내 blockquote에 완료 기준 형식 규칙을 추가하고, 예시 Task 1·2와 Task N 고정 블록의 `[D]` 3건을 문장 본문 + 접기(bash 코드 블록·설계 주의·의존 근거)로 재배치했다. 한 줄이던 awk/grep 명령은 여러 줄로 풀어 쓰고, `run-tests.sh`의 `extract_gate`/`extract_pair_gate`(sed 코드 스팬 추출) 2개 함수를 코드 블록 추출 awk 함수 1개로 통합했다 — 앵커 행 다음 첫 ```bash 블록에서 공통 들여쓰기를 벗기고 `P=`/`S=` 경로 할당 행을 제외해 뽑는다. 회귀 테스트 28건 전부 통과(기존 27건 + 경로 주입 계약 검사 1건 신설).
- **특이 사항**: Task 0 블록과 Task N 첫 항목은 접을 상세가 없는 단문이라 본문 한 줄로 유지했다. 추출 실패 모드 중 "경로 할당 행 잔존"은 정상 fixture 검사로만 간접 격추되던 것을 명시 검사로 승격했다.
- **audit 대응**: 교차모델 audit이 의존 관계 서술 배치를 부분 충족(PARTIAL)으로 지적 — 최초 구현은 본문에 선행 사실("아래 두 게이트의 선행 조건이다")만 남기고 우회 결과("그대로 통과한다"·"공집합이 된다"·"우회가 된다")를 접기 `의존 근거`로 내렸는데, 이는 접기 금지 기준(리스크)과 SKILL.md 자체 규칙에 어긋났다. `--response` 승인을 거쳐 Task N `[D]` 3건의 본문에 우회 결과를 올리고 접기에는 메커니즘만 남겼다. SKILL.md 의존 관계 규칙과 plan 안내 blockquote도 "결과는 본문, 메커니즘은 접기"로 함께 정리했다(주 Task 귀속 규칙에 따라 발견은 이 Task에만 계상). 나머지 1건(접기 구조 `[D]` 게이트가 태그 위치를 검사하지 않음)은 archive 후 재실행되지 않는 일회성 명령이고 Task N 게이트 3건은 `extract_gate` 추출 실패로 이미 감지되므로, 사용자 판단으로 known issue 수용했다. 2차 재감사는 그 보정의 부작용을 새 발견으로 지적했다 — Task N `[D]` 본문 3건이 마침표로 끊겨 두 문장이 되면서 자체 규칙("보장 내용 한 문장")과 어긋난 건으로, `--response` 승인을 거쳐 마침표를 연결 표현(`하며`·`이며`·`이라`)으로 바꿔 한 문장으로 되돌렸다(우회 결과는 본문 유지, 접기·코드 블록 미변경, 회귀 테스트 28건 통과). 구조 게이트 건은 2차·3차에서도 같은 지적으로 재확인됐으나 중복이라 건수에 세지 않고 수용을 유지했다. 3차 재감사는 신규 발견 0건으로 적합 판정이라 이 Task의 지표 3종은 그대로 둔다.

---

### Task 3: SKILL.md에 완료 기준 형식 규칙 기재

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: `## 산출물 접기 기준` 아래 `### 완료 기준 형식` 하위 절을 신설해 규칙 4항목(본문 / 접기 / 접지 않는 경우 / 의존 관계)과 앵커 보존 주의를 기재했다. `## 작업 진행 중`의 완료 기준 불릿에서 이 절을 가리키게 해 진입 경로를 만들었다. 앵커 문구 `완료 기준 형식`은 2회 등장.
- **특이 사항**: 새 절을 `## 옵션` 앞(접기 기준 절 하위)에 두어 `--response` ↔ `--clear` 사이 범위를 건드리지 않았다 — response 스모크가 그 구간을 계약으로 검사한다. 규칙을 접기 기준 절 하위에 붙인 이유는 접기 금지 기준(결정사항·리스크·액션 아이템)의 적용례라는 관계를 문서 구조로 드러내기 위함이다.

---

### Task 4: GitHub 렌더링 확인

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: `issue-0066` 브랜치를 푸시하고 GitHub 파일 뷰에서 spec·plan 템플릿의 접기 렌더링을 사용자가 확인했다 — 접힘/펼침 정상, 리스트 구조 유지, 코드 블록 표시 정상. 형식 조정은 불필요했다.
- **특이 사항**: 화면 판정 전에 GitHub 공식 렌더러(`gh api /markdown`, mode=gfm)로 HTML 구조를 사전 검증했다 — 접기 6개(spec 1·plan 5) 전부 `<li>` 안에 `<details>`가 들어가고 bash 하이라이팅과 하위 리스트가 유지됨을 확인. 화면 동작 자체는 명령으로 환원 불가라 사용자 확인으로 완료 처리했다.

---

### Task N (고정): 교차모델 issue-audit 검증 — 사용자 수동 수행

<!--
plan 템플릿의 마지막 고정 Task에 대응하는 블록이다. 삭제하지 말 것.
사용자가 직접 수행하며, 구현 AI는 이 블록을 대신 채우지 않는다.
지표 4종을 두지 않는다 — 수행 모델은 위 `모델 기록` 표의 `audit 모델` 행이 SSoT이고,
audit 발견·보정 반영은 이 Task가 만들어낸 값이라 각 대상 Task 블록에 집계된다.
-->

- **결과**: 완료
- **수행 내용 요약**: 사용자가 OpenAI GPT-5로 `issue-audit`를 3회 수행했고, 최종 리포트는 `./issue-0066-audit-report.md`에 있다(3차 재감사가 이전 회차를 덮어쓴 최신본). 누적 발견 3건은 전부 Task 2에 귀속되며 `--response` 승인을 거쳐 2건 보정·1건 수용했다. 3차 재감사 판정은 적합 — 충족(PASS) 11건, 미충족(FAIL)·부분 충족(PARTIAL) 0건, 신규 발견 0건.
- **특이 사항**: 3차에서 다시 올라온 F-1(접기 구조 `[D]` 게이트가 태그 위치를 판정하지 않음)은 1·2차와 같은 건이라 누적 집계에 중복으로 세지 않고 known issue 수용을 유지했다 — archive 후 재실행되지 않는 일회성 DoD 명령이고, 실제 보호가 필요한 Task N 게이트 3건은 `run-tests.sh`의 `extract_gate` 추출 실패로 감지된다. 상시 회귀 게이트로 승격할 때는 검사 범위를 완료 기준 하위로 제한하고 반례 fixture를 추가해야 한다. audit 모델 벤더(OpenAI)는 `모델 기록` 표의 구현 모델과 Task별 `수행 모델` 전체(모두 Anthropic)와 상이하다.
