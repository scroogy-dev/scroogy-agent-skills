# Issue #31 실행계획 교차모델 audit 리포트 검토→피드백→승인 실행 흐름 옵션화

> 스펙: [issue-0031-spec.md](./issue-0031-spec.md)

---

## Tasks

> AI가 순서대로 실행할 작업 단위를 정의합니다.
> 각 작업은 독립적으로 검증 가능해야 하며, **완료 기준은 자연어 대신 실행 명령 + 임계값**으로 적습니다.
> 명령으로 환원이 어려운 항목만 판정 주체(다른 AI / 사람)를 적고 강등 사유를 남깁니다.
> 마지막 고정 Task(교차모델 issue-audit)는 삭제하지 말고 그대로 둡니다.

### Task 1: 열린 결정 확정

- [ ] 완료
- **목표**: spec의 "열린 결정" 3건(옵션 이름, 자동탐색 경로, 옵션+절차 동시 반영 여부)을 확정한다.
- **작업 내용**:
  1. 옵션 이름 후보(`--feedback` / `--review-audit` / `--triage` / `--from-audit` 등)를 게이트 성격·호출 의도 기준으로 비교해 1개 확정한다 (대칭·메타포가 아니라 사용자가 그 기능을 부를 때의 의도 기준).
  2. 자동탐색 기본 경로(`.ai/99_workspace/issue-<번호>-audit-report.md`), 다중 리포트 처리, 인자 생략 시 동작을 확정한다.
  3. 옵션 플래그 + `issue-workflow.md` 절차 동시 반영 여부를 확정한다 (권장: 둘 다).
- **완료 기준**: summary "Task 1" 칸에 옵션 이름·자동탐색 동작·동시 반영 여부 3건이 한 줄씩 결론으로 기재된다 (사람 결정 사항이므로 [ND] — 강등 사유: 명령으로 합/불 판정 불가).

---

### Task 2: SKILL.md에 옵션 추가

- [ ] 완료
- **목표**: 확정한 옵션을 `issue-work/SKILL.md`의 `## 옵션` 섹션에 추가한다.
- **작업 내용**:
  1. `## 옵션`에 새 옵션 섹션을 추가하고 용도·동작(리포트 읽기 → 발견사항별 피드백만/수정 금지 → 보정 대상·방향 표 → 항목별 승인 → 승인분만 보정)을 기재한다.
  2. `issue-audit`과의 경계(audit 수행 = 사용자/`issue-audit`, 피드백·보정 = `issue-work`)를 옵션 설명에 명시한다.
- **완료 기준**: `grep -cE '^### `--' issue-work/SKILL.md` ≥ 4.

---

### Task 3: issue-workflow.md 절차 반영 (SSoT + active 동기화)

- [ ] 완료
- **목표**: 컨텍스트 초기화 후에도 게이트가 유지되도록 워크플로우 절차에 반영한다.
- **작업 내용**:
  1. `issue-work/templates/issue-workflow-template.md`(SSoT)에 "audit 리포트 검토 → 피드백 먼저 → 항목별 승인 → 승인분만 보정" 게이트 절차를 추가한다.
  2. active 복사본 `.ai/90_issues/active/issue-workflow.md`를 템플릿과 동일하게 동기화한다.
- **완료 기준**: `diff issue-work/templates/issue-workflow-template.md .ai/90_issues/active/issue-workflow.md` 차이 없음.

---

### Task N (고정): 교차모델 issue-audit 검증 — 사용자 수동 수행

<!--
이 블록은 모든 이슈 계획의 마지막 Task로 고정한다. 삭제하지 말 것.
audit은 L2 [QD] 보완 검증 — L1 [D] 결정적 게이트의 대체가 아니라 보완이다.
이 Task는 사용자가 직접 수행하며, 구현 AI는 자동으로 닫지 않는다.
-->

- [ ] 완료
- **목표**: 스펙 위반·누락·소스코드와의 모순을 구현 모델과 다른 시각으로 잡는다.
- **실행 주체**: **사용자가 직접** 수행한다. 구현 AI는 이 Task를 **자동으로 닫지 않으며**, `issue-audit`를 자동 실행하지도 않는다.
- **작업 내용**:
  1. spec `완료의 정의`의 `[D]` 항목 검증 명령을 전부 재실행해 통과를 확인한다.
  2. 계획·구현을 수행한 모델과 **다른 벤더 모델**(Non-Anthropic 포함, 최소 동급 이상 역량)로 사용자가 직접 `issue-audit`를 실행한다. 방향은 칭찬이 아니라 허점 탐색("스펙 위반·누락·소스코드와의 모순을 찾아라").
  3. 사용자가 audit 결과(지적 사항·감사 모델)를 summary에 반영·기록하고, 필요 시 앞 Task를 보정한다.
- **완료 기준**: `[D]` 검증 명령 전부 통과 + 구현 모델 ≠ audit 모델 기록이 summary 모델 칸("벤더, 모델명" 형식)에 남는다.
