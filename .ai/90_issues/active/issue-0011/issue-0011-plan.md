# Issue #11 실행계획 디렉토리 표현 순서를 IDE 기준으로 통일

> 스펙: [issue-0011-spec.md](./issue-0011-spec.md)

---

## Tasks

### Task 1: 영향 범위 조사 및 정렬 규칙 초안 작성

- [x] 완료
- **목표**: 디렉토리 트리/구조를 출력·생성하는 스킬 전수를 식별하고, 적용할 정렬 규칙 문구를 확정한다.
- **작업 내용**:
  1. `ai-workspace`, `readme-sync`의 SKILL.md/템플릿에서 디렉토리 표현 블록 위치를 정리한다.
  2. `ai-workspace-directory`, `code-map`, `context-harvest`, `install-skills`, `issue-work` 등에서 동일 패턴 사용 여부를 점검한다.
  3. 정렬 규칙 문구를 한 문장으로 확정한다 (알파벳순 case-insensitive, 디렉토리 우선, 파일 후행, 숨김 항목 처리 기준 포함).
  4. 공통 규칙으로 둘지(예: `.ai/10_rules/` 또는 ADR), 각 스킬에 명시할지 방향을 결정한다.
- **완료 기준**: 영향받는 스킬 목록과 정렬 규칙 문구, 배치 위치 결정이 summary에 기록됨.

---

### Task 2: 정렬 규칙을 영향 스킬에 반영

- [x] 완료
- **목표**: Task 1에서 결정한 규칙을 `ai-workspace`, `readme-sync` 및 추가 영향 스킬의 SKILL.md/템플릿에 명문화한다.
- **작업 내용**:
  1. 각 스킬 SKILL.md에 "디렉토리 표현 순서" 지시 문장을 동일한 표현으로 추가한다.
  2. 템플릿 내 예시 트리 블록이 새 규칙과 어긋나면 함께 재정렬한다.
  3. 본문 외 다른 부분은 건드리지 않는다 (최소 변경 원칙).
- **완료 기준**: 영향받는 모든 스킬에서 grep으로 규칙 문구가 검색되고, 예시 트리가 IDE와 동일한 순서로 정렬되어 있음.

---

### Task 3: 셀프 검증 및 PR 준비

- [ ] 완료
- **목표**: 변경된 스킬이 의도대로 동작하는지 확인하고 PR을 준비한다.
- **작업 내용**:
  1. 본 repo의 디렉토리 트리 표현(예: AI-CONTEXT.md의 "디렉토리 구조", README.md의 트리 블록)을 새 규칙으로 점검한다.
  2. 필요 시 본 repo 트리 블록을 같이 재정렬한다.
  3. `issue-0011-summary.md`를 최종 정리하고 PR 메시지를 작성한다.
- **완료 기준**: 본 repo 트리 표현이 IDE 표시 순서와 일치하며, summary에 검증 결과가 기록됨.
