# Issue #61 실행요약 — 기술부채·known issue 원장 도입

> 스펙: [issue-0061-spec.md](./issue-0061-spec.md) | 계획: [issue-0061-plan.md](./issue-0061-plan.md)

## 다음 작업

> ▶️ 다음 작업: Task N — 교차모델 issue-audit 검증 (사용자가 타벤더 모델로 직접 수행)

## 모델 기록

<!--
이슈 단위 요약(대표값)이다. 세부 SSoT는 아래 `Task별 수행 결과`의 `수행 모델`이며,
한 구분에 여러 모델이 관여했으면 대표 모델을 적고 세부는 Task별 기록을 본다.

형식: "벤더, 모델명". 설계·구현·audit 어느 칸이든 어느 벤더·모델이든 가능하다
(예: Anthropic, Claude Opus 4.8 (claude-opus-4-8) / OpenAI, GPT-5.x / Google, Gemini 3.x).
설계 모델과 구현 모델은 다를 수 있어 칸을 나눈다 — 같으면 같은 값을 적는다.
"audit 모델 ≠ 구현 모델" 조건을 나중에 명령으로 확인 가능하도록 기록한다. 모델 전환은 사람이 수행하므로 기계로 강제하기 어렵다.
구현에 여러 벤더가 관여했으면 audit 모델의 벤더는 Task별 `수행 모델`의 모든 벤더와도 달라야 한다
— 대표 행 비교만으로는 audit 벤더가 구현 일부에 참여한 경우를 놓친다.
-->

| 구분 | 모델 |
|------|------|
| 설계 모델 | Anthropic, Claude Fable 5 (claude-fable-5) |
| 구현 모델 | Anthropic, Claude Opus 5 (claude-opus-5) |
| audit 모델 | <!-- 구현 모델과 다른 벤더 모델. 형식: 벤더, 모델명. 마지막 교차모델 audit Task에서 사용자가 기록 --> |

---

## Task별 수행 결과

<!--
Task 0 및 일반 실행 Task(Task N 제외) 블록의 지표 4종은 사후 집계용이라 표기를 고정한다
(값이 없어도 필드는 지우지 않는다. Task N 예외의 근거는 아래 Task N 블록 주석).
- 수행 모델: "벤더, 모델명" — `모델 기록` 표와 같은 형식. 한 Task를 모델 전환·재시도로 둘 이상이 수행했으면
  관여한 모델을 전부 ` / `로 나열한다 — 하나만 남기면 누락된 벤더가 audit을 맡아도 Task N의 강화 조건을 통과한다.
  `-`는 미착수·스킵 Task 전용이며, `결과`가 완료·부분 완료면 실제 값을 필수로 기록한다
  (`-`로 남기면 Task N의 교차모델 강화 조건이 공집합이 되어 우회가 된다)
- audit 발견: 이 Task와 관련된 audit 발견사항 건수 — 없으면 `0건` (`-` 금지)
- 보정 반영: `--response` 항목별 승인을 통과해 실제로 보정한 건수 — 없으면 `0건` (`-` 금지)
- 재시도: 같은 Task에서 재수정·재시도한 횟수 — 없으면 `0회` (`-` 금지)

한 발견이 여러 Task 산출물에 걸쳐도 가장 직접 수정된 주 Task 하나에만 귀속한다 —
`audit 발견`·`보정 반영` 모두 그 주 Task에서 1회만 세고 관련된 다른 Task에는 세지 않는다.
Task 축에서도 이슈 단위 합계가 중복으로 부풀지 않게 하기 위함이다(아래 회차 축 중복 제외와 같은 취지).

수치 3종은 회차별 값이 아니라 이슈 전체 누적이다 — 여러 차수 감사가 같은 발견을 재확인해도
1건으로 세고(중복 제외), 새 회차 값으로 덮어쓰지 않는다. 보정률 사후 집계의 분모·분자가 회차 반복으로 부풀지 않게 하기 위함이다.

수치 3종에 `-`를 쓰지 않는 이유: 아래 스니펫이 `[0-9]+`만 추출하므로 `-`인 Task는
합계에서 조용히 빠지고, 전부 `-`면 합계 자체가 빈 문자열이 된다. `-` 허용은 비수치 필드(`수행 모델`)에만 둔다.

수치 3종의 기본값은 주석이 아니라 리터럴 `0건`/`0회`로 둔다: 주석 placeholder는 렌더링 값이
비어 있는데도 집계기가 주석 안 숫자를 실제 값으로 오인하고, `미기록` 같은 형식 위반도 걸러지지 않는다.

보정률은 별도 필드로 두지 않고 `보정 반영 / audit 발견`으로 사후 집계한다.
아래 스니펫이 동작하도록 `- **<필드>**: <숫자><단위>` 표기를 지킨다 (줄 시작, 굵게, 콜론 뒤 숫자).

  # 이슈 하나의 보정률
  found=$(grep -E '^- \*\*audit 발견\*\*:' issue-0061-summary.md | grep -oE '[0-9]+' | paste -sd+ - | bc)
  fixed=$(grep -E '^- \*\*보정 반영\*\*:' issue-0061-summary.md | grep -oE '[0-9]+' | paste -sd+ - | bc)
  echo "보정률: ${fixed}/${found}"

  # 모델별 집계는 `수행 모델` 행과 함께 뽑는다
  grep -E '^- \*\*(수행 모델|audit 발견|보정 반영|재시도)\*\*:' issue-0061-summary.md
-->

### Task 0 (고정): 구현 시작 게이트 — 전제·모호점 확인

<!--
plan 템플릿의 첫 고정 Task에 대응하는 블록이다. 삭제하지 말 것.
질의로 해소한 전제는 spec `## 전제 (Assumptions)`에 남기고, 여기에는 수행 사실만 적는다.
나열한 항목이 없었으면 `수행 내용 요약`에 `전제 누락 없음` 한 줄을 기록한다.
-->

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: 전제·모호점 5건을 나열해 사용자 질의로 전부 확정하고 spec `## 전제`에 반영했다 — (1) 원장 위치를 `.ai/70_ledger/` 최상위로 변경(이슈 본문 "확정"을 뒤집음), (2) 하위 구조를 `active/`+`archive/`로 분리, (3) `출처` 필드는 식별자만 기재, (4) index 골격은 ai-workspace 단독 소유, (5) audit 리포트의 기등재 항목은 별도 섹션. 아울러 이슈 #61 코멘트의 확정 사항(git-pr-feedback 연계·PR 스레드 출처)이 spec/plan에 누락돼 있어 Task 4로 신설하고, 기존 Task 4(ai-workspace)를 Task 5로 밀었다.
- **특이 사항**: 위치 변경이 이슈 본문 "배제한 대안"의 `.ai/70_*` 신설 항목과 상충하므로 **이슈 #61에 결정 갱신 코멘트를 등록했다(2026-08-02)** — 위치 변경 근거와 Task 0 확정 4건(출처 식별자 기재·index 골격 단독 소유·기등재 별도 섹션·git-review 제외)을 함께 담았다. 이후 **이슈 본문도 갱신해 게시했다(2026-08-03)** — 옛 경로 3곳을 `.ai/70_ledger/`로, 배제한 대안의 최상위 `.ai/70_*` 항목에 번복 표시와 근거를 달고, 코멘트로만 확정돼 있던 git-pr-feedback 연계·출처 필드 확장·#64 선행 의존을 본문에 반영했다. 본문·코멘트·spec·구현이 같은 위치와 같은 소비자 목록을 가리키므로 audit 1단계 요구사항 대조에 불일치가 남지 않는다. 조사로 확인한 부수 사실: `--clear` 5단계 리포트 이관이 원장 `출처` 경로를 깨뜨릴 수 있어 식별자 기재로 회피했고, `git-review`는 의견 유형 체계가 없어 이번 범위에서 제외했다.

---

### Task 1: 원장 항목 템플릿 정의

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: `issue-work/templates/ledger-entry-template.md` 신설 — 필수 필드 7종을 `- **<필드>**:` 앵커로 고정하고, 파일명 규칙(`K-<번호>-<slug>.md`, 4자리 zero-padding)·배치 위치(`active/` 등재, 청산 시 `archive/`)·출처의 경로 미기재 원칙을 본문에 명시했다. 완료 기준 2종 검증 명령 모두 출력 0건 통과.
- **특이 사항**: 템플릿 주석에 번호 채번 규칙을 명시했다 — `active/`와 `archive/`를 합쳐 최대 번호 + 1로 정한다. archive를 빼고 세면 청산된 항목의 번호를 재사용하게 되어 출처 추적이 깨진다. 재검토 이력 섹션은 "없음" 명시를 요구해 빈 섹션과 미수행을 구분한다.

---

### Task 2: issue-work `--response` 원장 이관 규칙 반영

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 1건
- **보정 반영**: 1건
- **재시도**: 0회
- **수행 내용 요약**: `issue-work/SKILL.md` `--response` 3단계에 이관 목적지를 `.ai/70_ledger/`로 고정하고, 5단계에 `원장 등재` 하위 항목을 신설해 등재 경로(`active/K-<번호>-<slug>.md`)·index 표 갱신·필수 필드 2종(수용 사유·재검토 조건)을 명시했다. `issue-workflow-template.md`에 이관 목적지 안내 행을 추가하고 `active/issue-workflow.md` 사본을 동기화했다(diff 0). 완료 기준 2종 검증 명령 모두 출력 0건 통과.
- **특이 사항**: "별도 이슈로 이관"을 원장 등재로 대체하되 이슈 승격 경로는 없애지 않고 **등재 후 항목 `상태`를 바꾸는 후속 단계**로 재배치했다 — 승격 경로를 삭제하면 원장이 이슈화 대상까지 흡수해 후속 조치가 묻힌다. 필수 필드 2종의 근거를 각각 분리해 적었다(수용 사유 없음 → 사유 없는 무한 보류, 재검토 조건 없음 → issue-audit이 재제기 시점을 판정 못 해 매 감사마다 신규 보고). 뒤 근거는 Task 3에서 구현할 기등재 판정 규칙과 맞물리므로 그 Task에서 표현이 어긋나지 않게 유지해야 한다. **audit 보정(F-3, 중간(MEDIUM))**: 등재는 신설만 규정하고 audit이 `기등재 K-n 재제기`로 올린 발견을 받았을 때 기존 항목을 갱신하는 경로가 없어, 같은 부채가 새 번호로 늘거나 해결된 항목이 `active/`에 남는 구멍이 있었다. `--response` 5단계에 재제기 처리 3갈래(계속 수용 → 재검토 이력·조건 갱신 / 승격 → `승격(이슈 #N)` + archive 이관 / 반영 → `해소(PR #N)` + archive 이관)와 "보정으로 원인이 사라진 기등재 종결" 규칙을 추가했다. 같은 종결 규칙을 등재 주체 다른 한 축인 `git-pr-feedback/SKILL.md`에도 대칭으로 넣어(코드 수정 → `해소`, 별도 이슈 → `승격`) 두 주체의 비대칭을 없앴고, `issue-workflow-template.md`와 active 사본에 재제기 시 새 번호 금지를 한 줄 반영했다.

---

### Task 3: issue-audit 원장 참조 규칙 반영

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 1건
- **보정 반영**: 1건
- **재시도**: 0회
- **수행 내용 요약**: `issue-audit/SKILL.md` 0단계에 원장 index 선택 적재 절차를 4번 항목으로 신설(이후 항목 번호 재정렬, 외부 참조 0건 확인)하고 `## 참조 문서`에 같은 경로를 추가했다. 2단계 `발견 기록 규칙`에 `기등재 대조 (번호 부여 전)` 규칙을 발견 번호 계승 규칙 **앞에** 배치해 `F-` 번호 부여 전에 판정하게 했고, 리포트 템플릿에 `### 기등재 참조 항목` 섹션을 발견 사항 표 바깥에 신설, `## 출력 요약 형식`에 기등재 참조 건수를 위험도 집계와 분리해 표기하는 줄을 추가했다. [D] 완료 기준 2종 검증 명령 모두 출력 0건 통과.
- **특이 사항**: 규칙 구멍 3건을 닫았다 — (1) 원장 부재 시 기등재 0건으로 보고 진행(감사 자체는 성립), (2) 위험도가 등재 시점보다 높아 보이면 재제기가 아니라 원장 갱신 권고로 유도(감사인 재평가로 재검토 조건을 우회하면 원장이 반복 보고를 막지 못함), (3) 원장 항목이 이미 해소·낡았을 때도 재보고 금지 + 갱신 권고 — 이 경로가 없으면 감사인이 낡은 원장을 발견하고도 적을 곳이 없다. 계보 표기에 `기등재 K-n 재제기`를 추가했다. **audit 보정(F-2, 낮음(LOW)으로 재평가)**: 위 (3)의 억제 규칙이 과잉이었다 — 원장 index는 대조 대상을 `active/`로 한정하는데 SKILL.md는 "원장(`.ai/70_ledger/`)"이라 전체로 읽혔고, 여기에 "이미 해소됐으면 재보고 금지"가 붙어 청산한 결함의 **회귀까지 억제**할 수 있었다. 대조 대상을 `active/`의 `수용` 항목으로 명시하고, `archive/`의 청산 항목과 같은 결함이 다시 나타나면 억제하지 않고 `F-` 번호·위험도 집계를 부여하는 신규 발견(계보 `K-<번호> 재발`)으로 보고하도록 갈랐다. 계보 표기 목록에 `기등재 K-n 재제기`와 `K-n 재발`을 함께 열거하고, 원장 index 2종에도 같은 경계를 반영했다. **[QD] 항목(기등재 판정 규칙이 0단계 적재 → 2단계 대조 흐름과 의미적으로 정합)은 Task N 교차모델 audit 채점 대상으로 남아 미확정이다.**

---

### Task 4: git-pr-feedback 원장 등재 선택지 추가

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: `git-pr-feedback/SKILL.md` `### 항목별 선택`에 `- **수용 — 원장 등재**` 선택지를 기존 3종 다음에 추가하고, 등재 규칙 5개 항목(템플릿·경로·채번, 필수 필드 2종, 출처 식별자 기재, 기등재 시 재확인, 승인 게이트 경계)을 목록 아래에 신설했다. `## 결과 요약` 표에 수용 항목 행과 `K-<번호>` 표기를 반영하고, `## 참조 문서`에 `.ai/70_ledger/index.md` 조건부 적재를, `## 관련 skill`의 issue-work 항목에 항목 템플릿 공유 관계를 추가했다. 선택지가 늘어 프론트매터 `description`과 `## 개요`의 대응 수단 서술도 "답글 게시·코드 변경·원장 등재"로 맞췄다. 완료 기준 검증 명령 4종 모두 출력 0건 통과.
- **특이 사항**: 규칙 구멍 3건을 닫았다 — (1) **기등재 중복 등재 금지**: 같은 지적이 원장에 있으면 새 번호를 따지 않고 기존 항목의 `재검토 이력`에 재확인을 남긴다(PR마다 새 번호로 쌓이면 원장이 중복 보고를 막지 못한다). (2) **승인 게이트 경계**: 원장 파일 생성은 로컬 변경이라 외부 공개 행위가 아니고, 함께 선택한 답글 게시와 등재 파일 커밋·push만 게이트 대상임을 명시했다. (3) **조치 실행 매핑 밖**: 원장 등재는 `gh`·MCP 매핑에 해당하지 않아 두 수단의 가용 여부와 무관하게 수행함을 적었다. 의견 유형 `수용(known issue)`(제안)과 처리 방식 `수용 — 원장 등재`(선택)의 관계를 한 문단으로 분리해, 등재하지 않은 수용은 추적처가 없어 다음 리뷰·감사에서 재보고된다는 점을 남겼다. 선택지 순서는 plan 지시대로 보류 뒤에 배치했다.

---

### Task 5: ai-workspace·안내도에 70_ledger/ 반영

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Opus 5 (claude-opus-5)
- **audit 발견**: 1건
- **보정 반영**: 1건
- **재시도**: 0회
- **수행 내용 요약**: 템플릿 골격 `ai-workspace/templates/shared/.ai/70_ledger/`를 신설했다 — `index.md`(`50_adr/index.md` 문체를 따른 파일 목록 + 항목 목록 표 + `## 수명 주기` 4단계 표) + `active/.gitkeep` + `archive/.gitkeep`. `context-loading.md` 참조 원칙 첫 행 열거, dev·doc AI-CONTEXT 템플릿 각 3곳(트리 `[6순위]` 행, 진입 절차 선택 적재, 작성 규칙 배치 열거), `ai-workspace/SKILL.md` update-3단계 3곳(대상 디렉토리 목록·하위 구조 표·이동 판단 기준 표)을 갱신했다. 이 repo에는 `.ai/70_ledger/` 첫 인스턴스를 만들고 `.ai/AI-CONTEXT.md` 3곳·`.ai/10_rules/context-loading.md`를 같은 형태로 맞췄다. 완료 기준 [D] 3종과 spec `완료의 정의` [D] 6종 전수 재실행 모두 출력 0건 통과.
- **특이 사항**: plan에 없던 보강 2건을 넣었다 — (1) **update의 신규 디렉토리 전파 경로**: update-3단계는 "정의된 하위 디렉토리 생성"만 규정해, 상위 디렉토리 자체가 없는 기존 설치본에는 `70_ledger/`도 골격 `index.md`도 생기지 않는다. 목록에만 추가하면 [D] 검증은 통과하나 "갱신되는 `.ai` 구조가 원장을 포함한다"는 Task 목표가 실효를 잃어, 대상 디렉토리 생성·골격 index 복사 규칙을 한 문단 추가했다(설치본에 이미 있으면 사용자 작성분이라 미덮어씀). (2) **소유 관계 명시**: 스킬 목록의 issue-work 행에 원장 등재 연계와 템플릿 분담(항목 템플릿=issue-work / index 골격=ai-workspace)을 적어 spec 전제의 단독 소유가 문서에서도 드러나게 했다. 이동 판단 기준은 본문 서술의 "승격" 단어에 오판정되지 않도록 `- **상태**:` 필드 앵커 값으로 판정하게 적었다. 미조치로 남긴 것 — ai-workspace의 "함께 사용 가능한 skill" 목록에 git-pr-feedback·issue-audit이 애초에 없어 원장 생산자 한 축이 그 목록에 드러나지 않는다. 목록 완전성은 이 이슈 범위 밖이라 손대지 않았다. **audit 보정(F-1, 중간(MEDIUM))**: update-3 "기존 파일 정리"가 `70_ledger/index.md`를 상태 앵커 없는 일반 항목으로 보고 `active/`로 옮길 수 있다는 지적이다. 확인 결과 이는 원장 고유 결함이 아니라 update-3에 **제어 파일 제외 규칙이 아예 없다**는 기존 구멍이었고, `50_adr/index.md`(→`active/`)·`40_domain/index.md`·`glossary.md`(→판단 불가 시 `legacy/`)가 이미 같은 방식으로 깨져 있었다. 리포트 권장대로 `70_ledger/` 행에만 예외를 달면 이웃 3건이 남으므로, 사용자 승인을 받아 판단 기준 표 위에 **루트 상주 파일 일반 제외 규칙**(각 디렉토리 `index.md`와 `40_domain/glossary.md`는 이동·`legacy/` 대상에서 제외)을 한 문단으로 넣어 네 건을 한 번에 닫았다.

---

### Task N (고정): 교차모델 issue-audit 검증 — 사용자 수동 수행

<!--
plan 템플릿의 마지막 고정 Task에 대응하는 블록이다. 삭제하지 말 것.
사용자가 직접 수행하며, 구현 AI는 이 블록을 대신 채우지 않는다.
지표 4종을 두지 않는다 — 수행 모델은 위 `모델 기록` 표의 `audit 모델` 행이 SSoT이고,
audit 발견·보정 반영은 이 Task가 만들어낸 값이라 각 대상 Task 블록에 집계된다.
-->

- **결과**: <!-- 완료 / 부분 완료 / 스킵 -->
- **수행 내용 요약**: <!-- audit 리포트 위치, 발견사항 건수, `--response` 검토 결과 -->
- **특이 사항**:
