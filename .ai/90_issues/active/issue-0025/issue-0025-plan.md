# Issue #25 실행계획 — issue-work 템플릿: 결정적 하네스 기반 DoD + 교차모델 audit

> 스펙: [issue-0025-spec.md](./issue-0025-spec.md)

---

## Tasks

> AI가 순서대로 실행할 작업 단위를 정의합니다.
> 각 작업은 독립적으로 검증 가능해야 하며, 완료 기준은 가능한 한 실행 명령으로 적습니다.

### Task 1: spec 템플릿 `완료의 정의` 섹션 보강

- [x] 완료
- **목표**: 완료 항목을 결정적 검증 단계로 내리도록 spec 템플릿이 유도하게 만든다.
- **작업 내용**:
  1. 검증 레벨(L1/L2/L3, `[D]`/`[QD]`/`[ND]`, 약어 범례 포함)과 핵심 규율을 주석으로 설명. 각 레벨 설명은 한 줄에 하나씩 리스트로 끊어 줄바꿈을 적용한다.
  2. 각 체크리스트 항목에 `레벨 태그 + 검증 수단(명령/테스트/스크립트 또는 판정 주체)`을 적는 예시 추가. 강등 시 사유 기재 형식 포함.
  3. 가짜 `[결정적]`·과한 자동화 함정 1줄 경고.
- **완료 기준**: `grep -E '\[D\]|\[QD\]|\[ND\]' issue-work/templates/issue-spec-template.md` 가 태그 3종을 모두 출력하고, "강등 사유"·"검증" 키워드가 존재한다.

---

### Task 2: plan 템플릿 — 완료 기준 명령화 + 교차모델 audit 고정 블록

- [x] 완료
- **목표**: Task 완료 기준을 명령+임계값으로 적게 하고, 마지막 Task로 교차모델 audit을 의무화한다.
- **작업 내용**:
  1. (a) 각 Task `완료 기준`을 자연어 대신 `실행 명령 + 임계값`으로 적도록 주석·예시 보강.
  2. (b) 마지막 고정 Task 블록 추가: ① 1단계(`[D]`) 검사 전부 통과 확인 → ② 계획·구현 모델과 **다른 모델**로 `issue-audit` 실행(허점 탐색 방향, 최소 동급 역량). audit이 `[QD]` 보완 검증임을 주석에 명시.
- **완료 기준**: `grep -i 'issue-audit' issue-work/templates/issue-plan-template.md` 가 고정 블록을 출력하고, "다른 모델"·"임계값"(또는 명령 예시) 키워드가 존재한다.

---

### Task 3: summary 템플릿 — 모델 기록 칸 추가

- [x] 완료
- **목표**: "audit 모델 ≠ 구현 모델" 조건을 나중에 명령으로 확인 가능하게 기록한다.
- **작업 내용**:
  1. 상단 또는 전용 섹션에 `계획·구현 모델` / `audit 모델` 이름 기록 칸 추가.
- **완료 기준**: `grep -iE '구현 모델|audit 모델' issue-work/templates/issue-summary-template.md` 가 두 칸을 모두 출력한다.

---

### Task 4: (선택) SKILL.md 규율 반영

- [ ] 완료
- **목표**: 워크플로우 설명에 규율을 1~2줄로 포인터화한다(템플릿이 SSoT).
- **작업 내용**:
  1. `issue-work/SKILL.md`에 "완료 기준은 결정적 단계로 내리고 마지막 Task는 교차모델 audit" 취지를 1~2줄 추가.
- **완료 기준**: `grep -iE 'audit|결정적' issue-work/SKILL.md` 가 추가 문장을 출력한다. (불필요하다고 판단되면 스킵하고 summary에 사유 기재)

---

### Task 5: 교차모델 issue-audit (이 이슈 자체에 dogfooding 적용)

- [ ] 완료
- **목표**: 이번에 의무화한 "마지막 Task = 교차모델 audit"을 이 이슈 자신에도 적용해 스펙 위반·누락·소스 모순을 잡는다.
- **작업 내용**:
  1. Task 1~3의 `[D]` 완료 기준 grep을 전부 재실행해 통과 확인.
  2. 계획·구현 모델과 **다른 모델**로 `issue-audit` 실행. 방향은 칭찬이 아니라 허점 탐색("스펙 위반·누락·소스 모순을 찾아라").
  3. 지적 사항을 summary에 반영하고, 필요 시 Task 1~4를 보정.
- **완료 기준**: `[D]` grep 전부 통과 + 다른 모델 audit 1회 수행 기록이 summary의 모델 기록 칸에 남는다(구현 모델 ≠ audit 모델).
