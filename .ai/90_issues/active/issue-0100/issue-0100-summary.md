# Issue #100 실행요약 issue-work·issue-audit: 요구사항 승인 게이트 신설과 계획 감사 모드(--plan)·계획 보정 도입

> 스펙: [issue-0100-spec.md](./issue-0100-spec.md) | 계획: [issue-0100-plan.md](./issue-0100-plan.md)

## 다음 작업

> ▶️ 다음 작업: Task N — 2차 최종 감사 리포트(`.ai/99_workspace/issue-0100-audit-report.md`)의 F-3을 `--response`로 보정 반영함(2026-09-09, 1차 F-1·F-2는 앞서 반영). spec `[D]` 재실행 뒤 사용자가 Task N 결과를 기록하거나 3차 감사 여부를 정한다

## 모델 기록

<!--
이슈 단위 요약(대표값)이다. 세부 SSoT는 아래 `Task별 수행 결과`의 `수행 모델`이며,
한 구분에 여러 모델이 관여했으면 대표 모델을 적고 세부는 Task별 기록을 본다.
형식: "벤더, 모델명". 계획 audit 모델은 계획 모델과, 최종 audit 모델은 구현 모델과 다른 벤더여야 한다.
계획 감사를 건너뛴 이슈는 계획 audit 모델과 아래 계획 감사 줄에 `생략`을 적는다.
-->

| 구분 | 모델 |
|------|------|
| 계획 모델 | Anthropic, Claude Fable 5.1 (claude-fable-5-1) |
| 계획 audit 모델 | 생략 |
| 구현 모델 | Anthropic, Claude Fable 5.1 (claude-fable-5-1) |
| 최종 audit 모델 | OpenAI, GPT-6 |

- **계획 감사**: 생략 (`--plan` 모드가 이 이슈의 산출물이라 수행할 수 없음. 요구사항 승인은 2026-09-08 대화로 받음)

---

## Task별 수행 결과

<!--
Task 0 및 일반 실행 Task(Task N 제외) 블록의 지표 4종은 사후 집계용이라 표기를 고정한다.
값이 없어도 필드는 지우지 않는다. 수치 3종은 `0건`/`0회` 리터럴이 기본값이며 `-` 를 쓰지 않는다.
집계는 issue-work `scripts/summarize-metrics.sh` 로 한다.
-->

### Task 0 (고정): 구현 시작 게이트 (전제·모호점 확인)

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Fable 5.1 (claude-fable-5-1)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: `--resume`으로 복구한 뒤 spec·plan을 다시 읽고 미결 4건을 사용자에게 질의했다(2026-09-09). 검토 필요 2(가짜 `[D]` 결정화 보류·원장 등재)·검토 필요 3(`--plan` 유지)·이 이슈의 계획 감사 `생략` 유지·이슈 본문 근거 없는 추가 항목 2건 포함 유지가 전부 제안값대로 승인되어 spec `전제`에 확인 일자를 반영했다. 그 외 추가 전제·모호점은 발견되지 않았다.
- **특이 사항**: 계획 세션(2026-09-08)과 구현 세션(2026-09-09)이 다르지만 모델은 같다. 전제 유실 점검은 세션 기준으로 수행했다.

---

### Task 1: 용어 통일 (계획 모델·최종 audit 모델·계획 종료 게이트·최종 감사)

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Fable 5.1 (claude-fable-5-1)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: 옛 표기를 새 용어로 치환했다. issue-work SKILL.md 7곳(관련 skill·새 이슈 시작 시 4단계·게이트 2건 문구·작업 진행 중·이슈 완료 시·`--clear` 1단계), 템플릿 3종(summary 표 행 `계획 모델`·`구현 모델`·`최종 audit 모델` 개명과 주석, plan 게이트 헤더·Task 0 주석·Task N `[QD]` 행 2개, workflow 이슈 완료 시 1단계), `check-clear.sh` 주석·메시지 5줄, 러너 fixture 헤더·케이스 이름·대조 문자열 4줄이다. issue-audit SKILL.md 개요에 기본 모드를 최종 감사로 부른다는 한 줄을 넣고, 갱신한 workflow 템플릿으로 `active/issue-workflow.md`를 덮어썼다. spec DoD R8 `[D]` 2건과 issue-work·issue-audit 러너가 통과했다.
- **특이 사항**: R8 DoD 둘째 항목이 summary 표 행 4개(`계획 audit 모델` 포함)를 요구해 그 행을 이 Task에서 신설했다(주석에 계획 모델과 다른 벤더, 생략 시 `생략` 명시). Task 4는 표 아래 `계획 감사` 줄과 spec·workflow 템플릿 반영을 맡는다.

---

### Task 2: issue-work SKILL.md 새 이슈 시작 절차 분할과 계획 감사 질의

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Fable 5.1 (claude-fable-5-1)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: "새 이슈 시작 시" 3단계를 3-1(승인 전: 목표·요구사항·연관 문서 후보 작성 뒤 승인 요청)과 3-2(승인 후: 완료의 정의·전제·plan·summary)로 나눴다. 승인 요청 형식(spec 경로와 목표 한 줄·포함·제외 목록만 제시, 전문 미출력, 이슈 본문 근거 없는 항목 표시·질의)을 적고, 4단계(계획 종료 게이트) 뒤에 5단계 계획 감사 질의를 넣었다(기본값 없음, 수행 시 타벤더 `issue-audit --plan` → `--response` → Task 0, 건너뜀 시 `생략` 기록 → Task 0). 관련 skill의 issue-audit 항목과 작업 진행 중에 계획 감사 연계를 한 줄씩 보강했다. spec DoD R1·R2 `[D]` 통과, 번호 단계 5개, issue-work 러너 통과.
- **특이 사항**: R1 `[QD]`(승인 요청 제시 형식이 `--clear` 3단계와 같은 방식으로 읽히는지)는 Task N 교차모델 audit 채점으로 남긴다.

---

### Task 3: issue-work --response 계획 리포트 탐색·spec·plan 보정

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Fable 5.1 (claude-fable-5-1)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: `--response` 1단계에 계획 감사 리포트(`issue-<번호>-plan-audit-report.md`) 자동 탐색과 두 종류 공존 시 목록 선택을 넣고, 2단계 1단계 발견 정의에 계획 감사 리포트의 대조 기준(이슈 본문 요구 항목 대비 spec 요구사항)을 병기했다. 5단계에 "계획 감사 리포트의 보정" 하위 항목을 추가해 영향 파일 목록에 spec·plan을 적고 승인분만 반영하며, 건수는 Task 블록 지표 대신 summary `계획 감사` 줄에 적는다는 규칙과 spec 요구사항이 바뀌면 3-1 승인 게이트를 다시 거친다는 규칙을 적었다. 용도 문단에 계획 감사 리포트도 대상임을 한 줄 보강했다. 번호 단계는 5개 그대로이고 spec DoD R3 `[D]`와 러너 `--response` 스모크가 통과했다.
- **특이 사항**: 없음.

---

### Task 4: issue-work 템플릿 반영

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Fable 5.1 (claude-fable-5-1)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: spec 템플릿 요구사항 주석에 이슈 본문 근거 표기 안내(예시 문구, 승인 요청에서 따로 표시됨)와 승인 뒤 완료의 정의·plan·summary 작성 순서를 넣었다. summary 템플릿 `모델 기록` 표 아래에 `- **계획 감사**: 생략` 줄과 주석(수행 시 `수행 · 발견 N건 · 보정 N건`, 지표 4종 집계 대상 아님, 첫 `### Task ` 앞 배치)을 두고 표 주석에 생략 표기 안내를 더했다. workflow 템플릿 "작업 진행 중"에 계획 감사 리포트를 `--response`로 Task 0 착수 전에 검토한다는 한 줄을 넣고 `active/issue-workflow.md`를 덮어썼다. spec DoD R4 `[D]` 2건(템플릿 앵커·행 개수·러너, `summarize-metrics.sh` 생략·수행 fixture 무위반)과 workflow diff 0건이 통과했다.
- **특이 사항**: `계획 audit 모델` 행은 Task 1에서 신설했다(R8 DoD 요구). 이 Task는 그 행이 정확히 1개인지만 재확인했다.

---

### Task 5: issue-audit 추적성 헬퍼 check-plan.sh와 테스트

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Fable 5.1 (claude-fable-5-1)
- **audit 발견**: 3건
- **보정 반영**: 3건
- **재시도**: 0회
- **수행 내용 요약**: `issue-audit/scripts/check-plan.sh --trace <spec> <plan>`을 만들었다(실행 권한, 머리말 주석·`usage`·종료 코드 규약은 `classify-risk.sh`와 같은 형식). 판정 4종(`R 없는 DoD 그룹`·`DoD 없는 R`·`Task 없는 R`·`대상 요구사항 없는 일반 Task`)을 1행씩 내고, 고정 Task는 `### Task 0 (고정)`·`### Task N (고정)` 두 헤더만 제외한다. 러너에 정상 fixture 통과, 반례 4종 격추, 고정 단어 오분류 없음, `(고정)` 개서 우회 격추, 고정 Task 필드 미집계, 공집합 2종 격추, 사용오류 6종(exit 2) 케이스 16건을 추가했다(159건 통과). spec DoD R6 `[D]` 2건(러너 케이스·종료 코드, 이 이슈 spec·plan 자체 통과)과 Task 고유 검사가 통과했다.
- **특이 사항**: spec R6의 4종 외에 "추적 불가" 출력 2종을 더했다. 포함 목록이 비었거나 plan에 Task 블록이 없으면 무출력·종료 0이 되어 경로 오기가 통과로 보이기 때문이다(check-clear.sh의 Task 블록 없음 처리와 같은 취지, 종료 코드 1). 고정 Task 블록의 `대상 요구사항` 필드는 참조 집계에 넣지 않는다. Task가 spec에 없는 R을 가리키는 경우(dangling)는 판정 4종 밖이라 잡지 않는다. 이 검사는 issue-work 러너가 템플릿에 대해 수행한다. 1차 최종 감사(OpenAI, GPT-6)의 F-1(값 없는 필드를 연결로 인정)·F-2(펜스·주석·절 밖 앵커 집계)를 2026-09-09 `--response`에서 승인받아 반영했다. 필드는 값에 R<n>이 있을 때만 연결로 세고, 두 파서가 코드 펜스·HTML 블록 주석 안의 행을 건너뛰며, DoD 그룹은 `## 완료의 정의` 절 안에서만 센다. 러너 케이스 10건을 추가했다(169건 통과). 2차 최종 감사(OpenAI, GPT-6)의 F-3(F-2 잔여. 값 안의 한 줄 주석, 백틱 4개 예시 안의 백틱 3개 행, 물결표 펜스, 주석 안의 펜스 행을 오판정)을 2026-09-09 `--response`에서 승인받아 반영했다. 값의 닫힌 주석을 지운 뒤 R<n>을 세고, 펜스는 여는 문자·길이를 기억해 같은 문자를 같은 개수 이상 쓴 행에서만 닫으며(`~~~` 포함), 판정 순서를 주석 상태·펜스 상태·펜스 열기·주석 열기로 고정했다. 러너 케이스 10건을 더했다(179건 통과).

---

### Task 6: issue-audit --plan 모드 소절과 리포트 템플릿 주석

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Fable 5.1 (claude-fable-5-1)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: issue-audit SKILL.md 끝에 `## 옵션` → `### --plan` 소절 1개를 신설했다. 시점(계획 종료 게이트 뒤·Task 0 앞, 사용자가 다른 벤더 모델로 직접, 자동 실행 금지)·입력(spec·plan, diff 제외)·대조 기준(GitHub 이슈 본문, 30_contract·40_domain·50_adr·70_ledger)·1단계(이슈 본문 요구 항목 대비 spec 요구사항 대조, 판정 값 4종 매핑, DoD 표는 포함 R ↔ DoD 그룹)·2단계 5관점(추적성 `check-plan.sh --trace` 호출, 가짜 `[D]` 구현 전 트리 실행과 회귀 방지 예외 표시, 범위·모호성·과잉 설계 `[QD]`)·공통 규칙(위험도·처리·이모지·판정·원장 대조·번호 헬퍼 동일)·결과 기록(`issue-<번호>-plan-audit-report.md`, 회차 보존·이관 동일, `F-` 축 분리)을 적었다. frontmatter description과 `## 개요`·`## 관련 skill`에 모드 존재를 한 줄씩 넣고, 리포트 템플릿 1단계 표 주석에 `--plan` 모드의 행 의미를 적었다. spec DoD R5 `[D]`(소절 1개·앵커 16종·템플릿 주석·description)와 issue-audit 러너(리포트 구조 스모크 포함)가 통과했다.
- **특이 사항**: `--plan` 소절 안에는 `####` 하위 헤더를 두지 않았다. spec R5 검증 명령이 다음 `##+` 헤더에서 절을 끊기 때문이다. R5 `[QD]`(최종 감사 절차와의 정합, 회귀 방지 예외 표시 서술)는 Task N 채점으로 남긴다.

---

### Task 7: 안내도 정합

- **결과**: 완료
- **수행 모델**: Anthropic, Claude Fable 5.1 (claude-fable-5-1)
- **audit 발견**: 0건
- **보정 반영**: 0건
- **재시도**: 0회
- **수행 내용 요약**: `.ai/AI-CONTEXT.md`의 issue-audit `scripts/` 행에 `check-plan.sh`를, 스킬 목록 `issue-audit` 행에 최종 감사·`--plan` 계획 감사 설명을, `## 이슈 작업 워크플로우` 표에 `--plan`과 "계획·구현 검증 시"를 반영하고 `last updated`를 2026-09-09로 갱신했다. spec 전제 "검토 필요 2"에 따라 원장 `K-0009`(계획 감사의 가짜 `[D]` 사전 판별 헬퍼 미결정화, 기술부채, 낮음(LOW), 수용 사유·재검토 조건 기재)를 `.ai/70_ledger/active/`에 등재하고 `index.md` 항목 목록을 갱신했다. spec DoD R7 `[D]`, 공통 `[D]` 2건(전체 스킬 러너 통과, 변경·신규 md 줄 끝 공백 0건)이 통과했다.
- **특이 사항**: 원장 등재는 spec 전제가 "이슈 완료 시"로 적었지만 Task N이 전제 이행을 확인할 수 있게 이 Task로 앞당겼다(plan Task 7 작업 내용 4항 추가). K-0009 위험도는 `classify-risk.sh --impact '기능 저하' --likelihood '특수 조건·엣지'`로 산출했다.

---

### Task N (고정): 교차모델 issue-audit 검증 (사용자 수동 수행)

<!--
사용자가 직접 수행하며, 구현 AI는 이 블록을 대신 채우지 않는다.
지표 4종을 두지 않는다. 수행 모델은 위 `모델 기록` 표의 `최종 audit 모델` 행이 SSoT이고,
audit 발견·보정 반영은 각 대상 Task 블록에 집계된다.
-->

- **결과**: <!-- 완료 / 부분 완료 / 스킵 -->
- **수행 내용 요약**: <!-- audit 리포트 위치, 발견사항 건수, `--response` 검토 결과 -->
- **특이 사항**:
