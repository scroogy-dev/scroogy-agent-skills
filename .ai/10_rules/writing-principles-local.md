# 산출물 작성 원칙 (로컬 확장)

> 이 파일은 **사용자가 관리**한다. ai-workspace update는 파일이 없을 때만 이 빈 템플릿을 복사하며, 있으면 보존한다 (init 완전 재설치는 `.ai/` 전체를 초기화하므로 예외).
> 동기화 원본 `writing-principles.md`와 규칙이 충돌하면 **local 우선**이다. 이 파일의 규칙을 따른다.

<!-- 이 repo 고유의 산출물 서술 규칙을 추가하세요. 예:
- 용어: "고객" 대신 "회원"으로 표기
- 분량: 리뷰 코멘트는 10줄 이내
- 표기: 날짜는 YYYY-MM-DD 형식
-->
