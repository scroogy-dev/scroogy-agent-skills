# 리뷰 결과

<!-- 판정 한 줄 → 최종 의견 접기 → 요약 → 비즈니스 리뷰 → 테크 리뷰 순으로 쓴다. 판정과 요약은 두 리뷰를 모두 마친 뒤 맨 위에 작성한다 — 결론이 먼저 보이는 역피라미드 구조. 작성 규칙은 git-review/SKILL.md "결과 기록 형식" 참조 -->

<판정>

<!-- 판정은 SKILL.md "판정 산출"의 헬퍼 `--verdict` 출력을 그대로 한 줄로 적는다 — 🟢 승인(APPROVE) / 🟡 조건부 승인(CONDITIONAL) / 🔴 변경 요청(REQUEST CHANGES). 리뷰어가 직접 고르지 않는다 -->

<details>
<summary>최종 의견 펼치기</summary>

<!-- 리뷰 종합 판단과 후속 액션 — 3문장 이내. 판정 자체는 위 한 줄이 맡는다 -->

</details>

## 요약

- 비즈니스 리뷰: <이모지> <상태>
- 테크 리뷰: <이모지> <상태>
- 검토 파일: N개 <!-- 리뷰 대상 diff의 변경 파일 수 -->

<!-- 상태는 SKILL.md "상태 산출"의 헬퍼 `--status` 출력을 그대로 쓴다 — 🟢 통과(PASS) / 🟡 주의(WARN) / 🔴 보완 필요(FAIL). 리뷰어가 직접 고르지 않는다 -->

| 카테고리 | 건수 | 상태 |
|----------|------|------|
| 기능 | <등급별 건수 — 텍스트 등급만, 예: 높음(HIGH) 1 · 낮음(LOW) 2, 발견 없으면 0> | <이모지> <상태> |
| 아키텍처 |  |  |
| 버그 |  |  |
| 보안 |  |  |
| 코드품질 |  |  |
| 성능 |  |  |
| 테스트 |  |  |
<!-- 발견 0건 카테고리도 행을 남긴다 — 검증을 수행했다는 증거. 건수 열에는 이모지를 붙이지 않는다 -->

## 비즈니스 리뷰

<!-- 도메인 정책·소프트웨어 계약 관점의 리뷰 포인트를 아래 블록 형식으로 적는다. 발견이 없으면 "발견 없음" 한 줄만 둔다 -->

<!-- 블록 규칙: 볼드 제목 한 줄 → 일반체 지적·권장 조치 1~3문장 → 항목 바로 아래 근거 접기.
     접기 밖에 제목·지적 사항·권장 조치를 두고, 접기 안에 영향·발생확률 축 판정·근거·코드 위치·호출 흐름을 둔다.
     항목을 리스트가 아니라 문단으로 두는 이유는 리스트 항목 안에 접기를 넣으면 GitHub·IDE 미리보기에서 렌더링이 깨지기 때문이다.
     리뷰 포인트가 여럿이면 이 블록을 같은 형식으로 반복한다. 카테고리·섹션 단위 접기는 두지 않는다.
     등급은 SKILL.md "위험도 분류"의 헬퍼로 산출하고 이모지는 그 출력을 그대로 쓴다. 종합 판단은 상단 최종 의견 접기에 적는다 -->

**<이모지> <등급> <제목>**

<지적 사항과 권장 조치 — 일반체 1~3문장>

<details>
<summary>근거 펼치기</summary>

<!-- 영향 축 / 발생확률 축 판정 한 줄, 참조 문서 대조 근거 -->

</details>

## 테크 리뷰

<!-- 발견이 있는 카테고리만 아래 소절을 반복한다. 발견 0건 카테고리는 요약 표로 충분하다 -->

### <카테고리> — <이모지> <상태>, N건

**<이모지> <등급> <제목>**

<지적 사항과 권장 조치 — 일반체 1~3문장>

<details>
<summary>근거 펼치기</summary>

<!-- 영향 축 / 발생확률 축 판정 한 줄, 코드 위치, 호출 흐름 추적 -->

</details>

<!-- 소절 안에 리뷰 포인트가 여럿이면 위 블록을 같은 형식으로 반복한다 -->

<!-- 섹션별 접기에 담기 어려운 공통 참고자료가 있으면 마지막에 "## 부록" 접기 섹션을 추가한다 -->
