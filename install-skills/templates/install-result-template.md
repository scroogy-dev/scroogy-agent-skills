<!-- 설치 결과 보고 형식 — 파일 산출물이 아니라 install-skills/SKILL.md "설치 절차" 8단계에서 사용자에게 보고하는 대화 출력 구조다 -->

## 경로별 결과

| 대상 경로 | 검증 결과 | 설치 개수 |
|-----------|-----------|-----------|
| <대상 경로> | <PASS 또는 FAIL> | <N>개 |

<!--
- 데이터 행은 자리표시자다 — 설치 대상 경로 수만큼 행을 만들고 실제 실행값으로 채운다. 예시값을 그대로 복사하지 않는다.
- 검증 결과는 6단계 verify-install.sh 출력의 `RESULT:` 행을 그대로 옮긴다 — PASS / FAIL. AI가 재판정하지 않는다.
- 헬퍼는 `--target`을 여러 개 받으면 RESULT를 하나로 합산하므로, 경로별 표기는 경로마다 실행한 결과로 채운다.
- FAIL 행이 있으면 표 아래에 헬퍼가 출력한 `FAIL:` 사유 행을 그대로 나열한다 — 리스크라 접지 않는다.
- 설치 개수는 그 경로에 복사한 skill 개수다.
-->

## 설치된 skill 목록

- `<skill명>` — <해당 SKILL.md frontmatter `description`의 한 줄 요약>

<!--
- 설명의 출처는 "스캔" 절과 같은 frontmatter `description`이다. 원문을 전량 전재하지 않고 핵심 기능 중심의 한 줄 요약본으로 노출한다
  — 전량 전재는 말미 트리거 문장(skill 선택용 검색어 나열)까지 보고에 싣는다.
- 요약 문구는 세션마다 조금씩 다를 수 있다 — 이 템플릿이 고정하는 대상은 보고 구조이지 설명 문구가 아니다 (이슈 #84 스펙 전제).
- 경로마다 같은 목록이 설치되므로 한 번만 나열한다. 경로별로 대상이 달랐을 때만 경로를 함께 적는다.
-->

## 적용 옵션 내역

- **경로 옵션**: <지정한 경로 옵션 — 없으면 기본값 `~/.claude/skills/`>
- **`--clear`**: <지정 / 미지정>
- **`--self`**: <지정 / 미지정 — 지정 시 install-skills 자신도 설치 대상>
- **배포 제외 패턴**: `tests/`, `*.test.*`
- **Antigravity 레거시 점검**: <수행 / 미수행>
  - 판정: <헬퍼 `RESULT:` 행 그대로 — PASS 또는 FAIL>
  - 상세: <헬퍼 `INFO:` 행이 있으면 그대로 옮김 — 심링크·부재·빈 디렉토리>
  - 조치: <FAIL일 때만 7단계 조치 결과. PASS면 `조치 없음`>

<!--
- 레거시 점검은 --antigravity 또는 --all 일 때만 수행한다. 대상이 아니면 `미수행 (대상 경로 아님)`으로 적고 하위 3행을 두지 않는다.
- INFO는 판정값이 아니라 부가 정보다 — 헬퍼의 최종 판정은 `RESULT:` 행 2종(PASS·FAIL)뿐이며, 심링크·부재·빈 디렉토리는 `INFO:` 상세를 남기고 PASS로 끝난다.
-->
